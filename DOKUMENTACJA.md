# Dokumentacja projektu — INSTYTUTem.pl (redesign wizualnie wzorowany na archon.au)

> Ten plik to żywy dziennik dokumentacji technicznej projektu. Uzupełniany na bieżąco w trakcie pracy — po każdej istotnej decyzji, dodanej zależności lub sprawdzonej dokumentacji zewnętrznej biblioteki.
>
> Design system marki: patrz [DESIGN-SYSTEM.md](DESIGN-SYSTEM.md) — obowiązuje bez wyjątków przy każdej zmianie wizualnej.
>
> Postęp i checklisty: patrz [PLAN.md](PLAN.md) — zawiera stały proces weryfikacji wizualnej wobec archon.au, obowiązujący przy każdej zmianie kodu.
>
> Wnioski i błędy z dotychczasowej pracy, koncepcja projektu i zasady współpracy: patrz [CLAUDE.md](CLAUDE.md) — wczytywany automatycznie na start każdej sesji, żeby nie powtarzać tych samych pomyłek.

---

## Zasada nadrzędna: zachowanie SEO przy migracji na www.instytutem.pl

Ustalone 2026-08-27 — priorytet ekstra wysoki, obowiązuje przy każdej decyzji o strukturze URL, treści i meta danych.

**Kontekst:** Ta strona (budowana na bazie designu archon.au) **zastąpi żywą, działającą stronę www.instytutem.pl** — prawdziwy salon kosmetologiczny w Płocku z ugruntowanymi pozycjami w Google. To migracja/redesign istniejącej strony z ruchem organicznym do stracenia, nie budowa od zera. Domena pozostaje ta sama: **www.instytutem.pl**.

### Twarda zasada #1: te same adresy URL

Każdy dziś zaindeksowany adres URL musi zostać zachowany **dokładnie taki sam** (1:1 path) w nowej wersji. Jeśli któryś musi się zmienić — wymagane jest przekierowanie **301** (permanent, nigdy 302), bez wyjątków.

### Lista adresów URL do zachowania (zebrana z menu nawigacji instytutem.pl, 2026-08-27)

⚠️ **Niekompletna z założenia** — pochodzi z crawlu menu nawigacji, nie z pełnego indeksu. Przed wdrożeniem sprawdzić kompletną listę w Google Search Console (Coverage/Indexed pages) lub `sitemap.xml` na instytutem.pl (może zawierać dodatkowe strony, np. pojedyncze wpisy blogowe).

```
/zabiegi
/zabiegi/depilacja-laserowa-plock-lightsheer
/zabiegi/endermologia-plock-lpg-alliance
/zabiegi/kriolipoliza-plock-cooltech
/zabiegi/lipoliza-iniekcyjna-plock
/zabiegi/karboksyterapia-plock
/zabiegi/dermapen-plock-mikronakluwanie
/zabiegi/wypelnianie-kwasem-hialuronowym-plock
/zabiegi/peelingi-medyczne
/zabiegi/trychologia
/zabiegi/nici-liftinguj%C4%85ce-pdo
/zabiegi/mikrodermabrazja
/zabiegi/mezoterapia-iglowa-plock
/zabiegi/plasma
/zabiegi/endermologia-twarzy-plock-lpg-endermolift
/zabiegi/oczyszczanie-wodorowe-plock
/laser-tulowy-plock                      ← UWAGA: bez prefiksu /zabiegi/, niespójne z resztą — zachować mimo to
/radiofrekwencja-mikroiglowa-plock       ← UWAGA: bez prefiksu /zabiegi/
/fototerapia-dermalux-plock              ← UWAGA: bez prefiksu /zabiegi/
/fibryna-i-osocze-bogatoplytkowe         ← UWAGA: bez prefiksu /zabiegi/
/pakiet-powitalny-dla-nowych-klientow
/polec-nas
/pakiety
/blog
/na-raty
/kontakt
/cennik
/polityka-prywatności
```

Niespójności w strukturze (część zabiegów pod `/zabiegi/...`, część luzem w root) to dziś obowiązujący, zaindeksowany stan — **nie porządkować ich przy okazji redesignu**. Ujednolicenie struktury URL to osobny przyszły projekt, robiony świadomie z przekierowaniami 301, nie efekt uboczny zmiany wyglądu.

### Reszta checklisty technicznego SEO (obowiązuje przy wdrożeniu)

| Co | Dlaczego |
|---|---|
| Ta sama treść tekstowa na każdej podstronie (nie skracać przy redesignie) | Głębia/jakość treści wpływa na ranking |
| Te same lub świadomie poprawione title + meta description | Nie zmieniać losowo przy okazji redesignu |
| Ta sama struktura nagłówków (H1/H2) na stronach zabiegów | Wpływa na to, na jakie frazy strona rankuje |
| Schema.org (LocalBusiness / Service) przeniesione i uzupełnione | Wsparcie lokalnego SEO |
| Szybkość strony (Core Web Vitals) nie gorsza niż dziś | Bezpośredni czynnik rankingowy |
| NAP (nazwa/adres/telefon) identyczne wszędzie, w tym Google Business Profile | Niespójność = utrata lokalnego SEO |
| robots.txt / sitemap.xml zaktualizowane i zgłoszone w GSC po starcie | Przyspiesza ponowne zaindeksowanie |
| Mobile-first, HTTPS zachowane | Już jest — nie regresować |

### Proces wdrożenia (minimalizacja ryzyka)

1. Budowa na stagingu (subdomena testowa) z `<meta name="robots" content="noindex">`.
2. Przed publikacją: test każdego przekierowania (jeśli jakieś wystąpi).
3. Publikacja **jednorazowo, w całości** — nie mieszanka starych i nowych stron.
4. Po starcie: zgłoszenie nowego sitemap.xml w Search Console, monitoring Coverage report przez 2–4 tygodnie.

### Otwarty problem do rozwiązania przed wdrożeniem

Obecny `index.html` w repo zawiera wciąż **treść przeniesioną z archon.au** (Australia, Teneriffe/Brisbane, inny adres/telefon/zabiegi) przetłumaczoną na polski — to był etap "odtwórz design 1:1", zanim ustalono, że efekt końcowy zastąpi instytutem.pl. Przed wdrożeniem cała treść (usługi, adres Płock, dane kontaktowe, oferta) musi zostać zastąpiona prawdziwą treścią instytutem.pl, zachowując przy tym design system z DESIGN-SYSTEM.md i strukturę URL z listy wyżej.

### Zasada: podział zabiegów na „dla kobiet"/„dla mężczyzn" — tylko przy depilacji laserowej

Ustalone z użytkownikiem 2026-08-27, w odpowiedzi na pytanie o dobrą praktykę SEO przy takim podziale.

**Decyzja:** Podział zabiegów wg płci **nie jest stosowany uniwersalnie** — tylko przy depilacji laserowej.

- **Depilacja laserowa** — podział uzasadniony: różne partie ciała (mężczyźni: plecy/klatka/broda; kobiety: nogi/bikini/pachy), różne cenniki per partia, realnie różny search intent (ludzie osobno wpisują frazy z „dla mężczyzn"/„dla kobiet"). Wdrożenie: **nowe, dodatkowe** podstrony (np. `/zabiegi/depilacja-laserowa-dla-mezczyzn-plock`), linkowane z istniejącego, dziś rankującego huba (`/zabiegi/depilacja-laserowa-plock-lightsheer`) — bez ruszania tego URL, celujące w inne (long-tail) frazy niż hub, żeby nie kanibalizować własnego rankingu.
- **Pozostałe ~17 zabiegów** (RF mikroigłowa, mezoterapia, peelingi, endermologia twarzy, plasma, mikrodermabrazja, kriolipoliza itd.) — **bez podziału płciowego**. To ten sam zabieg dla obu płci; rozbicie na dwie wersje tworzy duplicate/thin content i rozprasza dzisiejszy ranking pojedynczej strony na dwie słabsze.

**Dlaczego:** Archon.au (źródło wizualne) ma pełny podział nawigacji Men/Women dla wszystkich kategorii zabiegów — ale to czysto wizualna/strukturalna cecha australijskiej strony, nigdy nie zweryfikowana pod kątem realnej treści i SEO instytutem.pl. Zgodnie z ustaloną koncepcją projektu (wizualnie archon.au, treściowo instytutem.pl) nie wolno kopiować tej struktury nawigacji bez sprawdzenia, czy pasuje do rzeczywistej oferty i zachowania wyszukiwania klientów instytutem.pl.

**Konsekwencja dla kodu:** obecne mega-menu „Mężczyźni"/„Kobiety" w `index.html` (linie ok. 80–125, dziedziczone 1:1 z archon.au) wymaga przebudowy — zostaje tylko przy depilacji laserowej, reszta zabiegów wraca do wspólnej, niepodzielonej listy zgodnej z faktyczną strukturą `/zabiegi` na instytutem.pl. Zadanie w [PLAN.md](PLAN.md).

**Do weryfikacji przed ostatecznym wdrożeniem:** Google Search Console (raport Queries) pokazałby, czy realni użytkownicy faktycznie wpisują frazy z „dla kobiet"/„dla mężczyzn" przy innych zabiegach niż depilacja — obecna decyzja oparta jest na ogólnej wiedzy branżowej, nie na danych z konta GSC instytutem.pl (użytkownik nie potwierdził jeszcze dostępu).

---

## Zasady prowadzenia dokumentacji

1. **Context7** (`context7@claude-plugins-official`) jest używany do pobierania aktualnej, wersjonowanej dokumentacji bibliotek/frameworków przed ich użyciem — zamiast polegać na wiedzy pamięciowej, która może być nieaktualna.
2. Każdy wpis w sekcji „Log decyzji" zawiera: datę, co zostało sprawdzone/zdecydowane, i dlaczego.
3. Każda nowo dodana zależność (biblioteka, framework, narzędzie) trafia do sekcji „Stos technologiczny" wraz z wersją i krótkim uzasadnieniem wyboru.
4. Dokumentacja jest pisana na bieżąco, nie retrospektywnie na koniec projektu.
5. **Sekcja „Lista zabiegów (oferta)" musi być aktualizowana przy KAŻDYM dodaniu lub usunięciu zabiegu z oferty** — w tej samej rundzie, w której zmienia się kod. Ustalone 2026-08-27 po tym, jak menu „Zabiegi" (nav/drawer/stopka) i galeria kart na stronie głównej rozjechały się — dwa zabiegi (Laser Tułowy, Fototerapia LED Dermalux) trafiły do nawigacji w jednej rundzie, ale nie do galerii, bo nie istniało jedno miejsce z pełną, aktualną listą do sprawdzenia. Przy każdej zmianie oferty: (a) zaktualizuj listę niżej, (b) sprawdź nią wszystkie 4 miejsca, w których oferta się powiela (nav desktop, drawer mobilny, stopka, galeria `#treatments`), (c) zanotuj rundę w logu decyzji.

---

## Lista zabiegów (oferta) — źródło prawdy

> **Jedyne, aktualne źródło prawdy dla oferty zabiegów.** Ta sama treść (nazwa, kolejność, URL) musi występować w **4 miejscach** w `index.html`: mega-menu „Zabiegi" w nagłówku (desktop), rozwijana lista w drawerze mobilnym, kolumna „Zabiegi" w stopce, oraz galeria kart `#treatments` na stronie głównej. Przy dodaniu/usunięciu/zmianie nazwy zabiegu: zaktualizuj najpierw tę listę, potem wszystkie 4 miejsca w kodzie, na końcu zanotuj rundę w logu decyzji niżej. Stan na 2026-08-27 (Runda 27).

### Zabiegi Hi-Tech (11)

| # | Nazwa | URL | Zdjęcie w galerii |
|---|---|---|---|
| 1 | Epilacja laserowa LightSheer® | `/zabiegi/depilacja-laserowa-plock-lightsheer` | ✅ `epilacja-laserowa-lightsheer.jpg` (AI, Runda 50 — druga wersja, gładka skóra po korekcie z Rundy 49; pierwsza wersja z Rundy 45 zastąpiła stockowe zdjęcie z CDN archon.au) |
| 2 | Laser Tulowy Erbowo-Szklany | `/laser-tulowy-plock` | ✅ `laser-tulowy-erbowo-szklany.jpg` (AI, Runda 51 — druga wersja, plecy z subtelną blizną zamiast gałązki eukaliptusa z pierwszej wersji z Rundy 43) |
| 3 | Endermologia LPG® Alliance | `/zabiegi/endermologia-plock-lpg-alliance` | ✅ `endermologia-lpg-alliance.jpg` (AI, Runda 46 — zastąpiło mylnie podpięty `womens-laser-hair-removal.jpg` z CDN archon.au) |
| 4 | LPG® Ergolift | `/zabiegi/lpg-ergolift` | ✅ `lpg-ergolift.jpg` (AI, Runda 44) |
| 5 | Kriolipoliza cooltech® | `/zabiegi/kriolipoliza-plock-cooltech` | ✅ `kriolipoliza-cooltech.jpg` (AI, Runda 47 — zastąpiło `mens-waxing.jpg` z CDN archon.au) |
| 6 | Bloomea® | `/zabiegi/bloomea` | ✅ `bloomea.jpg` (AI, Runda 52 — zastąpiło `led-light-therapy.jpg` z CDN archon.au) |
| 7 | Oczyszczanie wodorowe | `/zabiegi/oczyszczanie-wodorowe-plock` | ✅ `oczyszczanie-wodorowe.jpg` (AI, Runda 53 — zastąpiło `laser-genesis-facial.jpg` z CDN archon.au) |
| 8 | Fototerapia LED Dermalux® | `/fototerapia-dermalux-plock` | ✅ `fototerapia-dermalux.jpg` (AI, Runda 55) |
| 9 | Plasma™ | `/zabiegi/plasma` | ✅ `plasma.jpg` (AI, Runda 56) |
| 10 | Radiofrekwencja mikroigłowa RF | `/radiofrekwencja-mikroiglowa-plock` | ✅ `radiofrekwencja-mikroiglowa.jpg` (AI, Runda 57 — zastąpiło `fractional-rf.jpg` z CDN archon.au) |
| 11 | Mikronakłuwanie SkinPen® | `/zabiegi/dermapen-plock-mikronakluwanie` | ✅ `mikronaklowanie-skinpen.jpg` (AI, Runda 58 — zastąpiło `microneedling.jpg` z CDN archon.au) |

### Zabiegi Iniekcyjne (9)

| # | Nazwa | URL | Zdjęcie w galerii |
|---|---|---|---|
| 12 | Autologiczne wypełniacze ATR | `/zabiegi/autologiczne-wypelniacze-atr` | ✅ `autologiczne-wypelniacze-atr.jpg` (AI, Runda 59) |
| 13 | Kwas polimlekowy PLLA | `/zabiegi/kwas-polimlekowy-plla` | ✅ `kwas-polimlekowy-plla.jpg` (AI, Runda 60) |
| 14 | Mezoterapia igłowa | `/zabiegi/mezoterapia-iglowa-plock` | ✅ `mezoterapia-iglowa.jpg` (AI, Runda 61) |
| 15 | Kwas hialuronowy | `/zabiegi/wypelnianie-kwasem-hialuronowym-plock` | ✅ `kwas-hialuronowy.jpg` (AI, Runda 62 — zastąpiło `lip-blush-tattoo.jpg` z CDN archon.au) |
| 16 | Nici liftingujące PDO/PLLA | `/zabiegi/nici-liftinguj%C4%85ce-pdo` | ✅ `nici-liftingujace-pdo-plla.jpg` (AI, Runda 63) |
| 17 | Fibryna i osocze bogatopłytkowe | `/fibryna-i-osocze-bogatoplytkowe` | ✅ `fibryna-osocze-bogatoplytkowe.jpg` (AI, Runda 64) |
| 18 | Stymulatory tkankowe | `/zabiegi/stymulatory-tkankowe` | ✅ `stymulatory-tkankowe.jpg` (AI, Runda 65) |
| 19 | Lipoliza Iniekcyjna | `/zabiegi/lipoliza-iniekcyjna-plock` | ✅ `lipoliza-iniekcyjna.jpg` (AI, Runda 66 — zastąpiło `womens-waxing.jpg` z CDN archon.au) |
| 20 | Karboksyterapia | `/zabiegi/karboksyterapia-plock` | ✅ `karboksyterapia.jpg` (AI, Runda 67 — zastąpiło `tattoo-removal.jpg` z CDN archon.au) |

### Zabiegi Manualne (4)

| # | Nazwa | URL | Zdjęcie w galerii |
|---|---|---|---|
| 21 | Mezoterapia mikroigłowa | `/zabiegi/mezoterapia-mikroiglowa` | ⬜ placeholder |
| 22 | Mezoterapia bezigłowa | `/zabiegi/mezoterapia-bezig%C5%82owa` | ✅ `mezoterapia-beziglowa.jpg` (AI, Runda 68) |
| 23 | Mikrodermabrazja | `/zabiegi/mikrodermabrazja` | ✅ `mikrodermabrazja.jpg` (AI, Runda 69 — zastąpiło `tier-2-facials.jpg`, ostatnie z CDN archon.au) |
| 24 | Peelingi Medyczne | `/zabiegi/peelingi-medyczne` | ✅ `peelingi-medyczne.jpg` (AI, Runda 70) |

**Razem: 24 zabiegi, 23 z prawdziwym zdjęciem, 1 placeholder** (Mezoterapia mikroigłowa, `.ph-gradient`, wstrzymana — patrz „Otwarte TODO" w [PLAN.md](PLAN.md)).

**Pozycje niepewne / do potwierdzenia z użytkownikiem** (nie ruszane bez pytania):
- **LPG® Ergolift** (`/zabiegi/lpg-ergolift`) — URL wymyślony w Rundzie 9 (brak dopasowania w liście realnych, zaindeksowanych adresów z sekcji SEO wyżej w tym dokumencie). Podczas przeglądu w Rundzie 27 zauważono, że realny, zaindeksowany URL `/zabiegi/endermologia-twarzy-plock-lpg-endermolift` („Endermologia Twarzy LPG Endermolift™") mógłby być tym samym zabiegiem pod inną nazwą — ale użytkownik już raz odrzucił dodanie „Endermologia Twarzy" jako osobnej pozycji (Runda 13, „duplikat Endermologii Twarzy"), więc **nie zmieniano tego bez pytania**. Jeśli „LPG® Ergolift" to faktycznie ten sam zabieg, URL wymaga poprawki.
- **Trychologia** — nie jest osobnym zabiegiem w tej liście; potraktowana jako „Konsultacja trychologiczna" w menu „Konsultacje" → „Dla Twarzy" (ustalone w innej rundzie równoległej sesji).

---

## Stos technologiczny

| Technologia | Wersja | Status | Uzasadnienie |
|---|---|---|---|
| HTML5 / CSS3 / JavaScript (vanilla, ES6) | — | Aktywny | Strona marketingowa jednostronicowa (na razie) — brak potrzeby frameworka czy build-stepu. Najprostszy sposób na wierne, kontrolowane odtworzenie 1:1 designu z DESIGN-SYSTEM.md. Łatwy hosting gdziekolwiek (statyczne pliki). |
| Google Fonts (Tenor Sans, Inter) | CDN | Aktywny | Kroje wymagane przez design system (DESIGN-SYSTEM.md §3), ładowane przez `<link>` w `<head>`. |

---

## Log decyzji i konsultacji dokumentacji

| Data | Temat | Źródło (Context7 / inne) | Notatka |
|---|---|---|---|
| 2026-08-26 | Instalacja narzędzi | `claude plugin install superpowers@claude-plugins-official`, `claude plugin install context7@claude-plugins-official` | Oba pluginy zainstalowane w zakresie użytkownika (dostępne we wszystkich projektach na tej maszynie). Superpowers dostarcza metodykę pracy (TDD, debugging, planowanie). Context7 dostarcza aktualną dokumentację bibliotek na żądanie. |
| 2026-08-26 | Stos technologiczny strony głównej | Plan wdrożenia (agent planujący) | Wybrano czysty HTML/CSS/JS bez frameworka i bez build-stepu — zgodnie z prośbą „zrób tak samo jak na archon.au”, priorytet: wierność designowi, prostota, łatwy hosting. Node/npm/git dostępne w środowisku, ale świadomie nieużywane na tym etapie. |
| 2026-08-26 | Zbudowano stronę główną (index.html) | — | Pełny zakres z archon.au: nav + mega-menu, hero, marquee korzyści, filozofia marki, RX Facials (3 karty), karuzela 7 opinii, kontakt (3 kolumny), vouchery, banner rezerwacji, newsletter, stopka. Zweryfikowano wizualnie (desktop + mobile) i funkcjonalnie (mega-menu, hamburger, karuzela, formularz) w przeglądarce. |
| 2026-08-26 | Kadr „łuk” — miejsce na stronie głównej | Plan wdrożenia | Jedyne wystąpienie na tej stronie: zdjęcie towarzyszące tekstowi w sekcji „Filozofia marki” (brak sekcji zespołu w treści, więc to jedyne naturalne miejsce zgodne z DESIGN-SYSTEM.md §5). |
| 2026-08-26 | Formularz newslettera — brak backendu | Plan wdrożenia | Zamiast udawać sukces, JS przechwytuje submit i pokazuje uczciwy komunikat: „Sign-ups aren't connected yet — needs an email service…”. Docelowo do podłączenia pod Mailchimp/Klaviyo/Formspree lub inny ESP. |
| 2026-08-26 | Tryb ciemny systemu | Plan wdrożenia | Świadomie **nie** wdrożono `prefers-color-scheme` — to strona firmowa, ma wyglądać identycznie niezależnie od motywu systemu odwiedzającego (inaczej niż artefakt-dokumentacja design systemu, który się dostosowuje). |
| 2026-08-26 | Instalacja `chrome-devtools-mcp@claude-plugins-official` | `claude plugin install chrome-devtools-mcp@claude-plugins-official` | Zainstalowany w zakresie użytkownika. Prawdziwy serwer MCP (Puppeteer/Chrome DevTools Protocol) + skille do debugowania a11y, wydajności (LCP), wycieków pamięci. Uzupełnia dotychczasowy podgląd w przeglądarce o głębszą diagnostykę (trace'y wydajności, network, źródłowe stack trace'y w konsoli). Wymaga restartu sesji, żeby narzędzia się załadowały. |
| 2026-08-26 | Wprowadzono stały proces weryfikacji wizualnej | Porównanie bezpośrednie z żywą archon.au | Od teraz każda zmiana kodu przechodzi przez porównanie zrzut-do-zrzutu z archon.au (patrz [PLAN.md](PLAN.md)). Pierwsza runda ujawniła i naprawiła: (1) konflikt CSS `padding` w `.nav-row`/`.section-pad` łamiący padding `.container`; (2) złe łamanie nagłówka hero (max-width 680→720px, line-height 1.08→1.2); (3) błędną kolejność przycisków w nav; (4) całkowicie błędną strukturę sekcji „Filozofia marki” — oryginał używa siatki 1:2 (nagłówek/akapity) z linią-separatorem nad eyebrow, nie pojedynczego bloku tekstu; (5) błędne miejsce sygnaturowego kadru-łuk — to cały poziomo przewijany rząd 11 kart zabiegów, nie jedno zdjęcie obok tekstu; (6) błędne tło sekcji Vouchers (miało być cream, jest black-olive) i zły wariant przycisku (outline→white); (7) marquee było źle umieszczone zaraz po hero — w oryginale jest między bannerem rezerwacji a newsletterem, na tle cream (nie olive). |
| 2026-08-26 | Ustalono „Zasady współpracy” | Zrzut ekranu od użytkownika | 7 stałych reguł pracy (PRD jako źródło prawdy, aktualizacja TODO, przypominanie o commitach, KISS/DRY, jasność, minimalne przykłady kodu) — zapisane w [CLAUDE.md](CLAUDE.md) (sekcja „Zasady współpracy”, wczytywana automatycznie na start sesji) i w pamięci Claude na przyszłe sesje. |
| 2026-08-26 | Pierwszy commit git | `git init` + `git commit` (hash `9e5c727`) | Repo zainicjowane, tożsamość gita skonfigurowana przez użytkownika (`git config --global user.name/email`, poza tym co Claude może robić automatycznie). Wszystkie 6 plików projektu w jednym commicie startowym. |
| 2026-08-27 | Druga runda weryfikacji (Chrome DevTools MCP) | Bezpośrednie pomiary DOM na żywej archon.au | Użytkownik zgłosił, że strona nadal nie wygląda jak oryginał — słusznie. Znaleziono i naprawiono: (1) **prawdziwe logo to plik SVG** z CDN archon.au, nie tekst Tenor Sans — pobrano za zgodą (`assets/images/archon-logo.svg`, 4.3 KB); (2) margines kontenera był 2x za mały (40px zamiast realnych ~72px/5%); (3) brakowało strzałek dropdown przy Laser/Men/Women/About; (4) karty w rzędzie zabiegów miały złe proporcje (243×600px zamiast 282×450px); (5) **sekcja kontaktu miała złe tło** (cream zamiast black-olive) i złą stylistykę (linki zamiast obramowanych boxów z przyciskami); (6) karuzela opinii miała zupełnie inny układ niż oryginał (wyśrodkowanie, gwiazdki, kropki paginacji zamiast tekstu "Slide X of Y"); (7) brakowało ikon social media w stopce. **Najważniejsze znalezisko**: animacja scroll-reveal (opacity:0 + IntersectionObserver) potrafiła zostawiać całe sekcje niewidocznymi — to prawdopodobnie główna przyczyna wrażenia "strona nie wygląda jak oryginał". Animację usunięto całkowicie. |
| 2026-08-27 | Trzecia runda — dopracowanie header + hero (na prośbę użytkownika) | Realna interakcja (klik/hover) na żywej archon.au przez Chrome DevTools MCP | Naprawiono: (1) **realny błąd layoutu hero** — `.hero{display:flex}` powodował, że treść kurczyła się i centrowała zamiast rozciągać na całą szerokość, przez co nagłówek/przyciski nie zaczynały się przy tej samej krawędzi co nawigacja; (2) dodano animację hover na wszystkich przyciskach (`translateY(-8px)` + cień, wartości zmierzone bezpośrednio z żywej strony); (3) usunięto domyślne obramowanie przycisku „Skin” (globalny reset stylów `<button>`); (4) powiększono i ujednolicono strzałki dropdown (SVG chevron); (5) **całkowicie przebudowano mega-menu** z błędnej 3-kolumnowej siatki na prawdziwą strukturę — jedna wąska kolumna (300px) lewo-wyrównana pod przyciskiem, ze wszystkimi kategoriami zabiegów ułożonymi pionowo. Przy okazji znaleziono i wstawiono prawdziwe linki: rezerwacje (`bookings.gettimely.com/archonspas/book`), vouchery (`.../purchase`), e-maile kontaktowe (`hello@archonspas.com.au`, `hello@archon.au`), Privacy Policy/Terms. |
| 2026-08-27 | Czwarta runda — dalsze dopracowanie header (na prośbę użytkownika) | Bezpośrednie pomiary DOM na żywej archon.au | Użytkownik zgłosił 3 dalsze problemy z headerem, wszystkie potwierdzone i naprawione: (1) **rozmiar czcionki linków nawigacji** — było `.8rem` (12.8px), realnie jest **14px**; (2) **wysokość nagłówka zmieniała się przy scrollu** — miałem efekt kurczenia się (`.nav--scrolled`), którego oryginał w ogóle nie ma (wysokość zawsze stała 80px) — usunięto całą funkcję z CSS i JS; (3) **błędna struktura „menu przyklejone do prawej”** — na oryginale nawigacja (Skin/Laser/.../Contact) i przyciski CTA (Book Online/Gift Vouchers) to jedna wspólna grupa wyrównana do prawej krawędzi (duży pusty odstęp między logo a menu), a nie trzy elementy rozłożone równomiernie przez `justify-content:space-between`. Przebudowano HTML (nowy wrapper `.nav-right` łączący `<nav>` i `.nav-ctas`) i CSS. Po poprawce prawa krawędź przycisku „Book Online” pasuje co do piksela do oryginału. |
| 2026-08-27 | Piąta runda — mega-menu Laser/Men/Women/About | Bezpośrednia interakcja (klik) na żywej archon.au przez Browser MCP, odczyt `href` z DOM | Użytkownik wkleił zrzuty innej strony (Wix Studio, „INSTYTUTem.™”) jako wzór struktury mega-menu; po doprecyzowaniu ustalono: styl ma zostać z archon.au, a treść/struktura ma odpowiadać realnej zawartości. Otworzono każdy z 4 dropdownów na żywej archon.au i spisano realną treść + linki — okazało się, że **żywa archon.au już ma dokładnie taki wzór** (nagłówek kategorii z podkreśleniem + lista, wąska 1 kolumna, po 2 grupy kategorii na dropdown), więc nie było konfliktu ze zrzutami użytkownika. Dodano 4 nowe mega-panele (Laser: Laser Treatments/Laser Facials; Men: Male Grooming/Men's Spa Treatments; Women: Women's Beauty/Pamper Zone; About: Customers/Archon) z realnymi etykietami, z reużyciem już istniejących anchorów gdzie treść się pokrywa (np. `skin.html#lip-blush`, `skin.html#microblading` w menu Women — te same zabiegi co w Skin; `about.html#loyalty`, `about.html#careers`, `about.html#business` z istniejącej stopki). JS w `js/main.js` był zakodowany na sztywno tylko pod Skin (`skinBtn`/`skinPanel`) — przebudowany na pętlę po tablicy 5 par przycisk/panel, z zachowaniem tej samej logiki (klik otwiera/zamyka, klik poza zamyka, Escape zamyka + zwraca fokus) plus dodatkowo: otwarcie jednego menu zamyka pozostałe. Dodano analogiczne rozwijane listy `<details>` w mobilnym drawerze (płaska lista bez nagłówków kategorii, tak jak już było zrobione dla Skin). Zweryfikowano wizualnie 1:1 z żywą archon.au (wszystkie 4 nowe panele, desktop) oraz funkcjonalnie w mobile (Chrome DevTools/Browser MCP, lokalny serwer `python3 -m http.server 8080`). |
| 2026-08-27 | Tłumaczenie całej strony na polski | Prośba użytkownika w czacie | Przetłumaczono cały widoczny tekst w `index.html` na język polski: nawigację, wszystkie mega-menu (Skin/Laser/Men/Women/About) i ich odpowiedniki w mobilnym drawerze, hero, sekcję filozofii marki, podpisy 11 kart zabiegów, sekcję RX Facials (nagłówki, opisy, 3 karty zabiegów, cenę z „$” zachowanym jako waluta AUD), 7 opinii klientów wraz z etykietami dostępności (aria-label „X z Y”, „Opinia Google”), sekcję kontaktu, vouchery, banner rezerwacji, marquee korzyści, newsletter (w tym komunikat błędu w `js/main.js`) i stopkę. Zmieniono też `<html lang="en">` → `lang="pl"` oraz meta title/description. **Świadomie NIE przetłumaczono**: nazwy marki „Archon”, adresów URL/anchorów (`skin.html#tier-1` itd. — to identyfikatory techniczne, zmiana zepsułaby spójność z resztą kodu i mega-menu), realnych danych kontaktowych (adres, numer telefonu), klas/id w HTML i CSS. Zweryfikowano wizualnie w przeglądarce (desktop, lokalny serwer `python3 -m http.server 8080`) — dłuższe polskie frazy w mega-menu (np. „Depilacja Brazylijska dla Kobiet”, „Możliwości Biznesowe”) mieszczą się bez zawijania w istniejącej szerokości panelu (300px). |
| 2026-08-27 | Analiza struktury instytutem.pl + Fresha | Bezpośredni crawl obu stron (Browser MCP, a11y tree + tekst strony) | Na prośbę użytkownika przeanalizowano strukturę www.instytutem.pl (mega-menu 18 zabiegów + 3 promocje, hero z 3 tabami, cytat właścicielki, karuzela opinii, rozbudowana stopka z godzinami/IBAN/regulaminami) oraz profil na Fresha (kategoryzacja usług problem-based, zespół, 33 recenzje 5.0/5, zakup karnetów/voucherów). Przygotowano mapę sekcji (artifact) oraz propozycję sitemapu podstron w [ANALIZA-INSTYTUTEM.md](ANALIZA-INSTYTUTEM.md). |
| 2026-08-27 | **Ustalono zasadę nadrzędną: zero utraty SEO przy migracji** | Rozmowa z użytkownikiem — potwierdzone: nowa strona zastąpi żywą www.instytutem.pl na tej samej domenie | Użytkownik potwierdził, że ta strona ma **zastąpić** obecną, żywą www.instytutem.pl (z ugruntowanymi pozycjami w Google), nie działać jako nowy, niezależny projekt. Ustalono twardą zasadę: każdy dziś zaindeksowany URL musi być zachowany 1:1, zmiany tylko przez przekierowania 301. Zebrano wstępną listę URL do zachowania z menu nawigacji (niekompletną — wymaga uzupełnienia przez Google Search Console/sitemap.xml przed wdrożeniem). Pełna zasada i checklista: patrz sekcja „Zasada nadrzędna: zachowanie SEO przy migracji na www.instytutem.pl” wyżej. Zapisano też jako zasadę w [CLAUDE.md](CLAUDE.md), wczytywaną automatycznie na start każdej sesji. **Otwarty problem**: obecny `index.html` ma wciąż treść z archon.au (Australia) przetłumaczoną na polski, nie prawdziwą treść instytutem.pl — do podmiany przed wdrożeniem. |
| 2026-08-27 | Ustalono zasadę: podział zabiegów wg płci tylko przy depilacji laserowej | Rozmowa z użytkownikiem — pytanie o dobrą praktykę SEO przy podziale na „dla kobiet"/„dla mężczyzn" | Przeanalizowano ryzyko SEO podziału zabiegów wg płci: uzasadniony tylko przy depilacji laserowej (realnie różna treść — inne partie ciała, inne cenniki, różny search intent), szkodliwy dla pozostałych ~17 zabiegów (duplicate/thin content, rozproszenie rankingu jednej dziś silnej strony na dwie słabsze). Ustalono: **nowe** podstrony dla depilacji dla kobiet/mężczyzn dodawane obok istniejącego, dziś rankującego huba (bez ruszania jego URL), reszta zabiegów bez podziału. Zidentyfikowano niespójność: obecne mega-menu „Mężczyźni"/„Kobiety" w `index.html` jest 1:1 skopiowane z archon.au (czysto wizualna struktura, nigdy niezweryfikowana pod treść/SEO instytutem.pl) — wymaga przebudowy, zadanie dodane do [PLAN.md](PLAN.md). Pełny opis decyzji: patrz sekcja „Zasada: podział zabiegów na „dla kobiet"/„dla mężczyzn"..." wyżej. |
| 2026-08-27 | Szósta runda — przebudowa sekcji „Opinie klientów” na podstawie pomiaru DOM | Bezpośredni pomiar `getBoundingClientRect`/`getComputedStyle` + realna interakcja (klik strzałek) na żywej archon.au przez Browser MCP | ⚠️ **Błąd znaleziony i naprawiony w Rundzie 31** — punkt (1) niżej był niepoprawny, patrz log tej rundy. Na prośbę użytkownika („odwzoruj ją 1:1 jak na oryginale”) zmierzono sekcję testimonial na żywo zamiast bazować na poprzednim przybliżeniu z Rundy 2. Znaleziono i naprawiono: (1) ~~**zmyśloną linię „Opinia Google - X gwiazdek”** — nie istnieje w oryginale (sam DOM oryginału ma tylko cytat + imię + 5 SVG gwiazdek, żadnego tekstu o źródle opinii); usunięta zgodnie z zasadą „nie dodawaj niczego niepotwierdzonego w oryginale”~~ **(2026-08-27, Runda 31: BŁĘDNE — ten dopisek istnieje naprawdę na archon.au, po prostu został przeoczony przy tym pomiarze; przywrócony)**; (2) **zły font cytatu** — było Tenor Sans (display serif) 1.3rem/1.5, realnie jest **Inter 16px/27.2px** (400); (3) **nagłówek sekcji skalował się z szerokością okna** (dziedziczył `clamp()` z `.h-section`) zamiast być stały **30px/500/-1px** jak na oryginale — dodano scoped override tylko dla tej sekcji (`.testimonial-section .h-section`), żeby nie ruszać wspólnej klasy używanej też w innych sekcjach; (4) **gwiazdki jako tekst ★** zamienione na 5 osobnych SVG (16×20px, gap 4px, ścieżka narysowana od zera — nie skopiowana z zasobu CDN Webflow, żeby nie powielać cudzego pliku 1:1); (5) **strzałki karuzeli** przeniesione z rzędu pod treścią (razem z kropkami) na boki 800px-owego boksu — na oryginale to `display:flex;gap:37px` z boksem 800px pośrodku, kropki osobno wyśrodkowane pod spodem; zweryfikowane, że przy 1280px szerokości okna pozycje X strzałek wychodzą identyczne co do piksela (140.5 / 1069.5); (6) **przełączanie slajdów** zmienione z `display:none/block` na `translateX` z przejściem 0.5s + animowaną wysokością maski dopasowującą się do aktywnego slajdu (oryginał też ma dynamiczną wysokość) — responsywność oparta o procenty (`width:calc(100%/7)` per slajd) zamiast sztywnych px, żeby nie psuć się na węższych oknach. Zweryfikowano klikiem obu strzałek na lokalnym serwerze (`python3 -m http.server 8080`, dodano `.claude/launch.json`) — przejście, zmiana kropki aktywnej i wysokości maski działają poprawnie. **Przypomnienie z tej rundy**: treść karuzeli (nazwiska/cytaty australijskich klientów, przetłumaczone na polski) to nadal placeholder z archon.au — zgodnie z ustaloną zasadą nadrzędną wyżej, przed wdrożeniem musi zostać zastąpiona prawdziwymi opiniami klientów instytutem.pl; ta runda dotyczyła wyłącznie warstwy wizualnej/strukturalnej, o to poprosił użytkownik. |
| 2026-08-27 | Siódma runda — pełne odwzorowanie rzędu zabiegów (kadr „łuk”), łącznie ze zdjęciami | Odczyt źródłowych reguł CSS (`document.styleSheets`) + pomiar DOM na żywej archon.au przez Browser MCP; realny hover (`matches(':hover')`) do weryfikacji | Na prośbę użytkownika („odwzoruj ją 1:1 jak na archon.au — łącznie ze zdjęciami”) zidentyfikowano, że sekcja to biblioteka **Flickity** (`.tricks-slider.flickity-enabled`), nie zwykły `overflow-x:auto` — odczytano bezpośrednio z arkusza stylów realne reguły `.tricks-slider_slide` (`width:22%; height:50vh; border-radius:150px 150px 20px 20px`, z breakpointami 38%/60vh ≤991px i 60%/55vh ≤479px) oraz hover (`border-radius` się odwraca na `20px 20px 150px 150px` + `translateY(-30px)`, oba `transition:.5s`). Znaleziono i naprawiono błędne założenie z Rundy 2: karty miały stały ciemny gradient (`::before`) na zdjęciu — **w oryginale go nie ma**, kontrast tekstu (biały, Tenor Sans, capitalize) zapewnia samo zdjęcie, a przyciemnienie (`rgba(33,34,31,.31)`) pojawia się **tylko na hover** przez osobny `<a class="slider-linkblock">` nałożony na zdjęcie. Pobrano 11 prawdziwych zdjęć z CDN Webflow archon.au do `assets/images/treatments/` (za zgodą użytkownika, ta sama konwencja co pobranie logo w Rundzie 2) w tej samej kolejności co istniejące polskie podpisy — wszystkie 11 par podpis↔zdjęcie zweryfikowane 1:1 z oryginałem. Przebudowano HTML karty na `<img class="trick-img">` (position:absolute, object-fit:cover) + `<a class="trick-link">` jako overlay, oraz kontener na `.trick-wrap` (max-width:100em/5% padding, bez wcześniejszego capu 72px — ta sekcja na oryginale nie ma capu) opakowujący `.trick-row` + nowy pasek postępu przewijania (`.progress`/`.progress-fill`, JS nasłuchujący `scroll`) — element z oryginału, którego brakowało. **Świadomie pominięto**: JS-owy parallax zdjęcia (oryginał ma `<img>` o `width:200%` przesuwane transformem przy scrollu) — uznane za nieproporcjonalne ryzyko/koszt względem efektu, zdjęcie jest statyczne. Zweryfikowano pomiarem DOM (rozmiary kart na 375px/1920px zgadzają się z regułami źródłowymi co do piksela) oraz realnym hover na lokalnym serwerze (`.claude/launch.json` przepięty na port 8090 — 8080 zajęty przez serwer innej sesji czatu). Napotkano powtarzalny **pusty zrzut ekranu przy scrollu** w Browser MCP (potwierdzone przez DOM, że treść faktycznie się renderuje poprawnie — `opacity:1`, `visibility:visible`, realne `getBoundingClientRect`) — to samo, znane ograniczenie narzędzia opisane w Rundzie 2 (dotyczy też samej archon.au), obejście: zrzut przy `scrollY:0` z tymczasowo powiększonym oknem (`resize_window`) zamiast scrollowania. **Nadal placeholder treściowy**: 11 zdjęć to zdjęcia stockowe klientów Archon (Australia) — do podmiany na prawdziwe fotografie zabiegów instytutem.pl przed publikacją, zgodnie z zasadą nadrzędną w CLAUDE.md; ta runda dotyczyła wyłącznie warstwy wizualnej. |
| 2026-08-27 | Ósma runda — pierwsza prawdziwa treść instytutem.pl: podmiana logo | Plik `logo.svg` dostarczony przez użytkownika z lokalnej ścieżki (`/Volumes/goodram/WORK/INSTYTUT EM/salon/logo/logo.svg`) | Na prośbę użytkownika podmieniono logo Archon na prawdziwe logo instytutem.pl we wszystkich 3 miejscach występowania (nav, drawer mobilny, stopka — wszystkie dzielą klasę `.logo-mark`). Plik skopiowany do `assets/images/instytutem-logo.svg` (2,5 KB), stary `archon-logo.svg` usunięty jako nieużywany. **Nowe logo ma inne proporcje niż wordmark Archon**: viewBox 1035.7×470.5 (~2,2:1, układ pionowy — mały wordmark „INSTYTUT" wersalikami nad dużym odręcznym podpisem „em"), podczas gdy Archon było płaskim wordmarkiem 149.6×23.59 (~6,3:1). Poprzednia stała wysokość `.logo-mark{height:20px}` była dobrana pod tamten kształt — przy nowym logo zmniejszała odręczny podpis „em" (główny element rozpoznawalności) do nieczytelnego rozmiaru. Zwiększono do **44px** (dobrane wizualnie tak, żeby podpis „em" czytał się wyraźnie, a mały wordmark nad nim pozostał widoczny, mieszcząc się wygodnie w stałej wysokości nav 80px) — zweryfikowane na żywo na 1440px (desktop), w drawerze mobilnym (375px, otwartym realnym kliknięciem hamburgera) i pomiarem DOM w stopce (wszystkie 3 wystąpienia renderują się w proporcji 2,201, zgodnej z natywną). **Uwaga uboczna, nie naprawiona w tej rundzie**: przy szerokości okna w wąskim przedziale ok. 900–1000px (powyżej progu chowania nav-linków, ale zbyt wąsko na pełną zawartość) flexbox nava ściska logo (`flex-shrink:1` domyślne na `.logo`, brak `flex-shrink:0`) łamiąc jego proporcje — zaobserwowane przypadkiem podczas tej rundy, niezwiązane z podmianą logo (dotyczyłoby też starego logo Archon w tym samym zakresie szerokości), zgłoszone jako osobne zadanie w PLAN.md zamiast naprawiane przy okazji. |
| 2026-08-27 | Naprawiono ściskanie logo w wąskim zakresie szerokości (zgłoszone w Rundzie 8) | Pomiar DOM przy 990px szerokości okna | Dodano `flex-shrink:0` do `.logo` w `css/styles.css`. Zweryfikowane: przy 990px logo renderuje się teraz w proporcji 2,201 (bez zniekształcenia); jako efekt uboczny nav-links (domyślny `flex-shrink:1`) może teraz zawinąć etykietę „O nas" do dwóch linii w tym samym wąskim zakresie — zaakceptowane jako mniejsze zło niż zniekształcona marka. |
| 2026-08-27 | Dziesiąta runda — pozycja mega-menu względem headera + hover 1:1 z archon.au | Bezpośredni odczyt `getComputedStyle`/`document.styleSheets` (real CSS rules, nie tylko wygląd) na żywej archon.au przez Browser MCP | Użytkownik zgłosił, że mega-panel nachodzi na header, oraz poprosił o odwzorowanie 1:1 z archon.au odstępów/czcionek/kolorów, w tym stanu hover. **Przyczyna nachodzenia**: `.mega-panel{top:100%}` liczy się względem `<li>` (position:relative), a `<li>` było wysokie tylko na wysokość przycisku (~32px) wyśrodkowanego flexem w 80px rzędzie nav — `top:100%` lądowało więc w połowie headera, nie na jego dole. Realna archon.au ma `.dropdown-menu-toggle{height:80px}` (dokładnie wysokość headera), więc `top:100%`/`top:80px` naturalnie trafia na sam dół — odwzorowano to samo: `.nav-links > li > .nav-link{height:80px}` (przycisk wypełnia cały rząd nav, wyśrodkowanie tekstu nadal przez `align-items:center` flexboksa w środku). **Hover wewnątrz panelu**: obecny kod miał `text-decoration:underline` na hover linku — realna archon.au (`.mega-link:hover{color:var(--rust)}`, rust=`#c7642d`) w ogóle nie używa podkreślenia, tylko zmianę koloru tekstu na rdzawy pomarańcz, z `transition:0.2s`. Dodano token `--a-rust:#C7642D` i podmieniono zachowanie. **„Podkreślenie" pod otwartą pozycją menu głównego** (to, co użytkownik zauważył wizualnie) to w oryginale `box-shadow:inset 0 -4px 0 -2px var(--dark-slate-grey)` (dark-slate-grey=`#3a412a`=nasze `--a-olive`) na stanie `.w--open` (odpowiednik `aria-expanded="true"`) — cienka 2px linia u dołu przycisku, niezależna od koloru tekstu. Hover samego tekstu przycisku (bez otwierania) zmienia kolor na `--brown-rule` (`#7a7772` = nasze `--a-gray`), nie na olive jak wcześniej mieliśmy. Rozdzielono to poprawnie: `.nav-link:hover{color:var(--a-gray)}` osobno od `.nav-link[aria-expanded="true"]{box-shadow:inset 0 -4px 0 -2px var(--a-olive)}`. **Odstępy panelu**: realny `.dropdown-wrapper{padding:20px 48px 25px}` (u nas było `20px 24px 25px` — poziomy padding za mały, poprawiony na 48px) oraz `.column-heading{padding-bottom:10px}` (u nas 8px, poprawione). **Dodatkowy, realny błąd znaleziony przy okazji weryfikacji** (nie zgłoszony przez użytkownika, wykryty pomiarem DOM podczas testowania poprawki pozycji): panel „Zabiegi" (3 kolumny, 820px po zmianie paddingu) nadal wychodził **~120px poza lewą krawędź** viewportu przy 1280px mimo zakotwiczenia `right:0` z Rundy 9 — to zakotwiczenie działało tylko przy szerszych oknach/innym układzie przycisku, nie jest ogólnie poprawne dla panelu szerszego niż dostępne miejsce po obu stronach przycisku. Naprawione **dynamicznym pozycjonowaniem w JS** (`openMegaMenu` w `js/main.js`): po otwarciu panelu mierzy jego szerokość i pozycję przycisku, po czym ustawia `left` tak, żeby panel zawsze mieścił się w viewport (z 20px marginesem), niezależnie od tego, który przycisk go otworzył i jak szeroki jest ekran — usunięto sztywny CSS-owy override `#zabiegiMegaPanel, #onasMegaPanel{left:auto;right:0}`, bo dynamiczne rozwiązanie obsługuje wszystkie panele jednym mechanizmem. Zweryfikowane wizualnie na 1280px: wszystkie 4 panele (Konsultacje/Problem/Zabiegi/O nas) mieszczą się w całości, zaczynają się dokładnie na dole headera (bez nachodzenia), hover linków pokazuje kolor rdzawy (nie podkreślenie), otwarta pozycja menu ma cienką oliwkową linię u dołu — bez regresji w widoku mobilnym (zmiana `height:80px` zaskopowana tylko do `.nav-links > li > .nav-link`, nie dotyka `.mobile-drawer .nav-link`). **Poprawka doraźna tej samej rundy**: użytkownik zgłosił, że „Cennik"/„Kontakt" (zwykłe `<a>`, bez dropdownu) siedzą wyżej niż pozostałe pozycje (`<button>`). Pomiar tekstu przez `Range.getBoundingClientRect()` (dokładniejszy niż `getBoundingClientRect()` na całym `<span>`, bo pomija boks line-height) pokazał różnicę tylko 0.4px w Chrome — niewidoczną gołym okiem — ale przyczyna była realna i przenośnie ryzykowna: `<button>` używa domyślnego `line-height:normal` przeglądarki, a `<a>` dziedziczy `line-height:1.7` z `body` (różne, niezdefiniowane międzyprzeglądarkowo zachowanie, które w Firefox/Safari mogłoby dać już zauważalną różnicę). Naprawione przez jawne `line-height:1` na `.nav-link`, jednakowe dla obu typów elementów — po poprawce tekst wszystkich 6 pozycji renderuje się na identycznych pikselach (31–48px w 80px rzędzie). **Druga poprawka doraźna**: użytkownik zgłosił, że górny/dolny padding panelu jest mniejszy niż na oryginale. Ponowny pomiar CSSOM wykazał, że wcześniej skopiowano tylko padding samego `.dropdown-wrapper` (`20px 48px 25px`), pomijając, że realny `.column-heading` ma **własne** `padding-top:20px` — dający w sumie 40px „powietrza" od góry panelu do tekstu pierwszej kategorii (20 wrapper + 20 heading), nie 20px jak mieliśmy. Podobnie `.mega-menu-link-2` ma własne `padding:4px 0` (nie sam `gap` na liście) — dający dodatkowe 4px pod ostatnią pozycją przed paddingiem wrappera (razem 29px na dole, nie 25px). Naprawione: dodano `padding-top:20px` do `.mega-group .cat`, zamieniono `.mega-group ul{gap:.5rem}` na realny per-link `.mega-group a{padding:4px 0}` (ta sama efektywna 8px przerwa między pozycjami, plus dodatkowe 4px „powietrza" pod ostatnią). Zweryfikowane pomiarem: odstęp od góry panelu do tekstu nagłówka kategorii = 42px (było 20px), od tekstu ostatniej pozycji do dołu panelu = 29px (było 25px) — zgodne z realnymi wartościami z archon.au. **Trzecia poprawka doraźna**: użytkownik zgłosił, że u dołu nadal brakuje „powietrza". Ponowny pomiar na żywej archon.au (metodą box-to-box, spójną z pomiarem górnym) potwierdził, że nasz dolny odstęp **już wtedy zgadzał się 1:1** z oryginałem (25px) — ale sama archon.au ma tu wyraźną asymetrię (górny odstęp do tekstu ~42px vs dolny ~29px), która w wiernym odwzorowaniu wygląda nierówno. Zamiast trzymać się sztywno 1:1 w tym jednym miejscu, na wyraźną prośbę użytkownika zwiększono `padding-bottom` panelu z 25px na **36px** (dając razem z własnym paddingiem linku efektywnie ~36–40px na dole, bliżej symetrii z górą) — świadome, zgłoszone odejście od czystej wierności archon.au w stronę lepszego balansu wizualnego. Zweryfikowane pomiarem: `bottomGapBox` = 36px (było 25px). |
| 2026-08-27 | Jedenasta runda — stopka tożsama z menu głównym + realne dane kontaktowe instytutem.pl | Bezpośredni crawl żywej instytutem.pl (Browser MCP, `footer.innerText` + `href` linków) | Użytkownik poprosił o dostosowanie stopki do menu w nagłówku oraz realne dane kontaktowe z instytutem.pl — to dokładnie zadanie zgłoszone jako TODO w Rundzie 9 („Stopka niespójna z nowym menu głównym"). **Menu w stopce**: zastąpiono 3 stare kolumny (Zabiegi na Skórę/Zabiegi Laserowe/Przydatne Linki, linkujące do nieistniejącej struktury `skin.html`/`laser.html`/`about.html`) czterema kolumnami **tożsamymi z nagłówkiem** — te same linki i etykiety co Konsultacje/Problem/Zabiegi/O nas w menu głównym (łącznie z podziałem na podkategorie), plus Cennik/Kontakt jako zwykłe pozycje na końcu kolumny „O nas". Podkategorie oznaczone tym samym `<li class="cat-label">` co w drawerze mobilnym (nowy, analogiczny styl `.footer-col .cat-label`, bez własnego linku — to etykieta, nie pozycja klikalna). `.footer-grid` rozszerzony z 4 do 5 kolumn (`1.2fr repeat(4,1fr)`). **Dane kontaktowe**: zamiast zgadywać, przeszukano bezpośrednio żywą instytutem.pl (nie tylko podsumowanie z ANALIZA-INSTYTUTEM.md) i pobrano realne `href`: telefon `tel:+48737836092`, e-mail `mailto:recepcja@instytutem.pl`, adres z linkiem do Google Maps `https://goo.gl/maps/upRkkPpan7BiD86L9` (ul. 1 Maja 6, 09-402 Płock), godziny otwarcia (pon–pt 08:00–21:00, sob 08:00–16:00, niedziela nieczynne), oraz prawdziwe profile social media (`facebook.com/instytutem`, `instagram.com/instytutem` — poprzednio było `archonaustralia`/`archon.australia`). Stopka copyright zmieniona z „Archon" na „INSTYTUTem.™", link do Terms usunięty (nie znaleziono odpowiednika na żywej stronie, tylko Polityka Prywatności — `instytutem.pl/polityka-prywatności`). **Świadomie NIE ruszone w tej rundzie**: osobna sekcja „Kontakt"/„Skontaktuj się z Nami" w środku strony (`#contact`, ok. linii 493–517) nadal ma dane z Australii (Teneriffe, telefon 3852 5660, e-mail archonspas.com.au) — użytkownik poprosił konkretnie o stopkę, ta sekcja to osobny, już wcześniej zgłoszony punkt w PLAN.md („Cała pozostała treść index.html — hero, oferta, adres, kontakt — podmienić z archon.au na treść instytutem.pl”). Zweryfikowane pomiarem DOM (5 kolumn, ~200px szerokości przy 1280px i ~156px przy 1024px, bez przewijania poziomego) oraz odczytem `innerText`/`href` całej stopki. **Poprawka doraźna tej samej rundy (2 zgłoszenia)**: (1) użytkownik zgłosił, że przy węższym oknie ostatnia kolumna „O nas" przeskakuje samotnie do nowego wiersza — przyczyna: breakpoint `@media(max-width:800px)` redukował siatkę do `1fr 1fr` (2 tory), a przy 5 elementach auto-placement CSS Grid nieuchronnie zostawia piąty (nieparzysty) sam w nowym wierszu. Naprawione przez nowy breakpoint `@media(max-width:1024px)`: kolumna logo/kontakt dostaje `grid-column:1/-1` (własny, pełnej szerokości wiersz), a pozostałe 4 kolumny menu (Konsultacje/Problem/Zabiegi/O nas) trafiają w 4-torową siatkę `repeat(4,1fr)` — zawsze razem w jednym wierszu, tak jak w nagłówku. Dopiero poniżej 600px (gdzie 4 wąskie kolumny z długimi listami przestają być czytelne) redukcja do `1fr 1fr`, a poniżej 480px do pojedynczej kolumny. Zweryfikowane pomiarem `getBoundingClientRect().top` na 990px (4 kolumny menu: identyczny `top`, logo osobno wyżej), 1280px (wszystkie 5 w jednym wierszu) i 600px (2+2+1, bez samotnej piątej). (2) Użytkownik zgłosił, że styl etykiet podkategorii (`cat-label`) „nie pasuje do całości" — były to gołe, pogrubione, szare wersaliki bez żadnego wizualnego oddzielenia od nagłówka kolumny (`h4`, bardzo podobny styl) ani od linków pod spodem. Ujednolicono z już istniejącym, zaakceptowanym wzorem z mega-menu w nagłówku (`.mega-group .cat`): dodano `border-bottom` (ta sama szara linia 1px) i `padding-bottom:8px`, zwiększono `margin-top` do `1.25rem` dla wyraźniejszego odstępu między podkategoriami. Zweryfikowane wizualnie zrzutem ekranu — etykiety podkategorii mają teraz spójną z resztą strony linię-separator. |
| 2026-08-27 | Dwunasta runda — dodano brakujący zabieg „Laser Tulowy Erbowo-Szklany" | Bezpośredni odczyt żywej podstrony `instytutem.pl/laser-tulowy-plock` (Browser MCP) | Użytkownik zauważył, że tego zabiegu brakowało zarówno w treści, jak i w menu — słusznie: jest on obecny na liście URL do zachowania z sekcji SEO w tym dokumencie (`/laser-tulowy-plock`, oznaczony jako „bez prefiksu /zabiegi/, zachować mimo to") oraz w pierwotnej analizie w [ANALIZA-INSTYTUTEM.md](ANALIZA-INSTYTUTEM.md), ale nie trafił do listy 22 zabiegów podanej ręcznie przez użytkownika w Rundzie 9, na podstawie której zbudowano menu „Zabiegi" — najwyraźniej pominięty przy przepisywaniu tamtej listy. Sprawdzono żywą podstronę, żeby potwierdzić dokładną nazwę i URL (nie zgadywać): „Laser tulowy erbowo-szklany", technologia frakcyjna 1927 nm, `/laser-tulowy-plock` (przekierowuje z `www.instytutem.pl` na `instytutem.pl`, potwierdzone jako ten sam, realny, zaindeksowany adres z listy SEO). Dodano pozycję „Laser Tulowy Erbowo-Szklany" → `/laser-tulowy-plock` w kategorii „Zabiegi Hi-Tech", zaraz po „Epilacja laserowa LightSheer®" (oba to zabiegi laserowe) — we wszystkich 3 miejscach, gdzie ta lista występuje: mega-menu desktopowe, rozwijana lista w drawerze mobilnym, kolumna „Zabiegi" w stopce (dodane w poprzedniej rundzie jako kopia menu głównego). Zweryfikowane odczytem DOM: link obecny we wszystkich 3 kontekstach z tym samym `href` i tekstem. Po zgłoszeniu użytkownikowi dwóch dodatkowych zauważonych braków („Fototerapia LED Dermalux", „Trychologia") użytkownik potwierdził dodanie obu. Przy weryfikacji (porównanie pełnej listy URL z sekcji SEO wyżej w tym dokumencie z obecną zawartością menu „Zabiegi") wykryto też **trzeci**, wcześniej niezauważony brak: `/zabiegi/endermologia-twarzy-plock-lpg-endermolift` — realny, zaindeksowany URL z listy SEO, którego też nie było w liście 22 zabiegów z Rundy 9. Zgłoszony i dodany razem z pozostałymi dwoma (ten sam typ luki, ta sama runda naprawy). Dla każdej z 3 pozycji sprawdzono żywą podstronę zamiast zgadywać nazwę: (1) **Fototerapia LED Dermalux®** (`/fototerapia-dermalux-plock`) — potwierdzona pełna nazwa z tytułu strony, dodana do „Zabiegi Hi-Tech" (technologia świetlna, ta sama kategoria co inne urządzenia); (2) **Trychologia** (`/zabiegi/trychologia`) — dodana do „Zabiegi Manualne" (najbliższa dopasowaniem kategoria, nie technologiczna ani iniekcyjna); **uwaga**: sama podstrona na żywej instytutem.pl zwraca błąd 404 (uszkodzony link na oryginale, potwierdzone też przez link w menu żywej strony wskazujący na ten sam, martwy adres) — mimo to dodano tę samą ścieżkę, zgodnie z zasadą wiernego odwzorowania struktury strony, nie naprawiania cudzych błędów przy okazji; (3) **Endermologia Twarzy LPG Endermolift™** (`/zabiegi/endermologia-twarzy-plock-lpg-endermolift`) — potwierdzona z nagłówka strony („Zabieg Endermologii Twarzy LPG - Endermolift™"), dodana do „Zabiegi Hi-Tech" zaraz po „Endermologia LPG® Alliance" (to samo urządzenie LPG, wariant na twarz). Wszystkie 3 dodane w tych samych 3 miejscach co poprzednio (mega-menu desktop, drawer mobilny, stopka) — zweryfikowane odczytem DOM: każdy z 3 nowych `href` występuje dokładnie 3 razy z tym samym tekstem. |
| 2026-08-27 | Trzynasta runda — korekty treści od użytkownika (Trychologia, duplikat Endermologii Twarzy) + odstęp linii w zawijających się pozycjach | Bezpośrednia korekta użytkownika w czacie | Użytkownik ustalił **nową stałą zasadę**: pytać przed każdym samodzielnym dodaniem czegokolwiek do strony — zapisane w pamięci Claude (`feedback_ask_before_adding_content.md`) na przyszłe sesje, bo w poprzedniej rundzie dodałem „Endermologię Twarzy LPG Endermolift™” z własnej inicjatywy (tylko zgłaszając to po fakcie), co okazało się błędem. **Korekty treści**: (1) „Endermologia Twarzy LPG Endermolift™” usunięta całkowicie (3 miejsca) — użytkownik potwierdził, że to duplikat „LPG® Ergolift” już obecnego w tej samej kolumnie „Zabiegi Hi-Tech” (to była realna, zaindeksowana strona instytutem.pl, ale najwyraźniej dwa różne URL-e prowadzą do tego samego zabiegu w ofercie salonu — informacja, którą mógł potwierdzić tylko właściciel treści, nie pomiar DOM); (2) „Trychologia” przeniesiona z kategorii „Zabiegi Manualne” do **Konsultacje → Dla Twarzy** jako nowa pozycja „Konsultacja trychologiczna” (nowy adres `/konsultacje/konsultacja-trychologiczna`, zgodny z konwencją nazewnictwa rodzeństwa w tej samej kategorii) — usunięta ze starego miejsca i dodana w nowym, w każdym z 3 miejsc (desktop/mobile/stopka). **Odstęp linii w zawijających się pozycjach**: użytkownik zgłosił, że w mega-menu (i analogicznie w stopce) pozycje zawijające się do 2 linii (np. „Fibryna i osocze bogatopłytkowe”, „Radiofrekwencja mikroigłowa RF”) wyglądały jak dwie osobne pozycje listy, bo odziedziczony `line-height:1.7` z `body` dawał między zawiniętymi liniami niemal tyle samo białej przestrzeni co odstęp `padding` między faktycznie różnymi pozycjami. Naprawione jawnym `line-height:1.3` na `.mega-group a` i `.footer-col a` (część, gdzie dotąd nie było ustawionej wartości) — linie zawiniętego tekstu są teraz wyraźnie ciaśniejsze niż odstęp między pozycjami, więc optycznie czytają się jako jeden element. Zweryfikowane wizualnie zrzutem ekranu przy węższym oknie (750px, gdzie kolumny są węższe i więcej pozycji się zawija) — zarówno w mega-menu, jak i w stopce. |
| 2026-08-27 | Czternasta runda — jednolity hover `--a-rust` na wszystkich linkach tekstowych + wpis w design systemie | Prośba użytkownika w czacie | Użytkownik poprosił o rozszerzenie koloru hover z mega-menu (`--a-rust`, `#C7642D`) na wszystkie klikalne linki tekstowe w całym serwisie, oraz o udokumentowanie tego w DESIGN-SYSTEM.md. Zmieniono: `.nav-link:hover` (było `--a-gray`, teraz `--a-rust`, dodano `transition:color .2s`), `.footer-col a:hover` (było samo podkreślenie tekstu, teraz zmiana koloru na rust bez podkreślenia — spójnie z mega-menu), `.footer-bottom a` (wcześniej brak jakiegokolwiek stanu hover — dodany), `.tcard .booklink` (dodany hover, wcześniej tylko stałe podkreślenie bez zmiany koloru). **Świadomie pozostawione bez zmian** (osobny język interakcji, nie „link tekstowy”): `.abtn` (przyciski — hover to uniesienie+cień, nie kolor), ikony social media w stopce (`--a-olive`), strzałki karuzeli opinii (`--a-taupe`), karty zabiegów „kadr łuk” (odwrócenie zaokrąglenia + przyciemnienie zdjęcia). Dodano do [DESIGN-SYSTEM.md](DESIGN-SYSTEM.md): nowy token `--a-rust` w tabeli kolorów (sekcja 2) z opisem zastosowania, zasadę użycia (rust tylko jako stan hover, nigdy jako domyślny kolor), oraz nową podsekcję „Linki i stany hover” w sekcji 8 (Komponenty) opisującą regułę i wymienione wyżej wyjątki. Zweryfikowane: (1) odczytem `document.styleSheets` — wszystkie nowe/zmienione reguły obecne i poprawnie sformułowane; (2) realnym hover (Browser MCP) na przycisku „Cennik” w nagłówku — kolor zmienia się na `rgb(199, 100, 45)` (dokładnie `#C7642D`), potwierdzając, że mechanizm działa. Symulacja hover na linkach w mocno przewiniętej stopce nie została potwierdzona zrzutem/pomiarem „na żywo” w tej rundzie — narzędzie do automatyzacji przeglądarki nie utrzymywało stanu `:hover` po scrollu (znane, udokumentowane wcześniej ograniczenie tego narzędzia, patrz Runda 7); reguła CSS dla stopki używa identycznego, już zweryfikowanego wzorca (`selector:hover{color:var(--a-rust)}` + `transition`), więc uznano za wystarczająco potwierdzone przez inspekcję CSSOM. |
| 2026-08-27 | Piętnasta runda — animowana strzałka przy pozycjach mega-menu w nagłówku | Realna interakcja (klik + hover) na żywej archon.au przez Browser MCP, w tym pobranie SVG ikony z CDN | Użytkownik poprosił o odtworzenie małej animowanej strzałki, która na archon.au pojawia się przy każdej pozycji w mega-menu. Zbadano mechanikę na żywo: strzałka to `<img>` (SVG pobrane z CDN Webflow) wewnątrz `<div class="footer-arrow">` (domyślnie `opacity:0`), a jej pojawienie się i przesunięcie (`transform:translate3d(8px,0,0)`) na hover jest sterowane inline stylem wstrzykiwanym przez silnik interakcji Webflow (IX2) — nie ma tego w statycznych regułach CSS (`transition-duration:0s` w spoczynku), więc nie da się tego skopiować 1:1 z arkusza stylów, tylko odtworzyć animację ręcznie. Pobrano treść SVG bezpośrednio z CDN (`rightarrow.svg` — prosta strzałka w prawo, `viewBox 0 0 16 12`) i odtworzono jako `::after` (maska CSS z `background-color:currentColor`, nie osobny `<img>`/inline SVG) na `.mega-group a` — **bez zmian w HTML**, żeby nie edytować ręcznie ok. 50 pozycji linków w 4 panelach. Domyślnie `opacity:0`, na `:hover` linku: `opacity:1` + `translateX(4px)` (przeskalowane z oryginalnych 8px do mniejszej szerokości naszych kolumn 220px vs 300px na archon.au), `transition:.2s ease`. Ponieważ strzałka używa `currentColor`, automatycznie przyjmuje ten sam rdzawy kolor co tekst linku na hover (Runda 14) — spójny, jeden mechanizm zamiast dwóch niezależnych efektów. **Świadoma zmiana układu**: żeby strzałka nie kolidowała z zawijającym się tekstem (nasze kolumny są węższe i mają dłuższe polskie frazy niż krótkie angielskie teksty na archon.au, które nigdy się nie zawijają), `.mega-group a` zmienione z `display:block` na `display:flex;justify-content:space-between;align-items:center` — strzałka zadokowana przy prawej krawędzi kolumny i wyśrodkowana pionowo względem całego (nawet 2-wierszowego) tekstu, zamiast sztywno zaraz za tekstem jak na oryginale (tam nigdy nie było takiej potrzeby, bo tekst się nie zawija). **Zakres świadomie ograniczony do mega-menu w nagłówku** (`.mega-group a`, tylko `.mega-panel`) — nie dotyczy drawera mobilnego ani stopki, bo użytkownik poprosił konkretnie o „mega-menu w headerze”; te dwa miejsca używają osobnych klas (`.mobile-drawer` linki, `.footer-col a`) i nie zostały ruszone. Zweryfikowane wizualnie (zrzuty ekranu po hover) na jednowierszowej pozycji („Dermokonsultacja (twarz)”) i dwuwierszowej, zawijającej się pozycji („Fibryna i osocze bogatopłytkowe”) — w obu przypadkach strzałka pojawia się poprawnie wyśrodkowana, bez kolizji z tekstem. **Poprawka doraźna tej samej rundy**: użytkownik zauważył, że na archon.au cała klikalna pozycja (nie tylko strzałka) przesuwa się w prawo na hover. Zmierzono precyzyjnie: tekst (`p.mega-link`) faktycznie ma własny `transform:translate3d(3px,0,0)` na hover, niezależny od przesunięcia strzałki (8px) — całość porusza się razem, strzałka odjeżdża dalej niż tekst. Dodano `transform:translateX(3px)` do `.mega-group a:hover` (na całym linku, nie tylko strzałce) z `transition:transform .2s ease` — ponieważ strzałka (`::after`) ma swój WŁASNY, dodatkowy `translateX(4px)` liczony względem już przesuniętego rodzica, efektywne przesunięcie strzałki wynosi 3+4=7px, a tekstu samo 3px — ten sam relacyjny efekt „strzałka ucieka dalej niż tekst” co na oryginale (tam: tekst 3px, strzałka 8px). |
| 2026-08-27 | Szesnasta runda — dok przycisku „Rezerwuj online” przy hamburgerze w widoku mobilnym | Prośba użytkownika w czacie | W widoku mobilnym `.nav-row{justify-content:space-between}` rozstawiał 3 elementy (logo, `.nav-right` z widocznym przyciskiem „Rezerwuj online”, hamburger) równomiernie po całej szerokości, więc przycisk wisiał osobno na środku zamiast przy hamburgerze. Naprawione w istniejącym bloku `@media(max-width:900px)`: `.nav-row{justify-content:flex-start;gap:12px}` + `.logo{margin-right:auto}` — logo zostaje dosunięte do lewej (margines automatyczny pochłania resztę miejsca), a przycisk i hamburger trzymają się razem po prawej z 12px odstępem (ta sama wartość co istniejący `gap` w `.nav-right`, dla spójności). Padding samego hamburgera (`.4rem`, symetryczny na wszystkich 4 bokach) pozostał nienaruszony — użytkownik wyraźnie o to prosił. Zweryfikowane pomiarem DOM na 375px: odstęp przycisk→hamburger = 12px, padding hamburgera = 6.4px równo z każdej strony, odległość hamburgera od prawej krawędzi = 20px (ten sam `clamp()` z `.container`, symetryczny z odległością logo od lewej krawędzi). |
| 2026-08-27 | Siedemnasta runda — mobilne menu 1:1 z archon.au | Realna interakcja (klik + JS state) i pomiar DOM na żywej archon.au w widoku mobilnym (375px), przez Browser MCP | Użytkownik zgłosił, że mobilne menu (drawer po kliknięciu hamburgera) nie wygląda tak jak na archon.au. Otwarto realne mobilne menu na archon.au (hamburger tam to `<div role="button" class="w-nav-button">`, nie `<button>` — synthetic `.click()` nie działał, wymagało prawdziwego CDP-klika) i zmierzono dokładnie: (1) **pozycje najwyższego poziomu to płaska lista** (Skin/Laser/Men/Women/About ze strzałką-chevronem po prawej, Contact bez strzałki) — u nas te same pozycje nie miały żadnej ikony wskazującej, że się rozwijają; (2) każdy wiersz ma `padding:30px 0` (nie odstęp `gap` między wierszami) i cienką linię-separator `border-bottom:1px solid rgba(202,199,192,.16)` — u nas było `gap:1.25rem` bez żadnych linii; (3) tekst `Inter 500 16px` (`text-transform:capitalize`) — u nas `1.1rem` bez transformacji; (4) pod listą są **3 pełnoszerokie przyciski** w kolejności Call Us (outline szary) → Book Online (pełny oliwkowy) → Gift Vouchers (outline ciemny) — u nas były tylko 2 przyciski, brakowało odpowiednika „Zadzwoń”. Wszystkie 3 style przycisków already istniały w naszym systemie jako gotowe warianty `.abtn` (`outline-gray`/`primary-olive`/`outline-dark`, ten pierwszy nieużywany nigdzie wcześniej w kodzie) — dodano brakujący przycisk „Zadzwoń” (`tel:+48737836092`, ten sam numer co w stopce) na początku listy CTA, zachowując tę samą kolejność co na oryginale. Dodano tę samą ikonę strzałki (`.caret`, reużyty z desktopowego mega-menu) do każdego `<summary>` w drawerze — poprzednio jej tam w ogóle nie było. Przebudowano CSS: `.mobile-drawer nav > ul > li{border-bottom:...}` zamiast `gap`, `.mobile-drawer .nav-link{padding:30px 0;font-size:16px;text-transform:capitalize;justify-content:space-between}`, ukryto natywny znacznik `<summary>` (`::-webkit-details-marker{display:none}`), padding boczny drawera zmieniony z `var(--sp-24)` na ten sam `clamp(20px,5%,72px)` co `.container` (dokładne dopasowanie zmierzonej wartości 18,64px przy 375px). **Świadomie zachowane bez zmian** (decyzja projektowa, nie luka w wierności): nasz drawer to pełnoekranowy panel wjeżdżający z prawej, z własnym nagłówkiem (logo + X) — archon.au zamiast tego zostawia oryginalny pasek nagłówka widoczny i tylko rozwija listę pod nim (hamburger zamienia się w X w tym samym miejscu, bez osobnego nagłówka panelu). Przebudowa architektury wejścia panelu (a nie tylko jego zawartości) uznana za nieproporcjonalne ryzyko regresji względem korzyści — nasz wzorzec pełnoekranowego panelu to powszechny, poprawny wzorzec UX, a użytkownik prosił o dopasowanie wyglądu **menu**, nie mechaniki wejścia. Podobnie nie zmieniono stylu przycisku „Rezerwuj online” w pasku nagłówka (pełny przycisk) na zwykły podkreślony link tekstowy, jakim jest na archon.au — to była świadoma, osobna decyzja użytkownika z wcześniejszej rundy (dokowanie przycisku przy hamburgerze), nie cofnięta tym poleceniem. Zweryfikowane wizualnie: lista z chevronami i liniami-separatorami, 3 przyciski CTA w prawidłowej kolejności, rozwinięcie akordeonu (wymuszone przez `details.open=true` z powodu ograniczeń narzędzia do klikania w tej rundzie) pokazuje poprawne wcięcie, etykiety podkategorii i obracającą się strzałkę. |
| 2026-08-27 | Osiemnasta runda — 3 poprawki menu mobilnego po uwagach użytkownika | Realna interakcja + inspekcja DOM przez Browser MCP | Użytkownik zgłosił 3 konkretne problemy z menu mobilnym z poprzedniej rundy. **(1) Hamburger nie zamieniał się w X / nie zamykał menu**: `hamburgerBtn` miał tylko `openDrawer` podpięty pod `click` — drugie kliknięcie (gdy ikona wizualnie już pokazywała X dzięki istniejącym regułom CSS `[aria-expanded="true"] span:...`) nadal wołało `openDrawer`, więc nic się nie działo. Naprawione: `hamburgerBtn` teraz przełącza (`isOpen ? closeDrawer() : openDrawer()`), zamiast tylko otwierać — dokładnie jak pojedynczy toggle-przycisk ☰↔✕ na archon.au. **(2) Zamykanie nie działało po rozszerzeniu okna**: `drawerCloseBtn` i `hamburgerBtn` dzielą klasę `.hamburger`, która ma `display:none` poza `@media(max-width:900px)` — więc po otwarciu menu na mobile i rozszerzeniu okna do desktopu **oba** przełączniki znikały jednocześnie, zostawiając w pełni otwarty, zablokowany panel (`position:fixed;inset:0`) bez żadnego sposobu na zamknięcie, do tego z `document.body.style.overflow="hidden"` nadal aktywnym (strona niescrollowalna nawet na desktopie). Naprawione dodaniem `window.matchMedia("(min-width: 901px)")` z listenerem `change`, który wywołuje `closeDrawer()` automatycznie, gdy okno przekroczy szerokość desktopową — przywraca `overflow`, `aria-expanded` i chowa panel. Zweryfikowane: otwarcie na 375px → `resize_window` na desktop → `open:false, hidden:true, overflow:""` (było wcześniej `open:true` na stałe, bez wyjścia). **(3) Brak hierarchii rozmiaru czcionki**: pozycje najwyższego poziomu (16px, ustalone w Rundzie 17 jako zgodne z archon.au) i zagnieżdżone linki po rozwinięciu strzałką dziedziczyły ten sam rozmiar (nie było żadnej reguły dla `details ul a`, więc zagnieżdżone linki brały domyślne 16px z `body`) — brak wizualnej różnicy między „Konsultacje” a „Dermokonsultacja (twarz)” pod spodem. Dodano `.mobile-drawer details ul a{font-size:14px}` — teraz główne pozycje wyraźnie większe od kategorii pod spodem, zgodnie z naturalną hierarchią. Zweryfikowane wizualnie po rozwinięciu „Konsultacje”: 16px vs 14px, wyraźna różnica na oko. |
| 2026-08-27 | Dziewiętnasta runda — znaleziona prawdziwa przyczyna „hamburger nie zmienia się w X” + 3 dalsze poprawki | Inspekcja DOM na żywo (`elementFromPoint`, `getBoundingClientRect`, debug `transform:scale(5)` do wizualnego potwierdzenia) przez Browser MCP | Użytkownik zgłosił, że hamburger nadal nie zmienia się w X mimo poprawki z Rundy 18. **Znaleziona prawdziwa przyczyna**: `.mobile-drawer{position:fixed;inset:0;z-index:200}` w pełni przykrywał oryginalny nagłówek (`z-index:100`) po otwarciu — łącznie z prawdziwym `hamburgerBtn`, który POPRAWNIE dostawał animację X (potwierdzone `getComputedStyle` — transformy 45°/-45°, poprawna matematyka). To, co użytkownik faktycznie widział i klikał, to był **inny, osobny przycisk** — `drawerCloseBtn` wewnątrz własnego nagłówka panelu (`.mobile-drawer-head`, duplikat logo + X), który nigdy nie dostawał `aria-expanded="true"` (nasz JS ustawiał ten atrybut tylko na `hamburgerBtn`), więc zawsze wyglądał jak zwykłe 3 kreski, mimo że funkcjonalnie poprawnie zamykał menu. Naprawa tego jednego architektonicznego problemu (usunięcie `.mobile-drawer-head` i przebudowa panelu, żeby nie przykrywał nagłówka) rozwiązała jednocześnie **punkt 1 zgłoszenia** i **osobną prośbę o wjazd panelu z pod headera zamiast z prawej strony z własnym duplikatem logo/przycisków** — to była jedna i ta sama zmiana. `.mobile-drawer` zmieniony z `inset:0` (pełny ekran, wjazd `translateX` z prawej) na `top:80px;left:0;right:0;bottom:0` (zaczyna się dokładnie pod 80px nagłówkiem) z wjazdem `transform:translateY(-100%)→translateY(0)` (realnie „wysuwa się z pod headera”, opadając w dół) i `z-index:90` (niżej niż nagłówek `z-index:100`, żeby nagłówek zawsze zostawał widoczny i klikalny na wierzchu). `hamburgerBtn` to teraz **jedyny** przełącznik — dostał dynamiczny `aria-label` („Otwórz menu” ↔ „Zamknij menu”). Zweryfikowane wizualnie **bezpośrednio** (nie tylko computed style): tymczasowe `transform:scale(5)` na przycisku w trakcie testu potwierdziło czysty kształt X — oraz `elementFromPoint` na pozycji przycisku potwierdził, że to faktycznie `hamburgerBtn` (nie coś innego) jest teraz widoczny i klikalny w tym miejscu. **Punkt 3 (rozmiar głównych pozycji 1:1)**: dokładniejszy pomiar żywej archon.au wykrył dwie brakujące właściwości poza samym `font-size:16px` (już poprawnym) — `line-height:24px` i `letter-spacing:0.5px` (dziedziczyliśmy globalne `line-height:1` z bazowej reguły `.nav-link`, ustawionej wcześniej z myślą o desktopowym headerze) — dodane jawnie do `.mobile-drawer .nav-link`. **Punkt 4 (rdzawy hover)**: dodano `.mobile-drawer details ul a:hover{color:var(--a-rust)}` — zagnieżdżone linki po rozwinięciu kategorii nie miały wcześniej żadnego stanu hover (nie używają klasy `.nav-link`, więc nie dziedziczyły globalnej reguły). Zweryfikowane: zamykanie (kliknięcie ponowne), automatyczne zamykanie przy rozszerzeniu okna do 990px (nadal działa z nową pozycją panelu) i wizualna hierarchia rozmiaru (16px vs 14px, teraz z poprawnym line-height) na rozwiniętej kategorii „Konsultacje”. |
| 2026-08-27 | Dziewiąta runda — całkowita przebudowa menu głównego na nową strukturę instytutem.pl (Konsultacje/Problem/Zabiegi/Cennik/O nas/Kontakt) | Zrzut ekranu docelowego paska nav od użytkownika + pełna treść rozwijanych list podana w czacie po pytaniu doprecyzowującym | **Sprawdzono najpierw żywą instytutem.pl** — jej obecne menu (ZABIEGI/PROMOCJE/BLOG/CENNIK/NA RATY/KONTAKT) różni się od zrzutu użytkownika, więc to nowa, docelowa struktura do zbudowania, nie odwzorowanie istniejącej strony. Zastąpiono całą starą nawigację odziedziczoną 1:1 z archon.au (Skóra/Laser/Mężczyźni/Kobiety/O nas — struktura płci, patrz decyzja z 2026-08-27 wcześniej w tym logu) nową: **Konsultacje** (2 kolumny: Dla Twarzy / Dla Ciała, po 3 pozycje), **Problem** (2 kolumny: Na Twarzy / Na Ciele, po 8 pozycji — kategoryzacja problem-based, spójna z wcześniejszą analizą Fresha w ANALIZA-INSTYTUTEM.md), **Zabiegi** (3 kolumny: Hi-Tech/Iniekcyjne/Manualne, łącznie 22 pozycje), **Cennik** (zwykły link, bez rozwijania), **O nas** (2 kolumny), **Kontakt** (zwykły link). Przyciski CTA zmienione z „Zarezerwuj Online"/„Bony Podarunkowe" na **„Rezerwuj online"/„Kup Voucher"**, a link rezerwacji podmieniony z archońskiego `bookings.gettimely.com/archonspas/book` (AU) na realny `https://rezerwuj.instytutem.pl` (znaleziony wcześniej podczas analizy instytutem.pl). **Zmiana CSS mega-menu**: z pojedynczej wąskiej kolumny 300px (ustalonej wcześniej jako wierne odwzorowanie archon.au dla ich krótkich list) na układ kolumnowy — jedna kolumna 220px na kategorię — bo nowa treść jest dużo obszerniejsza (do 22 pozycji w 3 kategoriach) niż cokolwiek na archon.au; od tego momentu ta sekcja jest architekturą treści instytutem.pl, nie odwzorowaniem wizualnym archon.au. **Realny błąd znaleziony i naprawiony w tej samej rundzie**: panel „Zabiegi" (3 kolumny, 772px) wychodził 106px poza prawą krawędź viewportu przy 1440px (`left:0` względem przycisku w środku nav) — potwierdzone pomiarem DOM (`panelRight:1546` > `innerWidth:1440`), nie tylko przez przycięty zrzut ekranu. Naprawione przez zakotwiczenie szerszych/dalszych paneli (`#zabiegiMegaPanel`, `#onasMegaPanel`) od prawej krawędzi przycisku (`right:0;left:auto`) zamiast od lewej — rozwijają się w stronę środka nav, gdzie jest więcej miejsca. Zweryfikowane pomiarem DOM (panel mieści się w całości: 80–852px z 1440px) i wizualnie na wszystkich 4 rozwijanych menu. **Drawer mobilny**: dotychczasowa konwencja płaskiej listy bez podziału na kategorie (ustalona w Rundzie 5) przestała się sprawdzać przy tej ilości treści (do 22 pozycji w jednym rozwijanym `<details>`) — dodano nieklikalne etykiety kategorii (`<li class="cat-label">`, Inter 10px/600/uppercase, ten sam styl co nagłówki kategorii na desktopie) wewnątrz płaskiej listy, żeby zachować orientację bez budowania zagnieżdżonej struktury. JS (`js/main.js`) uogólniony przez podmianę tablicy nazw menu (`["skin","laser","men","women","about"]` → `["konsultacje","problem","zabiegi","onas"]`) — sama logika otwierania/zamykania bez zmian. **Świadome decyzje o linkach docelowych** (żadna z tych podstron jeszcze nie istnieje — to nowe, puste adresy do zbudowania, ten sam status co istniejące wcześniej `skin.html` itp.): (1) gdzie pozycja z listy użytkownika odpowiadała już **realnemu, zaindeksowanemu URL-owi** z listy SEO wyżej w tym dokumencie (np. „Epilacja laserowa LightSheer®" → `/zabiegi/depilacja-laserowa-plock-lightsheer”, „Plasma™" → `/zabiegi/plasma`, 14 takich dopasowań łącznie) — użyto dokładnie tego realnego adresu, żeby przyszła strona zabiegu trafiła pod URL już dziś rankujący w Google; (2) dla pozycji bez istniejącego realnego adresu (nowe zabiegi typu „Bloomea®", „LPG® Ergolift", cała treść „Konsultacje" i „Problem") utworzono nowe adresy w tej samej konwencji (kebab-case, polskie, bez `.html`, np. `/problem/cellulit`, `/konsultacje/dermokonsultacja-twarz`) — **świadomie NIE użyto** URL-i `majowy86.wixstudio.com/instemnew/blank-X` wklejonych przez użytkownika, bo to własne, robocze linki z narzędzia Wix Studio (autogenerowane szkicowe adresy), a nie docelowe, realne adresy tej strony — użycie ich jako `href` wysyłałoby odwiedzających na obcą, niepowiązaną domenę roboczą. **Nierozwiązane, zgłoszone użytkownikowi wprost w odpowiedzi na czacie**: dostarczona treść kategorii „O INSTYTUTem." (pod „O nas") była identyczna z treścią kategorii „Dla Klientów" (Aktualne promocje/Program lojalnościowy/Vouchery) — wygląda na błąd kopiuj-wklej ze źródła użytkownika. Zamiast zdublować treść, kategoria zawiera tymczasowo jeden bezpieczny link („O nas" → `/o-nas`) z komentarzem TODO w kodzie (i desktop, i mobile) — czeka na prawdziwą treść od użytkownika. **Świadomie poza zakresem tej rundy**: stopka (`index.html`, kolumny linków ok. linii 605–636) nadal odwołuje się do starej struktury `skin.html#tier-1`/`laser.html#...`/`about.html#...` — nie ruszona, bo prośba dotyczyła menu głównego, nie stopki; stopka jest teraz niespójna z nowym nav i wymaga analogicznej przebudowy w kolejnej rundzie. |
| 2026-08-27 | Dwudziesta runda — rząd zabiegów: drag-to-scroll, ukryty scrollbar, asymetryczny margines | Odczyt bieżącego CSS (nietknięty przez rundy 10–19) + ponowna weryfikacja reguł hover na żywej archon.au (`document.styleSheets`) | Użytkownik poprosił o dokładne odwzorowanie 1:1 transformacji kształtu przy hover — **ponowna weryfikacja na żywej archon.au potwierdziła, że kod z Rundy 7 już był dokładnym 1:1** (`border-radius:150px 150px 20px 20px→20px 20px 150px 150px`, `translateY(-30px)`, przyciemnienie `rgba(33,34,31,.31)`, `transition:.5s`, bez dodatkowego skalowania zdjęcia) — bez zmian kodu w tym miejscu, tylko potwierdzenie. Trzy realne zmiany: (1) **ukryto natywny pasek przewijania** (`scrollbar-width:none` + `::-webkit-scrollbar{display:none}` na `.trick-row` — wcześniej było tylko `scrollbar-width:thin`, widoczne na Chrome/Safari); (2) **dodano drag-to-scroll przez myszkę** (`js/main.js`: `mousedown`/`mousemove`/`mouseup` na `.trick-row`, `scrollLeft` liczony z przesunięcia `pageX`; `cursor:grab`→`grabbing`; kliknięcie po realnym przeciągnięciu jest tłumione w capture-phase, żeby nie odpalało linku karty przypadkowo po drag) — zweryfikowane realnym gestem `left_click_drag` (`scrollLeft` faktycznie się zmienił) oraz że zwykły klik bez drag nadal nawiguje (`location.hash` zmienił się na `#treatments`); natywne przewijanie touch/trackpad zostało nietknięte (JS nasłuchuje tylko zdarzeń myszy); (3) **asymetryczny margines** — usunięto `.trick-wrap{max-width:100em;padding:0 5%}` (symetryczny padding na zewnętrznym, nieprzewijalnym wrapperze, ustalony 1:1 z archon.au w Rundzie 7), przeniesiono padding na sam przewijalny `.trick-row` i zrobiono asymetrycznym: `padding:0 0 var(--sp-16) clamp(20px,5%,72px)` — lewy gutter (spójny z resztą kontenerów strony) istnieje tylko w pozycji startowej scrolla, bo to prawdziwy `padding-left` na przewijanym elemencie i przewija się razem z treścią; prawy zawsze `0`, więc karty scrollują do faktycznej krawędzi viewportu bez wracającego marginesu na końcu. To świadome odejście od symetrycznego paddingu 1:1 z archon.au — zmiana zażądana wprost przez użytkownika dla tej sekcji, nie błąd do poprawy. Zweryfikowane pomiarem DOM na 1440px (`paddingLeft:71.25px` ≈ 5% dostępnej szerokości, `paddingRight:0px`, ostatnia karta dotyka prawej krawędzi `.trick-row` po przewinięciu do końca) i na 375px (`paddingLeft` łapie dolny próg `clamp` 20px). Brak poziomego przewijania na poziomie `<body>` (`document.body.scrollWidth` < `window.innerWidth`) — przewijanie zostaje w pełni wewnątrz `.trick-row`. |
| 2026-08-27 | Dwudziesta pierwsza runda — naprawiono zjadany margines początkowy (Runda 20) | Zrzut ekranu od użytkownika pokazujący brak marginesu z lewej + pomiar `scrollLeft`/`getComputedStyle` na żywo | Użytkownik przesłał zrzut ekranu z telefonu pokazujący, że mimo Rundy 20 pierwsza karta i tak zaczynała się bez marginesu, flush do lewej krawędzi. **Znaleziona przyczyna**: `padding-left:20px` był poprawnie ustawiony (potwierdzone `getComputedStyle`), ale `scroll-snap-type:x proximity` na `.trick-row` (+ `scroll-snap-align:start` na `.trick-card`, ustalone jeszcze w Rundzie 7 pod inny model interakcji) **automatycznie doszczepiał scroll do `scrollLeft:20` już przy starcie** — przeglądarka sama „doskrolowywała” tak, żeby start pierwszej karty pokrywał się z realnym początkiem scrollportu, efektywnie znosząc widoczny padding co do piksela. To był realny, zmierzony efekt (`scrollLeft:20` na starcie strony, nie `0`), nie kwestia CSS. **Naprawa**: usunięto `scroll-snap-type` i `scroll-snap-align` całkowicie — przy drag-to-scroll (Runda 20) hard snapping nie jest już potrzebny do niczego innego, więc to czyste uproszczenie (KISS), a nie kompromis. Zweryfikowane: `scrollLeft:0` na starcie, `firstCardLeft` = dokładnie wartości `padding-left` (20px na 375px, 71.25px na 1440px) — margines faktycznie widoczny w domyślnym widoku na obu szerokościach, potwierdzone też wizualnie zrzutem ekranu. |
| 2026-08-27 | Dwudziesta druga runda — naprawiono brak wyrównania z nagłówkiem na szerokich ekranach (>1440px) | Zrzut ekranu od użytkownika (okno ~2000px) + pomiar `getBoundingClientRect` na żywo | Użytkownik przesłał kolejny zrzut ekranu — tym razem przy bardzo szerokim oknie — pokazujący, że pierwsza karta zaczyna się wyraźnie bardziej na lewo niż nagłówek sekcji i logo w nagłówku. **Prawdziwa przyczyna, tym razem realny błąd wprowadzony w Rundzie 20**: `.container` (który mieści nagłówek/logo) ma `max-width:1440px; margin:0 auto` — powyżej 1440px szerokości okna dostaje dodatkowy margines centrujący `(szerokość_okna − 1440) / 2` PONAD swój wewnętrzny `padding-left`. `.trick-wrap`/`.trick-row` straciły ten sam `max-width`+`margin:auto` w Rundzie 20 (świadomie, żeby prawa strona mogła się rozciągać bez ograniczeń) — ale przez to na szerokich ekranach miały tylko sam wewnętrzny `padding-left` (72px, capowany), bez dodatkowego marginesu centrującego, którym dysponuje `.container`. Efekt: przy 2000px `.container` zaczynał treść na ~344px od krawędzi, a `.trick-row` tylko na 72px — różnica dokładnie równa brakującemu marginesowi centrującemu. **Naprawa**: dodano do `.trick-wrap` `margin-left:max(0px, calc((100% - var(--container-max)) / 2))` — dokładnie odwzorowuje to, co `margin:0 auto` daje `.container` po lewej stronie (bo oba elementy dzielą tego samego rodzica, więc `100%` liczy się z tej samej szerokości), ale **tylko z lewej** — `margin-right` zostaje `0`, więc prawa krawędź nadal biegnie bez ograniczeń do krawędzi viewportu (wymóg z Rundy 20 nienaruszony). Zweryfikowane pomiarem DOM na 2000px (nagłówek/eyebrow/karta wszystkie na `344.5px`, ostatnia karta po przewinięciu nadal na prawej krawędzi dostępnej szerokości), ponownie na 1440px (`margin-left:0px`, bez zmian, wyrównanie na `71.25px`) i na 375px (`20px`, bez zmian) — żadna z wcześniej poprawnych szerokości się nie cofnęła. |
| 2026-08-27 | Dwudziesta trzecia runda — margines centrujący nie znikał przy przewijaniu + przeciąganie działało tylko w przerwach między kartami | Zrzut ekranu użytkownika (widok po przewinięciu) + pomiar `scrollLeft`/`getBoundingClientRect` na żywo | Użytkownik zgłosił dwie rzeczy naraz. **(1) Margines nie znikał przy przewijaniu na szerokich ekranach** — naprawa z Rundy 22 (`margin-left` na `.trick-wrap`) usunęła poprzedni błąd wyrównania w widoku początkowym, ale wprowadziła nowy: margines centrujący był teraz `margin` na **zewnętrznym, nieprzewijalnym** `.trick-wrap`, więc zostawał na stałe niezależnie od scrolla, zamiast znikać jak reszta lewego marginesu (który celowo jest realnym `padding-left` na **przewijanym** `.trick-row`, żeby scrollował się razem z treścią). Naprawa: przeniesiono ten sam wzór (`max(0px, calc((100% - var(--container-max))/2))`) z `margin-left` na `.trick-wrap` do `padding-left` na `.trick-row`, dodając go do istniejącego `clamp(20px,5%,72px)` gutter — teraz to jedna, w pełni przewijalna wartość. Zweryfikowane na 2000px: w widoku początkowym nagłówek i pierwsza karta nadal na `344.5px`; po przewinięciu o dokładnie tyle, ile wynosi padding, karta osiąga `left:-0.5px` (czyli faktycznie `0`, prawdziwa krawędź okna) — margines realnie znika, nie zostaje. **(2) Przeciąganie działało tylko w przerwach między kartami, nie na samych zdjęciach** — przyczyna: `.trick-link` to `<a>` przykrywający całą kartę (na wierzchu nad `.trick-img`), a przeglądarki mają natywne zachowanie „przeciągnij ten link" uruchamiane przy ruchu myszy z wciśniętym przyciskiem nad linkiem/obrazkiem — ono przechwytywało gest, zanim zdążył zadziałać nasz JS oparty o `mousemove`. Naprawa: dodano `-webkit-user-drag:none` do `.trick-link` (wcześniej było już tylko na `.trick-img`) oraz `dragstart` → `preventDefault()` na `.trick-row` w JS (przechwytuje przez bąbelkowanie z dowolnego dziecka, pokrywa też Firefox, gdzie `-webkit-user-drag` nie działa). Zweryfikowane realnym gestem `left_click_drag` zaczynającym się **dokładnie na środku obrazka** karty (nie w przerwie) — `scrollLeft` zmienił się o dokładnie tyle, o ile przeciągnięto (400px), `location.hash` pozostał pusty (żaden przypadkowy klik-nawigacja); osobno zweryfikowano, że zwykły klik (bez przeciągnięcia) na obrazku nadal poprawnie nawiguje (`hash` → `#treatments`). |
| 2026-08-27 | Dwudziesta czwarta runda — pozioma paralaksa zdjęć 1:1 z archon.au + naprawiono przycinanie kształtu przy hover | Bezpośredni pomiar `getBoundingClientRect`/inline `style` na żywej archon.au, w tym po realnym przeciągnięciu Flickity (`left_click_drag`), żeby wychwycić żywą zmianę wartości, nie tylko stan spoczynkowy | Użytkownik poprosił o (1) odtworzenie poziomej paralaksy zdjęć znanej z archon.au oraz (2) poprawienie hover, który wyglądał jakby karta była „w kontenerze ograniczającym jej obłe kształty". **(1) Paralaksa** — w Rundzie 7 świadomie pominięta jako zbyt ryzykowna; tym razem zbadana dokładnie: wszystkie 11 zdjęć na żywej stronie miały identyczny `translateX(-6.896%)` w spoczynku, ale **po realnym przeciągnięciu Flickity wartości się rozjechały i zaczęły różnić per karta** (-6.896%, -16.75%, -26.61%, -36.46%, -46.31%, dalej ucięte na `-49%`) — potwierdza, że to nie jest stały, zapisany na sztywno offset per zdjęcie, tylko żywo przeliczana wartość. Skorelowano te wartości z pozycją środka każdej karty względem okna przeglądarki (`getBoundingClientRect().left + width/2`, względem `window.innerWidth`) — zależność liniowa, spójna na 3+ punktach pomiarowych: `translateX% ≈ -(centerX / innerWidth) × 47`, ograniczone do ±49% (tuż poniżej granicy, przy której 200%-szerokie zdjęcie zaczęłoby odsłaniać pusty brzeg). Odtworzone 1:1: `.trick-img` zmienione z `width:100%` na `width:200%;max-width:none` (jak realne `.parallaxhero`), JS (`js/main.js`) przelicza `translateX` dla każdego zdjęcia tym samym wzorem, nasłuchując `scroll` na `.trick-row` (łapie zarówno natywne przewijanie, jak i nasz drag, bo oba operują na `scrollLeft`) i `resize` na oknie, throttlowane przez `requestAnimationFrame`. Zweryfikowane: wartości początkowe różnią się per karta i rosną w kierunku `-49%` (cap), a po zmianie `scrollLeft` (symulującej przewinięcie) przeliczają się na nowo na żywo. **(2) Przycinanie przy hover** — sprawdzono bezpośrednio na archon.au: `.tricks-slider`/`.flickity-viewport` mają `overflow:visible` na **obu** osiach, bo ich przewijanie poziome to transform JS, nie natywny scroll przeglądarki — nie muszą nic przycinać. Nasz `.trick-row` używa prawdziwego `overflow-x:auto` (do naturalnego przewijania/drag), a to zgodnie ze specyfikacją CSS **wymusza** `overflow-y:auto` na drugiej osi (nie da się mieć „scrolluj poziomo, nigdy nie przycinaj pionowo" czystym CSS przy natywnym scrollu) — bez zapasu miejsca u góry, `translateY(-30px)` karty na hover wystawał poza górną krawędź `.trick-row` i był ucinany, co wizualnie wyglądało jak nieprzycięty kształt łuku „wchodzący" w niewidzialny sufit kontenera. Naprawa: dodano `padding-top:var(--sp-40)` do `.trick-row` (40px zapasu, z nadmiarem na 30px liftu) i zmniejszono `margin-top` na `.trick-wrap` o tyle samo (`calc(var(--sp-64) - var(--sp-40))`), żeby całkowity odstęp od tekstu powyżej się nie zmienił. Zweryfikowane pomiarem: przy hover `cardTopVsRowTop:10px` (karta zostaje 10px wewnątrz boksu rzędu nawet po liftowaniu, zamiast wychodzić poza niego) oraz wizualnie zrzutem ekranu — łuk w pełni widoczny, nieucięty, wyraźnie uniesiony nad sąsiednie karty. Sprawdzone też, że transform paralaksy (`.trick-img`) i transform hover (`.trick-card`) współistnieją bez konfliktu (osobne elementy, osobne właściwości transform) oraz że wyrównanie na mobile (375px) się nie cofnęło. |
| 2026-08-27 | Dwudziesta piąta runda — usunięto unoszenie karty na hover + prawdziwe nazwy zabiegów z menu „Zabiegi" | Prośba użytkownika w czacie | **(1) Usunięto lift**: `.trick-card:hover` miało `transform:translateY(-30px)` (1:1 z archon.au) — usunięte na wyraźną prośbę, zostawiono tylko odwrócenie łuku (`border-radius:20px 20px 150px 150px`) i przyciemnienie zdjęcia (`.trick-link:hover`, bez zmian). Skoro nie ma już liftu, cofnięto też kompensacyjny `padding-top:var(--sp-40)` na `.trick-row` i skrócony `margin-top` na `.trick-wrap` z Rundy 24 (były potrzebne wyłącznie jako zapas na uciekający w górę lift) — z powrotem `margin-top:var(--sp-64)` wprost, bez dodatkowego paddingu. Zweryfikowane: `getComputedStyle(karta).transform` przy hover to `none` (nie ma liftu), `borderRadius` nadal się odwraca. **(2) Podpisy kart zmienione z archońskich (Depilacja Laserowa dla Mężczyzn, Terapia Światłem LED, Zabieg Laser Genesis, Zabiegi Poziomu 2, Makijaż Permanentny Ust, Usuwanie Tatuaży, Frakcyjny RF — żadna z tych nazw nie występuje w realnym menu „Zabiegi" zbudowanym w Rundzie 9) na 11 prawdziwych nazw z tego menu, dobranych przez dopasowanie treści zdjęcia do sensu zabiegu (bez zmiany samych plików zdjęć — to nadal te same 11 fotografii z CDN archon.au, tylko z innymi podpisami): mężczyzna/depilacja→**Epilacja laserowa LightSheer®**, kobieta ze światłem→**Bloomea®** (zabieg na bazie światła, ten sam charakter co oryginalne LED), kobiety/sylwetka→**Endermologia LPG® Alliance**, mężczyzna/plecy→**Kriolipoliza cooltech®**, kobieta/blask skóry→**Oczyszczanie wodorowe**, twarz z piegami→**Mikrodermabrazja**, usta z bliska→**Kwas hialuronowy** (najczęstszy realny zabieg na usta), kobieta/nogi→**Lipoliza Iniekcyjna**, zbliżenie na skórę→**Mikronakłuwanie SkinPen®** (już było najbliższe, dopasowanie 1:1), mężczyzna/tors z tatuażami→**Karboksyterapia**, twarz z piegami (2. zdjęcie)→**Radiofrekwencja mikroigłowa RF** (nazwa zabiegu dosłownie odpowiadała nazwie pliku `fractional-rf.jpg`). `href` każdej karty zmieniony z placeholdera `#treatments` na faktyczny adres tego zabiegu z listy ustalonej w Rundzie 9 (tam gdzie zabieg miał realny, zaindeksowany URL z sekcji SEO — 9 z 11 — użyto go; dla „Bloomea®" i „Kwas hialuronowy" [`wypelnianie-kwasem-hialuronowym-plock`] również z tej listy) — nie było to wprost zgłoszone, ale zostawienie starego `#treatments` przy realnych nazwach zabiegów wyglądałoby na przeoczenie, więc zmienione przy okazji tej samej edycji. Zweryfikowane odczytem `textContent`/`href` wszystkich 11 kart z żywego DOM. **Alt teksty** zdjęć dostosowane opisowo do nowego kontekstu (nadal opisują to, co faktycznie widać na zdjęciu, nie fikcyjną treść). **Nadal aktualne**: to wciąż zdjęcia stockowe z Australii (nie instytutem.pl), teraz podpisane prawdziwymi nazwami polskich zabiegów — jeszcze mocniejszy sygnał, że wymagają podmiany na faktyczne fotografie przed publikacją. |
| 2026-08-27 | Dwudziesta szósta runda — min/max-width karty 1:1 z archon.au, wszystkie 22 zabiegi w kolejności menu, naprawiony overflow podpisu | Pomiar `getBoundingClientRect` na żywej archon.au przy wielu szerokościach okna, **z pełnym przeładowaniem strony przy każdej** (patrz niżej dlaczego) | Użytkownik poprosił o (1) min/max-width karty zgodnie z archon.au, (2) weryfikację wyświetlania nazw zabiegów w wąskich widokach, (3) dodanie wszystkich zabiegów z menu „Zabiegi" (placeholder tam, gdzie brak zdjęcia) i (4) kolejność kart zgodną z tym menu. **(1) min/max-width** — zmierzono szerokość `.tricks-slider_slide` na archon.au w szerokim zakresie okien (320–3000px). Odkryto nietrywialny kształt krzywej: karta **rośnie** do ok. 1600–1700px szerokości okna (szczyt ~314–315px), po czym **maleje z powrotem** przy jeszcze szerszych oknach (296px przy 2560px, 286px przy 3000px) — bo `.homegallery{max-width:100em}` usztywnia szerokość samego rzędu na 1600px, ale jego `padding:0 5%` liczy się względem coraz szerszego rodzica (okna), więc realna przestrzeń na karty w środku **kurczy się** mimo że kontener już się nie powiększa. Ważne odkrycie proceduralne: **pierwsze pomiary przy 320px dały ewidentnie błędną wartość (64px zamiast ~173px)** — Flickity nie zawsze przelicza szerokość komórek po samym resize viewportu (bez natywnego zdarzenia `resize`/przeładowania), zostawiając nieaktualną wartość z poprzedniej szerokości; naprawione przez pełne `location.reload()` przy każdym pomiarze, po czym wynik był powtarzalny. Finalne wartości: **max-width:315px** (szczyt krzywej, ~1700px okna), **min-width:173px** (realna wartość przy 320px, najwęższym typowym telefonie). Dodane do `.trick-card` — działają razem z istniejącymi procentowymi `width` na każdym breakpoincie, bez zmiany logiki responsywnej. Zweryfikowane na żywo: 2560px → karta usztywniona na `315px` (zamiast ~563px, jakie dałoby surowe 22%); wąski telefon → `173px` floor. **(2) Weryfikacja nazw w wąskich widokach — znaleziony i naprawiony realny bug**: przy 375px podpis „Peelingi medyczne/chemiczne" wychodził poza kartę (`capWidth:241px` > `cardWidth:213px`) — przeglądarka nie zawsze traktuje `/` bez spacji dookoła jako miejsce łamania wiersza, więc cały fragment „medyczne/chemiczne" renderował się jako jeden nierozdzielny ciąg. Dodatkowo `.trick-cap` jest bezpośrednim dzieckiem flex-a (`.trick-link{display:flex}`), a flex-item domyślnie ma `min-width:auto` (szerokość najszerszego nierozdzielnego fragmentu) — to samo w sobie ignorowałoby jakikolwiek `max-width`/`overflow-wrap` bez jawnego zniesienia. Naprawa: `.trick-cap` dostał `max-width:100%`, `min-width:0` (znosi domyślne ograniczenie flex-item) i `overflow-wrap:break-word`. Zweryfikowane: zero podpisów z przekroczeniem szerokości karty na 375px i na skrajnym minimum 173px (test wszystkich 22 kart programowo, nie tylko wzrokowo). **(3) i (4) Pełne 22 zabiegi, w kolejności menu „Zabiegi"** — rozszerzono rząd z 11 do wszystkich 22 pozycji z Rundy 9, w dokładnie tej samej kolejności co w menu głównym (Hi-Tech ×9, Iniekcyjne ×9, Manualne ×4). 11 pozycji, które już miały dopasowane zdjęcie z Rundy 25, zachowało je na tych samych miejscach (przesuniętych w nowej kolejności); pozostałych 11 (LPG® Ergolift, Plasma™, Autologiczne wypełniacze ATR, Kwas polimlekowy PLLA, Mezoterapia igłowa, Nici liftingujące PDO/PLLA, Fibryna i osocze bogatopłytkowe, Stymulatory tkankowe, Mezoterapia mikroigłowa, Mezoterapia bezigłowa, Peelingi medyczne/chemiczne) nie ma jeszcze zdjęcia — zamiast `<img>` mają ten sam placeholder `<div class="trick-img ph-gradient">` (na przemian zwykły/`warm`, `role="img"` + `aria-label="TODO: podmienić na prawdziwe zdjęcie — [nazwa]"`) już używany dla kart RX Facials i tła bannera rezerwacji — spójna, istniejąca konwencja, nie nowy wzorzec. JS paralaksy (`js/main.js`) zawężony z selektora `.trick-img` na `img.trick-img`, żeby nie próbował przesuwać transformem płaskich gradientów bez treści zdjęciowej. Zweryfikowane: `document.querySelectorAll('.trick-card').length === 22`, kolejność podpisów 1:1 z listą menu, placeholder renderuje się wizualnie tym samym stylem co reszta strony. |
| 2026-08-27 | Dwudziesta siódma runda — dodano 2 brakujące zabiegi do galerii, zmieniono nazwę „Peelingi", dodano „Listę zabiegów" jako źródło prawdy w dokumentacji | Porównanie DOM galerii (`#treatments`) z DOM menu „Zabiegi" (`#zabiegiMegaPanel`) na żywo | Użytkownik zgłosił brak dwóch zabiegów w galerii: „Laser Tułowy" i „lampa Dermalux". **Zbadano przyczynę zamiast od razu poprawiać**: okazało się, że menu „Zabiegi" (nav desktop, drawer mobilny, stopka) **już zawierało** oba te zabiegi — dodane w Rundzie 12 przez równoległą sesję pracującą nad tym samym repo (potwierdzone wpisem w tym logu). Galeria kart na stronie głównej (zbudowana przeze mnie w Rundzie 26, w tej samej sesji co obecna) bazowała na starszej, 22-elementowej wersji listy sprzed Rundy 12 i nigdy nie została z nią zsynchronizowana — stąd rozjazd, który zauważył użytkownik. Naprawa: dodano „Laser Tulowy Erbowo-Szklany" (`/laser-tulowy-plock`) zaraz po „Epilacja laserowa LightSheer®" i „Fototerapia LED Dermalux®" (`/fototerapia-dermalux-plock`) zaraz po „Oczyszczanie wodorowe" — dokładnie w tych samych pozycjach, co w menu „Zabiegi" — oba jako placeholder (`.ph-gradient`, brak zdjęcia, tak jak reszta niedopasowanych pozycji). Zweryfikowane programowo: `querySelectorAll('.trick-card')` i `querySelectorAll('#zabiegiMegaPanel a')` dają **tę samą liczbę (24) i tę samą kolejność tekstu** — nie tylko wzrokowo. **Zmieniono nazwę** „Peelingi medyczne/chemiczne" → „Peelingi Medyczne" na wyraźną prośbę, w **4 miejscach jednocześnie** (mega-menu, drawer, stopka, karta galerii + jej `aria-label`) przez `sed` + ręczną edycję karty. **Dodano sekcję „Lista zabiegów (oferta) — źródło prawdy"** w tym dokumencie (nazwa/URL/status zdjęcia dla wszystkich 24 pozycji, w 3 kategoriach) oraz punkt 5 do „Zasad prowadzenia dokumentacji" nakazujący aktualizować tę listę przy każdej zmianie oferty i sprawdzać nią wszystkie 4 miejsca powielenia (nav/drawer/stopka/galeria) — właśnie po to, żeby ten rozjazd się nie powtórzył. **Przy budowie listy zauważono i świadomie NIE poprawiono** (wymaga potwierdzenia użytkownika, nie oczywisty błąd): „LPG® Ergolift" (URL wymyślony w Rundzie 9, `/zabiegi/lpg-ergolift`) może być tym samym zabiegiem co realny, zaindeksowany `/zabiegi/endermologia-twarzy-plock-lpg-endermolift` — ale użytkownik w Rundzie 13 (innej sesji) już raz odrzucił dodanie „Endermologia Twarzy" jako duplikatu, więc zostawiono bez zmian i opisano w nowej sekcji jako otwarte pytanie zamiast zgadywać. |
| 2026-08-27 | Dwudziesta ósma runda — naprawiono niepoprawną wartość „Nagłówek sekcji" w DESIGN-SYSTEM.md (i w kodzie), obowiązującą sitewide | Bezpośredni pomiar `getComputedStyle` + odczyt reguł źródłowych (`document.styleSheets`, w tym media queries) na żywej archon.au dla 7 różnych sekcji | Użytkownik poprosił o weryfikację, czy tytuł i akapit sekcji galerii zabiegów mają ten sam format co archon.au, zaznaczając przypuszczenie, że to domyślny styl dla wszystkich sekcji. **Zmierzono `.heading.h2` (klasa nagłówka „If it matters to you it matters to us." — odpowiednik naszej „Jeśli to ważne...")**: realnie **30px / waga 500 / line-height 36px / letter-spacing -1px, płaskie na każdej szerokości okna** (sprawdzono reguły źródłowe łącznie z media queries — brak jakiejkolwiek zmiany `font-size` przy żadnym breakpoincie). To **kompletnie inna wartość** niż to, co było zapisane w DESIGN-SYSTEM.md („~48px / -2,5px", nigdy niezmierzone bezpośrednio — dokładnie ten sam typ błędu, przed którym ostrzega zasada nadrzędna w CLAUDE.md o niezgadywaniu wartości) i w kodzie (`.h-section{font-size:clamp(1.9rem,4vw,3rem)...}`, wagi domyślnie 400 z bazowej reguły `h1,h2,h3`, letter-spacing -0.04em). **Potwierdzono, że to faktycznie wspólny, dzielony styl** — sprawdzono 4 różne sekcje na archon.au (filozofia marki, RX Facials/„_80", opinie klientów/„center", vouchery) i wszystkie dają identyczne 30px/500/36px/-1px, różniące się tylko kolorem tekstu (`--a-ink-warm` na jasnym tle, `--a-cream` na ciemnym) — dokładnie tak, jak podejrzewał użytkownik. Naprawiono `.h-section` w `css/styles.css` na płaskie wartości (bez `clamp`/`vw`) + dodano `margin-bottom:var(--sp-24)` (też zmierzone, `24px`, wcześniej brakujące w bazowej klasie). Usunięto **zbędny, dublujący się override** `.testimonial-section .h-section` (ustalony w Rundzie 6 jako lokalna łatka tylko dla opinii, bo wtedy nie rozpoznano, że to WŁAŚCIWA wartość dla całej witryny, nie wyjątek) — bazowa klasa teraz sama daje identyczny wynik. Przy okazji naprawiono też `.lede` (akapit): było `1.05rem/1.75` (16,8px/29,4px), powinno być dokładnie **16px/27,2px** (już poprawnie opisane w DESIGN-SYSTEM.md, ale niedokładnie wdrożone w CSS). Zaktualizowano tabelę typografii w DESIGN-SYSTEM.md z notatką o korekcie. **Zweryfikowano pomiarem na naszej stronie**: `.h-section` = `30px/500/36px/-1px/24px` i `.lede` = `16px/27.2px/400` na 1440px, oraz `30px` (bez zmiany) przy 1920px — potwierdzone brakiem skalowania. **Ważne odkrycie przy weryfikacji „czy to dotyczy wszystkich sekcji"**: NIE dla wszystkich 7 miejsc, gdzie używamy `.h-section` w tym samym kodzie. Sekcja Kontakt („Bądźmy w kontakcie") i cytat w sekcji Newsletter/programu lojalnościowego („Zrób sobie chwilę poza codziennością") odpowiadają na archon.au zupełnie innej, większej klasie (`.heading.hero`/`.heading.display`, realnie **60px**, nie 30px) — **świadomie NIE tknięte w tej rundzie** (wymagałoby nowej klasy, szerszy zakres niż to, o co poproszono). Baner „Archon Rewards — Dołącz za Darmo" na archon.au to w ogóle nie nagłówek sekcji, tylko mała etykieta wielkimi literami (`h4.uppercase`, 12px/600/uppercase, bardzo bliska naszej istniejącej `.eyebrow`) — też **NIE tknięte**, tylko opisane jako do sprawdzenia. Oba wyjątki opisane wprost w DESIGN-SYSTEM.md i w podsumowaniu dla użytkownika, żeby nie zostały niezauważone. |
| 2026-08-27 | Dwudziesta dziewiąta runda — naprawiono wyrównanie i szerokość akapitu (`.lede`) względem nagłówka w układzie `.two-col-grid` | Bezpośredni pomiar `getBoundingClientRect`/`getComputedStyle` na żywej archon.au (z ominięciem aktywnej animacji scroll-reveal, patrz niżej) oraz na naszej stronie | Użytkownik zgłosił, że akapit w sekcji Filozofii (i przez to samo we wszystkich sekcjach z układem `.two-col-grid`) zaczyna się niżej niż nagłówek obok i ma inną szerokość niż „koniec okna". **Znaleziono dwa realne, niezależne błędy we własnym CSS** (nie cechę do skopiowania z archon.au — tam to jest wyrównane 1:1). (1) `.lede` miało `max-width:60ch` — starą, niezmierzoną wartość z wcześniejszej rundy; realny akapit na archon.au ma `max-width:none` i po prostu wypełnia całą szerokość kolumny `2fr` gridu (potwierdzone identyczną szerokością kolumny na obu stronach przy tym samym viewporcie: `732.34375px`). (2) w kodzie nigdy nie zresetowano domyślnego marginesu `<p>` (`1em` górą/dołem z przeglądarki) — `.lede` dostawał więc niechciane `16px` odstępu od góry, wizualnie zsuwając go w dół względem nagłówka wyrównanego przez `align-items:start` w gridzie (który wyrównuje same elementy gridu, nie tekst wewnątrz z własnym marginesem). Naprawa w `css/styles.css`: `.lede{max-width:none; margin:0;}` (reszta właściwości bez zmian). Cache-bust `css/styles.css?v=15` → `?v=16`. **Napotkana przeszkoda przy pomiarze na archon.au**: `.heading.h2` ma aktywną animację scroll-reveal sterowaną `IntersectionObserver` (inline `transform:translate3d(...)rotateX(-50deg)...;opacity:0` przed odsłonięciem) — `scrollIntoView()`/scroll programowy jej nie uruchamiał, dając fałszywy odczyt „~46px" różnicy. Wykryte przez inspekcję atrybutu `style` w trakcie animacji; obejście: tymczasowe wymuszenie `opacity:1;transform:none!important` czysto do pomiaru, co ujawniło prawdziwy stan spoczynkowy — nagłówek i akapit różnią się tylko o `-3px` (własny, zamierzony `margin-top` nagłówka), czyli **są poprawnie wyrównane do góry**, co potwierdziło, że cel to zero różnicy, nie jakiś inny konkretny odstęp. **Zweryfikowane na naszej stronie po poprawce** (pomiar DOM, nie tylko zrzut ekranu — zrzuty przy przewiniętej pozycji są w tym narzędziu zawodne, znany problem z tej sesji): sekcja Filozofii — `hTop===pTop===854.39px` (`diff:0`), `pMaxWidth:"none"`, `pWidth:732.34375px` (=dokładnie szerokość kolumny `2fr`), `gridColumns:"366.156px 732.344px"` (zgodne z proporcją 1:2 i z pomiarem na archon.au). Sprawdzono też pozostałe 2 sekcje z tym samym układem (RX Facials, Vouchery) — obie `diff:0`. Piąty akapit `.lede` na stronie (baner „Zyskaj 25$ zniżki") nie jest w `.two-col-grid`, ma inny, węższy kontener (`640px`) — to jego zamierzona szerokość wynikająca z układu sekcji, nie regresja z tej poprawki. Potwierdzone wizualnie zrzutem ekranu (viewport 1280×1000, scrollY:0, żeby ominąć znaną zawodność zrzutów przy przewinięciu). |
| 2026-08-27 | Trzydziesta runda — podmieniono 7 placeholderowych opinii z Archon (Australia) na prawdziwe opinie klientów instytutem.pl + dodano CTA „Więcej opinii”/„Zostaw opinię” | Bezpośredni odczyt DOM (`get_page_text`/`innerText`) żywej strony www.instytutem.pl i żywego profilu Fresha instytutem.pl przez Browser MCP — bez zgadywania treści | Na prośbę użytkownika zebrano realną treść zamiast dalej trzymać przetłumaczone opinie australijskich klientów Archon (Isaac W., Anthony, Liz, masaż szwedzki — nigdy niezwiązane z instytutem.pl). **Źródła, oba zweryfikowane bezpośrednim odczytem DOM, nie WebFetch/pamięcią**: (1) sekcja „Dziękujemy Wam za zaufanie!” na samej stronie głównej instytutem.pl — 3 opinie z pełnymi imieniem i nazwiskiem (Magdalena Górska, Karolina Piotrowska, Katarzyna Mazuchowska), wszystkie 5/5; (2) profil Fresha instytutem.pl (`fresha.com/a/instytutem-tm-plock-1-maja-6-jrr27hgf`, 5,0/33 opinie na dzień pomiaru) — kolejne 4 wybrane spośród 6 najnowszych, każda z realną datą względną, wykonaną usługą i terapeutką (Małgorzata C. — epilacja laserowa LightSheer®, z Nikola; Marzena D. — wizyta kontrolna, z Ewelina; Elżbieta Ż. — kwas hialuronowy, z Ewelina; Jolanta Z. — oczyszczanie wodorowe, z Ewelina); pominięto Beata M. i Lena K. (obie „2 usługi”, zbyt ogólne, mniej wartościowe SEO-wo niż nazwane zabiegi). **Decyzje treściowe podjęte z użytkownikiem** (nie zgadywane): 7 opinii łącznie (3 z instytutem.pl + 4 z Fresha, wypełnia istniejące 7 slotów karuzeli, `.t-track`/`.t-slide` mają `calc(100%/7)` zaszyte na sztywno w CSS, więc liczba 7 zachowana celowo, żadna zmiana JS/CSS karuzeli nie była potrzebna); podpis w formacie „Imię + Inicjał.” (np. „Magdalena G.”) zgodnie z zasadą z DESIGN-SYSTEM.md §1 wzorowaną na archon.au — świadome odejście od pełnych nazwisk, jakie faktycznie widnieją dziś na instytutem.pl. **Nowa funkcjonalność, potwierdzona z użytkownikiem, nieobecna na archon.au**: pod `.t-dots` dodano `.t-cta` z dwoma linkami `.abtn` (klasy i kolory 1:1 z tabelą przycisków w DESIGN-SYSTEM.md §4, żadnych nowych wariantów) — „Więcej Opinii” (`.abtn.outline-light`) i „Zostaw Opinię” (`.abtn.white`) — z URL-ami znalezionymi bezpośrednio w DOM strony instytutem.pl (`<a>` w jej własnej sekcji opinii), nie wymyślonymi: panel opinii Google (`google.com/search?q=instytutem...#lrd=...`) i bezpośredni link do wystawienia opinii (`g.page/r/CZrceaMFksUUEB0/review`). Dodano do `css/styles.css` tylko nowy blok `.t-cta{display:flex;justify-content:center;gap:16px;margin-top:40px}` — reużywa istniejące klasy `.abtn`, zero nowych reguł kolorów/rozmiarów. **Zweryfikowane**: `read_page` na lokalnym serwerze potwierdza 7 przycisków kropek (`Przejdź do opinii 1 z 7` … `7 z 7`) i oba linki CTA z poprawnymi `href`; `getComputedStyle` obu przycisków potwierdza zgodność z design systemem (`height:44px`, `border-radius:2px`, kolory dokładnie jak w tabeli); kliknięcie strzałki „następna” przesuwa `#testimonialTrack` o `translateX(-800px)` (czyli o szerokość jednej z 7 kart w masce 800px) — karuzela działa z nową treścią bez zmian w `js/main.js`. Zrzut ekranu w panelu Browser MCP przy przewiniętej pozycji wyszedł pusty/kremowy — to już udokumentowana usterka narzędzia (patrz CLAUDE.md, pkt 12), nie błąd w kodzie; wizualna kontrola zamiast tego zrobiona w zewnętrznej domyślnej przeglądarce (`open http://localhost:8090`), zgodnie z zasadą projektu. **Nadal otwarte**: treść pozostałych sekcji strony (hero, oferta, dane kontaktowe) to wciąż Australia/Archon — ta runda dotyczyła wyłącznie sekcji opinii, o to poprosił użytkownik. |
| 2026-08-27 | Trzydziesta pierwsza runda — naprawiono błąd z Rundy 6: dopisek źródła opinii („Google Review - 5 Stars”) jednak istnieje na archon.au, dodano 1:1 | Bezpośredni pomiar DOM (`innerText`, `getComputedStyle`) na żywej archon.au, po zgłoszeniu przez użytkownika, że dopisek jednak tam jest | Użytkownik zgłosił, że na archon.au pod każdą opinią jest dopisek źródła — sprzeczne z wcześniejszym ustaleniem z Rundy 6 („nie istnieje w oryginale”). **Zamiast zaufać własnej pamięci/wcześniejszemu ustaleniu, zmierzono ponownie na żywo** (zgodnie z zasadą nadrzędną CLAUDE.md) i potwierdzono, że użytkownik miał rację — Runda 6 przeoczyła ten element. Zmierzona struktura każdego slajdu: `.testimonial` (cytat) → `.testimonial-name` (imię) → `._5-stars` (gwiazdki, `margin-bottom:5px`) → `.small-text-2` („Google Review - 5 Stars”, Inter 600 12px/line-height 24px, kolor cream, `margin:0`). Odtworzone 1:1 w `index.html` jako nowy `<div class="t-source">` po `.t-stars` w każdym z 7 slajdów, i w `css/styles.css` jako nowa klasa `.t-source` z dokładnie zmierzonymi wartościami + `margin-bottom:5px` dodany do istniejącego `.t-stars`. **Treść dopisku dostosowana do prawdziwego źródła każdej opinii** (nie kopiowana ślepo jako „Google” dla wszystkich — zgodnie z zasadą „nie zgaduj”): 3 opinie pochodzące z głównej strony instytutem.pl (Magdalena G., Karolina P., Katarzyna M.) → „Opinia Google - 5 Gwiazdek” (użytkownik, właściciel treści źródłowej, potwierdził w czacie, że to faktycznie opinie z Google — nie do zweryfikowania samym kodem strony, bo są wpisane jako statyczny tekst Wix, nie żywy widget Google); 4 opinie z profilu Fresha (Małgorzata C., Marzena D., Elżbieta Ż., Jolanta Z.) → „Opinia Fresha - 5 Gwiazdek” (potwierdzone bezpośrednio, bo pobrane z samej platformy Fresha). **Zweryfikowane**: odczyt `getComputedStyle(.t-source)` na naszej stronie po zmianie daje dokładnie `Inter/12px/600/24px/rgb(241,237,231)` — identycznie jak na archon.au; `textContent` wszystkich 7 elementów `.t-source` potwierdzony programowo. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090`), zgodnie z zasadą projektu. |
| 2026-08-27 | Trzydziesta trzecia runda — wyśrodkowano nagłówek sekcji opinii (1:1 z archon.au) + dodano więcej odstępu w najdłuższych cytatach (świadome odejście na prośbę użytkownika) | Bezpośredni pomiar `getComputedStyle` na żywej archon.au (klasa `.center`) + porównanie długości naszej najdłuższej opinii z najdłuższą na archon.au | Użytkownik zgłosił dwie rzeczy: (1) nagłówek sekcji opinii powinien być wyśrodkowany jak na archon.au, (2) w najdłuższych opiniach jest za mało „powietrza”. **(1) Nagłówek**: zmierzono na żywo — nagłówek testimonial na archon.au ma klasę `heading h2 center`, a `.heading.h2.center{text-align:center}` to jedyna różnica względem bazowej `.heading.h2` (font-size/weight/line-height/letter-spacing i margin-bottom identyczne z resztą `.h-section` — potwierdzone wcześniej w Rundzie 28). Naprawa: dodano scoped `.testimonial-section .h-section{text-align:center}` w `css/styles.css` — nic więcej nie zmienione, zgodnie z zasadą minimalnej, celowej różnicy. **(2) Odstęp w cytatach**: zanim wprowadzono zmianę, zmierzono, czy to faktycznie niezgodność z archon.au, czy realny problem treściowy — okazało się to drugie: nasze marginesy (30/20/5px) i line-height (27,2px) cytatu są już 1:1 zmierzone z archon.au (żadna rozbieżność), ale **nasza najdłuższa opinia (Katarzyna M., 406 znaków) jest dłuższa niż najdłuższa opinia na samym archon.au (Joel P., 384 znaki)** — przy identycznym odstępie wygląda gęściej niż jakikolwiek przykład na oryginale. Po wyjaśnieniu tego użytkownikowi w rozmowie, wdrożono **świadome, potwierdzone odejście od archon.au** (nie 1:1): `.t-slide blockquote{line-height:30px}` (z 27,2px) i `margin-bottom:36px` (z 30px) — dotyczy wyłącznie tego komponentu, nie zmienia sitewide tokenu `.lede`/tekstu podstawowego (16px/27,2px, nadal obowiązuje wszędzie indziej). Opisane w DESIGN-SYSTEM.md (sekcja „Testimoniale”) jako jawny wyjątek z uzasadnieniem, żeby przyszła runda nie próbowała tego „naprawić” z powrotem do 1:1. **Zweryfikowane**: `getComputedStyle` na naszej stronie po zmianie — nagłówek `text-align:center`, cytat `line-height:30px`/`margin-bottom:36px`. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090`). |
| 2026-08-28 | Trzydziesta czwarta runda — naprawiono styl nagłówka sekcji Kontakt (nowa klasa `.h-hero`) + podmieniono treść z Australii na realne dane instytutem.pl | Bezpośredni pomiar `getComputedStyle` + reguły źródłowe (`document.styleSheets`, w tym media queries) na `.heading.hero.center` żywej archon.au, z neutralizacją nieuruchomionej animacji scroll-reveal (`opacity:1;transform:none!important` tylko do pomiaru, technika z CLAUDE.md pkt 11) | Użytkownik poprosił o pracę nad sekcją „Bądźmy w kontakcie”; z dwóch znanych otwartych punktów w PLAN.md (styl nagłówka + treść AU) wybrał oba. **(1) Nagłówek**: zmierzono `.heading.hero.center` na archon.au (element „Get in touch”) — realnie **60px / waga 500 / line-height:1 (=60px, skaluje się 1:1 z font-size) / letter-spacing -2px / margin-bottom 35px**, `text-align:center` tylko z modyfikatora `.center` (baza `.heading.hero` jest `text-align:left`), breakpoint `max-width:479px → 42px`. To rozstrzyga niepewność zapisaną w DESIGN-SYSTEM.md z Rundy 28 („-1px do -2px”, nigdy dokładnie przypisane do tego konkretnego miejsca) — dla Kontaktu jest to jednoznacznie **-2px**. Dodano nową klasę `.h-hero`/`.h-hero.center` w `css/styles.css` (mirror struktury źródłowej archon.au: baza + modyfikator `.center`, żeby dało się użyć też bez centrowania w przyszłości), podpięto do `<h2>` w `#contact` w miejsce błędnego `.h-section`. Przy tej samej okazji zmierzono i poprawiono odstęp eyebrow→nagłówek: realnie `20px` (margines eyebrow) + `1px` (margines nagłówka) ≈ `21px`, zaokrąglone do `20px` (inline `style="margin-top:20px"`, było zgadnięte `.75rem`=12px). **Odkryty przy okazji, NIE naprawiony w tej rundzie** (poza zakresem proszonej poprawki): odstęp między nagłówkiem a rzędem 3 kolumn kontaktu na archon.au to `150px` (element `.spacer.xlarge` w DOM, zmierzony po tej samej neutralizacji animacji) — u nas `.contact-grid{margin-top:40px}`, wyraźnie mniej; dodane do PLAN.md jako nowy punkt do decyzji. **(2) Treść**: zamiast dalej trzymać dane Archon Australia (telefon `3852 5660`/`+61738525660`, adres „100 Commercial Road, Teneriffe QLD 4005, Australia”, e-mail `hello@archonspas.com.au`), podmieniono na te same realne dane instytutem.pl, które już wcześniej znaleziono i zweryfikowano dla stopki (Runda 11) — **jedno źródło prawdy, nie nowy research**: telefon `+48 737 836 092` (`tel:+48737836092`), adres „ul. 1 Maja 6, 09-402 Płock” z linkiem do Google Maps (`https://goo.gl/maps/upRkkPpan7BiD86L9`, ten sam link co w stopce, zamiast generycznego `maps.google.com/?q=...`), e-mail `recepcja@instytutem.pl` (zachowano istniejący, polski `subject` linku mailto). **Zweryfikowane**: `getComputedStyle` na lokalnym serwerze (`http://localhost:8090`, otwarty jako druga karta w panelu przeglądarki, bo port 8090 był już zajęty przez serwer równoległej sesji — zweryfikowano `lsof`, że to inny proces tego samego katalogu, nie konflikt do rozwiązania) potwierdza `60px/500/60px/-2px/center/35px`, odstęp eyebrow→nagłówek `20px` (dokładnie zgodny z pomiarem na archon.au), oraz poprawne `href` wszystkich 3 linków i treść adresu. Zrzut ekranu w panelu Browser MCP przy przewiniętej pozycji wyszedł pusty/kremowy — znana usterka narzędzia (CLAUDE.md pkt 12), nie błąd w kodzie; strona otwarta też w zewnętrznej domyślnej przeglądarce (`open http://localhost:8090#contact`) zgodnie z zasadą projektu. **Poprawka doraźna tej samej rundy**: użytkownik zgłosił, że nagłówek w przeglądarce nadal wygląda na złym rozmiarze — przyczyną było zapomniane podbicie cache-bust w `<link rel="stylesheet" href="css/styles.css?v=16">` (bez zmiany parametru `v`, przeglądarka mogła serwować starą, scache'owaną wersję CSS mimo zmiany treści pliku na dysku) — ustalona konwencja projektu (patrz Runda 29) wymaga bumpa przy każdej zmianie CSS. Naprawione: `?v=16` → `?v=17`. Zweryfikowane ponownym odczytem `getComputedStyle` po przeładowaniu — `60px/500/60px/-2px`, potwierdzone też `link.href` wskazujące na aktualny plik. **Druga poprawka doraźna tej samej rundy**: użytkownik zgłosił, że etykieta „SKONTAKTUJ SIĘ Z NAMI” też musi wyglądać jak na archon.au, oraz że odstęp do bloków kontaktowych jest za mały. Ponowny pomiar `p.uppercase.center` na archon.au (ta sama technika neutralizacji animacji) potwierdził, że rozmiar czcionki etykiety był już zgodny (12px/600/uppercase/+0.5px — istniejąca `.eyebrow` to już miała), jedyna realna różnica to wyrównanie: archon.au centruje tę etykietę (`text-align:center`, bo cały blok „contact us”/„Get in touch” jest w wyśrodkowanym kontenerze), u nas `.eyebrow` domyślnie do lewej. Naprawione scoped regułą `#contact .eyebrow{text-align:center}` (nie zmieniono bazowej `.eyebrow` — używana w 4 innych miejscach strony z różnym, już zweryfikowanym wcześniej wyrównaniem, zmiana globalna zepsułaby je). Odstęp nagłówek→kolumny zwiększony z `40px` (`var(--sp-40)`, generyczny token) na zmierzone wcześniej w tej samej rundzie **150px** (`.contact-grid{margin-top:150px}`) — realna wartość `.spacer.xlarge` na archon.au między `.heading.hero` a rzędem kontaktowym. Cache-bust `?v=17` → `?v=18`. **Napotkana przeszkoda przy weryfikacji własnej strony**: zwykłe `navigate` z `force:true` na `http://localhost:8090/#contact` nadal ładowało scache'owaną wersję HTML ze starym `?v=16` w linku CSS, mimo że plik na dysku był już `?v=18` (potwierdzone przez `fetch(...,{cache:'no-store'})`, który zwracał aktualną treść) — obejście: nawigacja z dodatkowym, jednorazowym query paramem na samym URL strony (`index.html?bust=99`), który wymusił świeże pobranie HTML pomijając cache przeglądarki na poziomie dokumentu, nie tylko CSS. **Zweryfikowane** (po obejściu cache): `eyebrowTextAlign:"center"`, `gapH2ToGrid:150`, `gridMarginTop:"150px"`, `link.href` wskazujący na `?v=18`. Zrzut ekranu w panelu Browser MCP przy przewiniętej pozycji ponownie pusty/kremowy — ta sama znana usterka narzędzia (CLAUDE.md pkt 12), zignorowana zgodnie z ustalonym procesem; strona otwarta też w zewnętrznej przeglądarce (`open http://localhost:8090#contact`). |
| 2026-08-28 | Trzydziesta piąta runda — dociągnięto sekcję Vouchery 1:1 z archon.au (odstępy, kolory) | Bezpośredni pomiar `getComputedStyle`/`getBoundingClientRect` na żywej archon.au, przy 1440px (viewport 990px domyślnie w panelu dawał inny, 1-kolumnowy układ siatki — trzeba było przeskalować i przeładować stronę, żeby zobaczyć realny 2-kolumnowy layout desktopowy), z neutralizacją nieuruchomionej animacji scroll-reveal na wszystkich mierzonych elementach | Użytkownik poprosił o odwzorowanie sekcji kart podarunkowych 1:1. Struktura DOM już była poprawna z wcześniejszej rundy (eyebrow+divider, 2-kolumnowa siatka 1fr/2fr, przycisk, fine-print — potwierdzone identyczną proporcją kolumn 414px/828px na obu stronach). Zmierzono i naprawiono odstępy oraz kolory: (1) `.divider-rule{margin-top:.75rem}` (12px, zgadnięte) → **20px** (zmierzone, zgodne też z niezależnie zmierzonym odstępem eyebrow→nagłówek w Kontakcie z poprzedniej rundy — spójny wzorzec sitewide); (2) nowa scoped reguła `#voucher .title-rule{margin-bottom:55px}` (był 40px generyczny token) — odstęp divider→siatka zmierzony tylko dla tej sekcji, nie zmieniono współdzielonego `.title-rule` bo brak pomiaru dla Filozofii/RX Facials; (3) przycisk „Kup Cyfrowy Bon" `margin-top:1.5rem`(24px)→**50px**; (4) `.fine-print` (tylko tu używana klasa) `margin-top:.85rem`(13.6px)→**50px**, `font-size:.78rem`(12.48px)→**12px** flat. **Ważniejsze odkrycie przy pomiarze koloru**: `.section--olive .eyebrow` i `.section--olive .lede` miały `color:var(--a-taupe)` — zmierzono bezpośrednio na archon.au (`p.uppercase` i `p.paragraph` w tej sekcji), realny kolor to **kremowy** (`rgb(241,237,231)`, dokładnie `--a-cream`), taupe nigdzie tu nie występuje jako kolor tekstu. To błąd w współdzielonej regule (nie w tej jednej sekcji), więc naprawiono na poziomie `.section--olive .eyebrow`/`.section--olive .lede` — **ubocznie zmienia to też kolor tekstu w RX Facials** (współdzieli te same klasy), efekt pożądany (poprawka błędu, nie świadome odejście), ale nieprzetestowany osobno dla tej sekcji — odnotowane w PLAN.md. Podobnie `.fine-print{color:var(--a-taupe)}` → **`rgba(241,237,231,.52)`** (kremowy przy 52% krycia, zmierzony bezpośrednio na `p.paragraph.small` — inna, jaśniejsza wartość niż solidny taupe). Naprawiono też divider na czarno-oliwkowych sekcjach: realny `.divider-line.light` na archon.au to `rgba(255,255,255,.16)` (przezroczysta biel/kreem, nie solidny szary `--a-gray`) — dodano `.section--olive .title-rule .divider-rule{background:rgba(241,237,231,.16)}` (też wpływa na RX Facials, ten sam powód). Cache-bust `?v=18`→`?v=19`. **Zweryfikowane**: pomiar na naszej stronie po zmianach (przy tym samym 1440px, z pominięciem cache przez `?bust=` na URL strony, ta sama technika co w poprzedniej rundzie) — `eyebrowColor`/`paraColor` = `rgb(241,237,231)`, `dividerBg` = `rgba(241,237,231,0.16)`, odstępy `20/55/50/50px`, kolumny `414.156px/828.344px` — wszystko identyczne z archon.au. Zrzut ekranu w panelu Browser MCP przy przewiniętej pozycji ponownie pusty — znana usterka (CLAUDE.md pkt 12), zignorowana zgodnie z procesem; wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090#voucher`). |
| 2026-08-28 | Trzydziesta szósta runda — zmiana nazewnictwa w całym projekcie: „voucher”/„bon (podarunkowy)” → „karta podarunkowa” | Prośba użytkownika w czacie + `grep -rniE "voucher|bon\b"` po całym `index.html`/`css/styles.css`/`js/main.js`, żeby znaleźć wszystkie miejsca, nie tylko te oczywiste | Zamieniono terminologię wszędzie, gdzie występowała: (1) sekcja główna (dawn. `id="voucher"` → **`id="gift-card"`**, komentarz `<!-- VOUCHERS -->` → `<!-- GIFT CARDS -->`, eyebrow „Bony Podarunkowe na Pakiety Spa” → „Karty Podarunkowe na Pakiety Spa”, nagłówek/akapit/fine-print z „cyfrowy bon”/„bonu” → „cyfrowa karta podarunkowa”/„karty”); (2) przycisk w sekcji „Kup Cyfrowy Bon” → „Kup Cyfrową Kartę Podarunkową”; (3) oba przyciski CTA w nawigacji (desktop + drawer mobilny) „Kup Voucher” → „Kup Kartę Podarunkową”, `href="/vouchery"` → `href="/karty-podarunkowe"` (3 wystąpienia: nav desktop, drawer mobilny, stopka) — potwierdzono najpierw, że `/vouchery` **nie jest** na liście zaindeksowanych URL-i instytutem.pl do zachowania (Runda 9/DOKUMENTACJA.md, sekcja SEO) — to własny, wcześniej wymyślony placeholder tej sesji, nie realny adres z żywej strony, więc zmiana ścieżki nie narusza zasady nadrzędnej „te same URL-e”; (4) link w mega-menu „Dla Klientów” → „Vouchery podarunkowe” → „Karty podarunkowe”, też w komentarzu TODO obok (kategoria „O INSTYTUTem.”); (5) CSS: selektor `#voucher .title-rule{margin-bottom:55px}` (Runda 35) → `#gift-card .title-rule{...}` (musiał zostać przemianowany razem z `id`, inaczej reguła przestałaby się stosować — sprawdzone, że nic więcej w JS nie odwołuje się do `#voucher`, `grep` na `js/main.js` czysty), plus 3 komentarze CSS „Vouchers” → „Gift Cards”. Cache-bust `?v=19`→`?v=20`. **Zweryfikowane**: `document.body.innerText` na naszej stronie po zmianach nie zawiera już żadnego wystąpienia „voucher” ani „bon”/„bony”/„bonu”/„bonem” (dopasowanie regexem z granicami słów, żeby nie złapać przypadkowo np. „bonusu” w sekcji Newsletter — to inne słowo, świadomie nietknięte); `#gift-card .title-rule` nadal ma `margin-bottom:55px` po przemianowaniu selektora (potwierdza, że CSS się nie rozjechał z HTML); oba przyciski CTA w nav i link w stopce mają poprawny, spójny tekst i `href`. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090#gift-card`). |
| 2026-08-28 | Trzydziesta siódma runda — podpięto realny link zakupu karty podarunkowej z Fresha wszędzie, gdzie potrzebny | Prośba użytkownika w czacie (link dostarczony bezpośrednio) + `grep` po `index.html` w poszukiwaniu wszystkich placeholderów `/karty-podarunkowe` i starego archońskiego `bookings.gettimely.com/archonspas/purchase` | Zamieniono `href` na `https://www.fresha.com/book-now/testem-xh2mr620/gift-cards?share=true&pId=602910` (encodowane jako `&amp;` w HTML) w **6 miejscach**: link w mega-menu „Dla Klientów” (desktop), przycisk CTA „Kup Kartę Podarunkową” w nawigacji desktop, te same dwa elementy w drawerze mobilnym, przycisk „Kup Cyfrową Kartę Podarunkową” w samej sekcji (dawniej prowadził na `bookings.gettimely.com/archonspas/purchase` — archoński, australijski Timely, nigdy nieaktualny dla instytutem.pl), link „Karty podarunkowe” w stopce. Wszystkie dostały `target="_blank" rel="noopener"`, zgodnie z ustaloną konwencją strony dla linków zewnętrznych (ten sam wzorzec co linki do Google Maps, Facebooka, opinii Google itd.). **Zweryfikowane**: `document.querySelectorAll('a[href*="fresha.com"]')` na naszej stronie zwraca dokładnie 6 elementów, wszystkie z poprawnym, identycznym `href` (przeglądarka dekoduje `&amp;`→`&` automatycznie) oraz `target:"_blank"`/`rel:"noopener"`. Brak zmian w CSS w tej rundzie, więc bez cache-bustu. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090#gift-card`). |
| 2026-08-28 | Trzydziesta ósma runda — podpięto realny link rezerwacji online z Fresha wszędzie, gdzie potrzebny | Prośba użytkownika w czacie (link dostarczony bezpośrednio) + `grep` po `index.html`/`js/main.js` w poszukiwaniu wszystkich pozostałych linków rezerwacyjnych (`gettimely`, `rezerwuj.instytutem.pl`) | Zamieniono `href` na `https://www.fresha.com/book-now/testem-xh2mr620/all-offer?share=true&pId=602910` w **7 miejscach**: „Rezerwuj online” w nav desktop i drawerze mobilnym (wcześniej `rezerwuj.instytutem.pl` — realny, wcześniej znaleziony link z Rundy 9, teraz świadomie zastąpiony bezpośrednim linkiem Fresha na wyraźną prośbę użytkownika, ten sam wzorzec co karty podarunkowe w poprzedniej rundzie), „Rezerwuj online” w hero (był `bookings.gettimely.com/archonspas/book`, archoński), „Zarezerwuj Online” w bannerze rezerwacji (ten sam archoński link), 3× „Zarezerwuj Tylko To” w kartach RX Facials (były `bookings.gettimely.com/archonspas/bb/book?category=331160` — ten sam, ogólny, nigdy niezweryfikowany dla instytutem.pl link identyczny dla wszystkich 3 różnych zabiegów; teraz przynajmniej prowadzi na realny, właściwy dla instytutem.pl adres rezerwacji, choć wciąż nie jest to link do konkretnego zabiegu — brak takich głębokich linków per-zabieg na Fresha, poza zakresem tej prośby). Wszystkie zachowują `target="_blank" rel="noopener"`. **Zweryfikowane**: `document.querySelectorAll('a[href*="all-offer"]')` zwraca 7 elementów z identycznym, poprawnym `href`; `grep`/JS-owe sprawdzenie `gettimely`/`rezerwuj.instytutem.pl` na całej stronie — zero wystąpień. Brak zmian w CSS, bez cache-bustu. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090`). |
| 2026-08-28 | Trzydziesta dziewiąta runda — dociągnięto Banner Rezerwacji („Łatwe Rezerwacje Online") 1:1 z archon.au + skorygowano pomyłkę nazewniczą z Rundy 28 | Bezpośredni pomiar `getComputedStyle`/`getBoundingClientRect` + reguły źródłowe (`document.styleSheets`, łącznie z 3 media queries) na `.section-parallax.dark`/`.heading.display`/`.button.hero` żywej archon.au, przy 1440px i ponownie przy 990px (ujawniło nietypowy, ale realny quirk: czcionka nagłówka **rośnie** na tablecie względem desktopu) | Użytkownik poprosił o odwzorowanie 1:1. **Ważne odkrycie na starcie**: fraza „Take a moment outside your usual." (u nas „Zrób sobie chwilę poza codziennością.") na archon.au **nie jest** w sekcji Newsletter/programu lojalnościowego, jak błędnie założono w Rundzie 28 (i przepisano do PLAN.md/DESIGN-SYSTEM.md) — jest w tej samej sekcji co „easy online bookings”/„Book Online”, czyli dokładnie naszym Bannerze Rezerwacji (`#book`). Runda 28 pomyliła się przez analogię z pozycją na stronie, nie zweryfikowała bezpośrednio, w której sekcji na archon.au faktycznie leży ten tekst — naprawione w PLAN.md i DESIGN-SYSTEM.md. **Zmierzone i wdrożone**: (1) nowa klasa `.h-quote` — `60px/waga 500/-1px/line-height 94px` (odczytane z bazowej `.heading{...}` + modyfikatora `.display{...}`, nie zgadnięte), z dwoma realnymi breakpointami z samego CSS źródłowego archon.au: `≤991px→72px/71px` (nagłówek **większy** na tablecie niż na desktopie — potwierdzone 2 niezależnymi odczytami przy 990px i inspekcją reguły `@media(max-width:991px){.heading.display{font-size:72px}}`, żeby wykluczyć błąd pomiaru), `≤479px→51px/52px`; kolor czysto biały (`#FFFFFF`, nie `--a-cream` jak reszta `.section--olive`) — scoped `#book .h-quote{color:var(--a-white)}`; (2) sekcja zmieniona z padding-driven (`section-pad`, wysokość zależna od treści) na `.banner-cta{height:600px; display:flex; flex-direction:column; align-items:center; justify-content:center}` — realny `.main-container.display{height:600px}` na archon.au jest **flat**, ten sam wymiar na każdej szerokości (potwierdzone też w media query `@479px{height:600px}` — jawne powtórzenie tej samej wartości, nie inna); (3) przycisk zmieniony z błędnego `.abtn.white` na `.abtn.primary-black` — realny `.button.hero` ma tło `rgb(22,24,16)` (nasze `--a-olive-black`) i biały tekst, nie odwrotnie; (4) naprawiono kolejny przypadek błędu z Rundy 35 (taupe zamiast cream) — `.banner-photo .eyebrow{color:var(--a-taupe)}` miał **tę samą specyficzność** co naprawiona w Rundzie 35 `.section--olive .eyebrow{color:cream}`, więc wygrywał przez kolejność w pliku i po cichu przywracał błędny kolor tylko w tej jednej sekcji — usunięty; (5) odstęp eyebrow→nagłówek 20px (ten sam wzorzec co Kontakt/Vouchery), nagłówek→przycisk 52px (margin-bottom samego `.heading.display`, bez osobnego marginesu na przycisku — potwierdzone, że `.button-wrapper` na archon.au ma `margin:0`, więc dublowanie marginesu byłoby błędem). **Świadome, nieukryte odejścia od 1:1**: (a) pominięto „magnetic” efekt JS przycisku podążającego za kursorem (`.magnetic-wrapper`) — zostawiono już ustalony sitewide hover `.abtn` (unoszenie+cień), zgodny z DESIGN-SYSTEM.md, nie dodawano nowej, jednorazowej mikro-interakcji dla jednego przycisku; (b) pominięto wymuszony `width:150px` przycisku — nasz polski tekst „Zarezerwuj Online” jest dłuższy niż „Book Online”, więc przycisk zostaje auto-width jak reszta `.abtn` na stronie (uniknięcie realnego ryzyka zawijania/ucinania tekstu); (c) nagłówek zostaje semantycznie `<h2>`, nie `<h3>` jak w źródle (Webflow celowo pomija poziom, u nas nie ma takiej potrzeby w tym miejscu dokumentu). Realne zdjęcie tła nadal placeholder (`.ph-gradient`) — nie w zakresie tej rundy, już wcześniej otwarty punkt w PLAN.md. **Zweryfikowane**: pomiar na naszej stronie po zmianach przy 1440/990/420px — `60/72/51px` odpowiednio, `lineHeight` `94/71/52px`, `letterSpacing:-1px`, `color:rgb(255,255,255)`, `sectionHeight:600px` na każdej szerokości, odstępy `20px`/`52px`, przycisk `bg:rgb(22,24,16)`/`color:rgb(255,255,255)`/`height:44px`, eyebrow `color:rgb(241,237,231)`. Cache-bust `?v=20`→`?v=21`. Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090#book`). |
| 2026-08-28 | Czterdziesta runda — podpięto prawdziwe zdjęcie tła w Bannerze Rezerwacji (wygenerowane AI, dostarczone przez użytkownika) | Użytkownik wygenerował zdjęcie (Nano Banana/Gemini, na bazie promptu ustalonego wspólnie w poprzedniej rundzie) i wkleił je bezpośrednio na czacie | **Problem techniczny i jego rozwiązanie**: obraz wklejony bezpośrednio w czacie trafia tylko do kontekstu wizualnego modelu — nie zapisuje się nigdzie na dysku, do czego narzędzia plikowe miałyby dostęp (sprawdzono `/tmp`, `~/.claude`, `~/Library/Application Support/Claude/*`, `/var/folders` — nic). Rozwiązanie: poproszono użytkownika o zapisanie pliku ręcznie do nowo utworzonego `assets/images/banner/`, co zadziałało. **Przetwarzanie pliku**: oryginał `Gemini_Generated_Image_...jpeg` (2,7 MB, 2752×1536px) zoptymalizowany narzędziem `sips` (natywne dla macOS) — `sips -Z 1920 --setProperty formatOptions 82` → **342 KB, 1920×1071px**, bez widocznej utraty jakości (zweryfikowane wizualnie przez odczyt pliku wynikowego), zapisany jako `assets/images/booking-banner.jpg` (czysta nazwa kebab-case zamiast nazwy z generatora), folder tymczasowy `banner/` i oryginał usunięte po przetworzeniu. **Wdrożenie**: `.banner-photo-bg` (dawniej `ph-gradient warm` + `aria-label="TODO..."`) dostał `background-image:url("../assets/images/booking-banner.jpg")`, `background-size:cover`, **`background-position:center top`** — decyzja świadoma, nie domyślna: sekcja ma proporcje dużo szersze (1440×600, ~2,4:1) niż samo zdjęcie (~1,79:1), więc `cover` przycina wysokość, nie szerokość; wyliczono, że przy domyślnym center-crop zostałoby obcięte ~194px u góry i ~194px u dołu (w skali oryginału), co ucinałoby górę głowy — `top` przenosi całe przycięcie na dół (kolana/podołek), zachowując twarz i telefon w kadrze w całości. `aria-label` zaktualizowany z „TODO” na realny opis treści zdjęcia (dostępność). **Świadome odejście od koncepcji z PLAN.md**: punkt „Otwarte TODO” zakładał zdjęcie wnętrza salonu (zgodnie z fotografią archon.au dla tej sekcji, patrz Runda 39) — użytkownik zdecydował się na inny kierunek kreatywny (portret klientki z telefonem) już w poprzedniej rundzie przy ustalaniu promptu, więc to nie przeoczenie, tylko konsekwentna realizacja tamtej decyzji. **Zweryfikowane**: `fetch()` obrazu z lokalnego serwera zwraca `200`/`image/jpeg`/`342468` bajtów; `getBoundingClientRect()` warstwy tła potwierdza pełne pokrycie sekcji (`1425×600px` przy 1440px oknie). Zrzut ekranu w panelu Browser MCP przy przewiniętej pozycji ponownie pusty — znana usterka (CLAUDE.md pkt 12); wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090#book`). Cache-bust `?v=21`→`?v=22`. |
| 2026-08-28 | Czterdziesta pierwsza runda — dociągnięto pasek korzyści (marquee) 1:1 z archon.au, w tym zmiana mechanizmu z pętli CSS na scroll-linked JS | Bezpośredni pomiar `getComputedStyle`/reguły źródłowe (łącznie z 3 media queries) na `.text-rotator-first`/`.text-rotator`/`.start-icon` żywej archon.au + **empiryczny test mechanizmu** (dwukrotny odczyt `transform` w odstępie 10s bez interakcji, potem porównanie przed/po realnym scrollu w obu kierunkach) | Użytkownik poprosił o odwzorowanie 1:1, zaznaczając przypuszczenie, że kropki między frazami są rdzawe. **Zmierzone, nie zgadnięte — dwa realne zaskoczenia**: (1) **kolor kropki to nie rdza** — `.start-icon{background-color:var(--green-yellow-2)}`, a `--green-yellow-2` (mimo mylącej nazwy, pozostałość po wcześniejszej wersji projektu archon.au) resolves do `#FFC8AA`, jasny łosoś/brzoskwinia; matematycznie wykluczono też, że to rdza z przezroczystością (ujemny wymagany współczynnik alfa). Dodano nowy token `--a-peach:#FFC8AA` w `:root` (jedyne miejsce użycia). (2) **To nie jest samoczynnie zapętlona animacja czasowa** — dwukrotny odczyt inline `transform` elementów `.text-rotator-content` w 10-sekundowym odstępie BEZ scrollowania dał **zero zmiany pozycji**; dopiero realny scroll (kółko myszy, nie `scrollIntoView`) przesuwał tekst, w obu kierunkach (scroll w dół → tekst w prawo, w górę → w lewo, potwierdzone dwoma niezależnymi próbami ze spójnym współczynnikiem ~0,26px przesunięcia na 1px scrolla). Przedstawiono to użytkownikowi wprost (dwie opcje: wierny scroll-linked mechanizm vs prostsza samoczynna pętla) — **wybrano wierne odwzorowanie**. **Dodatkowe zmierzone wartości**: padding rzędu `20px 0` (było `16px`), typografia `.text-rotator` to `font-size` **względny do viewportu** z 3 realnymi breakpointami źródłowymi (nie zgadnięte) — `≥1440px:0.7vw`, `992–1439px:1vw` (baza), `≤991px:2vw` (większy niż baza — ten sam „odwrócony” quirk co `.h-quote` z Rundy 39, potwierdzony jako celowy wzorzec archon.au, nie przypadek), `≤479px:3vw`; kolor tekstu `rgb(122,119,114)` = dokładnie nasze istniejące `--a-gray` (bez zmian). **Wdrożenie**: HTML — każda fraza owinięta w `<span class="marquee-phrase">` + osobny `<span class="marquee-dot"></span>` po niej (10 fraz + 10 kropek, lista zdublowana dokładnie ×2 jak wcześniej, żeby zapętlenie było matematycznie proste: `halfWidth = scrollWidth/2`). CSS — nowe klasy `.marquee-phrase` (responsywny `font-size` w 4 regułach) i `.marquee-dot`, usunięto `@keyframes marquee` i jego użycie (nieużywany kod, DRY). JS (`js/main.js`, wzorowane na już istniejącym, analogicznym mechanizmie paralaksy rzędu zabiegów — `requestAnimationFrame`-batched, `scroll`+`resize` listenery) — nowy blok „benefits marquee”: śledzi deltę `window.scrollY` między klatkami, mnoży przez zmierzony współczynnik `0,26`, dodaje do narastającego przesunięcia zawijanego modulo (`((x % half)+half)%half`, żeby zawsze mieściło się w pętli niezależnie od kierunku i długości scrolla), stosuje jako `translateX` na `#marqueeTrack`. Respektuje `prefers-reduced-motion` (nie inicjalizuje się, jeśli włączone — spójne z resztą pliku). **Zweryfikowane**: świeże przeładowanie → `transform:none` (statyczny spoczynek, zgodnie z realnym zachowaniem); `scrollTo(0,500)` → `translateX(-928px)`, co po weryfikacji ręcznej okazało się matematycznie poprawnym zawiniętym odpowiednikiem `-130px` (`500×0,26=130`, `-130 mod 1058 = 928` — nie błąd, tylko normalizacja do zakresu `[0,halfWidth)`); powrót `scrollTo(0,0)` → `translateX(0px)` dokładnie, potwierdzając brak driftu w pętli tam i z powrotem. Kolor/rozmiar/margines kropki i `font-size` frazy zweryfikowane `getComputedStyle` na naszej stronie — identyczne z pomiarem na archon.au. Cache-bust CSS `?v=22`→`?v=23`, JS `?v=8`→`?v=9`. Zrzut ekranu w panelu Browser MCP zadziałał tym razem (widok bez przewinięcia, potem `scrollIntoView` — brak znanej usterki pustego renderu); dodatkowa wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090`). |
| 2026-08-28 | Czterdziesta druga runda — naprawiono realny błąd: pasek korzyści „kończył się”/pokazywał pustą przestrzeń przy przewijaniu na szerszych ekranach | Bezpośredni pomiar `getBoundingClientRect` toru vs sekcji na naszej stronie, przy 420/990/1920px i po realnym scrollu (nie `scrollTo()` — patrz przeszkoda niżej), plus przeliczenie matematyczne przyczyny | Użytkownik zgłosił, że podczas przewijania pasek widocznie się urywa. **Przyczyna, potwierdzona liczbowo**: Runda 41 duplikowała listę 5 fraz dokładnie ×2 w samym HTML (`marqueeHalfWidth` = jeden taki zestaw = 1058px przy 990px okna), zakładając, że to zawsze wystarczy do zawinięcia pętli. Nie wystarczało — kontener (`.marquee`, pełna szerokość sekcji, bez paddingu) bywa szerszy niż jeden zestaw (np. 1440–1920px > 1058px), więc przy pozycjach przewijania bliskich granicy zawinięcia widoczne okno (`[offset, offset+szerokość_sekcji]`) wychodziło poza koniec toru (`2×1058=2116px`) — realna, policzalna luka, nie efekt wizualny. **Naprawa**: HTML zredukowany do **jednego** kafelka (5 fraz+5 kropek); `js/main.js` przy starcie i przy każdym `resize` dynamicznie doklania kopie kafelka (`cloneNode`), dopóki `track.scrollWidth < szerokość_sekcji + szerokość_kafelka` nie przestanie być prawdą — matematyczna gwarancja, że dla każdego zawiniętego przesunięcia w zakresie `[0, szerokość_kafelka)` tor ma fizycznie wystarczająco treści, żeby pokryć całe widoczne okno. Funkcja `recalcMarquee` (podpięta pod `resize`) najpierw ścina tor z powrotem do jednego oryginalnego kafelka, przelicza jego szerokość na nowo (zmienia się z oknem, bo `font-size` jest `vw`-owy — patrz Runda 41), dopiero potem doklania kopie na nowo — inaczej stara liczba klonów zostałaby błędnie założona przy nowej szerokości ekranu. **Napotkana przeszkoda przy weryfikacji**: `window.scrollTo()` wywoływane z poziomu `javascript_exec` w tym narzędziu **nie wywoływało** zdarzenia `scroll` w sposób niezawodny (potwierdzone: własny, niezależny listener testowy też nic nie logował) — dopiero realny scroll (`computer` action `scroll`, symulacja kółka myszy) działał poprawnie i konsekwentnie. To nowy, nieudokumentowany wcześniej przypadek tego samego wzorca co pkt 1/11 w CLAUDE.md („syntetyczne eventy nie zawsze uruchamiają prawdziwą logikę") — dodane do wiedzy sesji, nie tylko dla scroll-reveal/klików, ale też dla natywnych zdarzeń `scroll` wywoływanych programowo. **Zweryfikowane** (realnym scrollem, po tej korekcie metody): pełne pokrycie sekcji (`trackLeft<=sectionLeft`, `trackRight>=sectionRight`) potwierdzone przy 420px (2 kafelki), 990px (2 kafelki) i 1920px (3 kafelki, bo tam jeden kafelek jest węższy względem szerszego okna), oraz po >4000px łącznego przewinięcia (kilka pełnych okrążeń pętli, bez żadnej przerwy w treści); po `resize` (420px) liczba dzieci poprawnie przeliczona, pokrycie nadal pełne. Cache-bust JS `?v=9`→`?v=10` (CSS bez zmian w tej rundzie). Wizualna kontrola w zewnętrznej przeglądarce (`open http://localhost:8090`). |
| 2026-08-28 | Czterdziesta trzecia runda — podpięto zdjęcie karty „Laser Tulowy Erbowo-Szklany" (wygenerowane AI, na bazie promptu ustalonego wcześniej w rozmowie) | Użytkownik wkleił plik do `assets/images/` | Ten sam schemat co Runda 40 (banner rezerwacji): plik `Gemini_Generated_Image_...jpeg` (2,9MB, 2048×2048) zoptymalizowany `sips -Z 1000 --setProperty formatOptions 82` do **157KB, 1000×1000** (dopasowane do rozmiaru pozostałych 11 realnych zdjęć zabiegów w `assets/images/treatments/`), zapisany jako `laser-tulowy-erbowo-szklany.jpg`, oryginał usunięty po przetworzeniu. W `index.html` karta zmieniona z `<div class="trick-img ph-gradient" role="img" aria-label="TODO...">` na `<img class="trick-img" src="..." alt="...">` — ta sama struktura co pozostałe realne zdjęcia, więc automatycznie **dziedziczy poziomą paralaksę** z `js/main.js` (selektor `img.trick-img`, bez zmian w JS potrzebnych). Alt-text opisuje treść zdjęcia (plecy/bark, odnowa skóry), nie samą nazwę zabiegu. **Zweryfikowane**: `fetch()` obrazu z lokalnego serwera zwraca `200`/`image/jpeg`/`156594` bajtów. Zaktualizowano „Listę zabiegów" (2/24 → ✅) i „Otwarte TODO" w PLAN.md (13→12 kart bez zdjęcia) — zgodnie z zasadą z Rundy 27 o jednym źródle prawdy. Brak zmian CSS/JS, bez cache-bustu. |
| 2026-08-28 | Czterdziesta czwarta runda — podpięto zdjęcie karty „LPG® Ergolift" (wygenerowane AI) + skorygowano założenie o rekwizycie botanicznym w promptach | Bezpośrednie porównanie 3 innych realnych zdjęć zabiegów z archon.au (`tattoo-removal.jpg`, `fractional-rf.jpg`, `mens-waxing.jpg`, już pobrane w `assets/images/treatments/`) | Przed wygenerowaniem tego zdjęcia użytkownik zapytał, czy w każdym promptcie musi być gałązka eukaliptusa (obecna w promptach dla Kontaktu i Lasera Tułowia). **Sprawdzono zamiast zakładać**: gałązka pojawiła się tylko w JEDNYM realnym zdjęciu (`laser-genesis-facial.jpg`), które posłużyło jako pierwszy, jedyny wzorzec — błędnie uogólnione na regułę sitewide. 3 kolejne sprawdzone zdjęcia (tors z tatuażem na szarym tle, profil twarzy na tle prawdziwych liści w scenie, plecy na czystym tle) **żadne nie mają** trzymanego rekwizytu botanicznego — to opcja, nie wymóg; wspólny mianownik to ciepła kolorystyka/światło/ziarnistość, nie konkretny prop. Skorygowano wytyczne dla przyszłych promptów. Użytkownik dodał też trwałą wytyczną: modelka/model na kolejnych zdjęciach ma mieć rysy/karnację typowe dla mieszkańców Polski (zasadne, bo to realny salon w Płocku, nie australijska klinika) — do stosowania w kolejnych promptach. **Wdrożenie zdjęcia**: ten sam schemat co Rundy 40/43 — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **178KB, 1000×1000**, zapisany jako `lpg-ergolift.jpg`, oryginał usunięty. Karta zmieniona z `ph-gradient` placeholder na `<img>` (automatyczne dziedziczenie poziomej paralaksy, jak w Rundzie 43). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`182170` bajtów. Zaktualizowano „Listę zabiegów" (4/24 → ✅) i „Otwarte TODO" w PLAN.md (12→11 kart bez zdjęcia). Brak zmian CSS/JS, bez cache-bustu. |
| 2026-08-28 | Czterdziesta piąta runda — podmieniono zdjęcie karty „Epilacja laserowa LightSheer®" ze stockowego CDN archon.au na wygenerowane AI | Użytkownik wkleił plik do `assets/images/` | Pierwsza podmiana w ramach otwartego punktu z Rundy 7: 11 zdjęć zabiegów pobranych z CDN archon.au (stockowe zdjęcia klientów Archon, Australia) miało zostać zastąpione realnymi fotografiami instytutem.pl przed publikacją — dotąd żadne nie było podmienione. Ten sam schemat co Rundy 40/43/44: `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **142KB, 1000×1000**, zapisany jako `epilacja-laserowa-lightsheer.jpg`. Stary plik `mens-laser-hair-removal.jpg` (CDN archon.au) **usunięty** z `assets/images/treatments/` — nieużywany nigdzie indziej (sprawdzone `grep` po `index.html` przed usunięciem). Karta zaktualizowana (`src`+`alt`, ta sama struktura `<img class="trick-img">`, dziedziczy paralaksę bez zmian w JS). **Zweryfikowane**: `fetch()` nowego pliku zwraca `200`/`image/jpeg`/`141702` bajtów. Zaktualizowano „Listę zabiegów" (teraz jawnie odróżnia „AI, Runda X" od „CDN archon.au") oraz „Otwarte TODO" w PLAN.md — rozbite na podpunkty per plik, żeby śledzić postęp podmiany pozostałych 10 zdjęć CDN jeden po drugim. Przy okazji odnotowano w TODO, że `womens-laser-hair-removal.jpg` jest myląco użyty przy karcie „Endermologia LPG® Alliance", nie przy depilacji — do uwzględnienia przy kolejnej podmianie tego pliku. Brak zmian CSS/JS, bez cache-bustu. |
| 2026-08-28 | Czterdziesta szósta runda — podmieniono zdjęcie karty „Endermologia LPG® Alliance" + wprowadzono rotację tła w promptach | Prośba użytkownika (tło zbyt monotonne przy kilku zdjęciach pod rząd) + użytkownik wkleił gotowy plik | Użytkownik zauważył, że 3 kolejne wygenerowane zdjęcia (Laser Tułowy, Ergolift, Epilacja laserowa) miały to samo ciepłe taupe-brown tło — zbyt monotonnie obok siebie w rzędzie kart. Ustalono w czacie rotację tła opartą na już istniejącej palecie marki z DESIGN-SYSTEM.md §2 (taupe/głęboka oliwka/ciepły szary/węglowy brąz/przydymiona terakota — nawiązania do `--a-taupe`/`--a-olive`/`--a-gray`/`--a-ink-warm`/`--a-rust`), reszta promptu (światło, ziarnistość, matowa skóra) bez zmian — nie zapisane jako formalna zasada w DESIGN-SYSTEM.md (to wytyczna dla promptów AI, nie CSS), tylko w PLAN.md jako przypomnienie na kolejne rundy. Ten prompt użył głębokiej oliwki. **Wdrożenie zdjęcia**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **105KB, 1000×1000**, zapisany jako `endermologia-lpg-alliance.jpg`. Stary plik **usunięty**: `womens-laser-hair-removal.jpg` (CDN archon.au) — dotąd mylnie podpięty pod tę kartę (nazwa pliku sugerowała depilację laserową, nie modelowanie sylwetki), sprawdzone `grep`, że nieużywany nigdzie indziej przed usunięciem. **Zweryfikowane**: `fetch()` nowego pliku zwraca `200`/`image/jpeg`/`107488` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (9 z 11 zdjęć CDN archon.au pozostaje do podmiany). Brak zmian CSS/JS, bez cache-bustu. |
| 2026-08-28 | Czterdziesta siódma runda — podmieniono zdjęcie karty „Kriolipoliza cooltech®" + ustalono zasadę treściową dla zabiegów redukujących/modelujących | Dyskusja z użytkownikiem w czacie, zweryfikowana bezpośrednim sprawdzeniem alt-tekstów realnych zdjęć na archon.au (nie zgadywaniem) | Użytkownik zapytał, czy zdjęcia zabiegów redukujących powinny pokazywać osoby z realnym „problemem", który zabieg rozwiązuje (np. wałeczki przy kriolipolizie), a nie tylko już-idealny efekt. **Początkowo odradzono** (ryzyko klinicznego/wstydliwego tonu, sprzecznego z DESIGN-SYSTEM.md §1/§9) — **ale użytkownik przypomniał sobie konkretne zdjęcie z archon.au** pokazujące wałeczki w artystycznym stylu, co skłoniło do bezpośredniej weryfikacji zamiast trzymania się pierwszego wrażenia. **Sprawdzono `alt` wszystkich zdjęć zabiegów na żywej archon.au** — potwierdzone: `mens-brazilian-waxing.jpg` ma alt „diverse men... different levels of body hair", `womens-laser-hair-removal.jpg` ma alt „proud of their bodies" (grupowe zdjęcie w bieliźnie, realne sylwetki) — czyli archon.au **faktycznie pokazuje prawdziwe, zróżnicowane ciała z cechą, którą zabieg adresuje**, nie tylko wyidealizowane rezultaty, ale zawsze w pewnym siebie, niewstydliwym ujęciu (nie klinicznie/„przed-po"). Skorygowano wcześniejsze stanowisko. **Iteracje promptu dla tej konkretnej karty** (3 podejścia, każde z realną korektą): (1) pierwsza wersja Gemini wygenerowała „wyciętego"/already-lean mężczyznę — mijało się z sensem zabiegu (kriolipoliza redukuje tkankę tłuszczową, więc pokazywanie już płaskiego brzucha nie ma logiki); (2) druga wersja (z realnymi wałeczkami) miała płaskie, dokumentalne/kliniczne światło i zwykłe czarne spodnie w kadrze — odrzucona jako zbyt „medyczna", nie edytorialna; (3) poprawiony prompt (dramatyczne boczne światło, bez zwykłej odzieży) przypadkiem wygenerował nagość — model zinterpretował `no clothing visible in frame` dosłownie; naprawione jawnym dodaniem bielizny (`plain neutral-toned fitted underwear`, na wyraźną prośbę użytkownika „bielizna nie ręcznik") + klauzulą `fully appropriate for a professional business website`. Czwarta wersja zaakceptowana. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **175KB, 1000×1000**, zapisany jako `kriolipoliza-cooltech.jpg`, stary plik `mens-waxing.jpg` (CDN archon.au) usunięty (sprawdzone `grep`, nieużywany gdzie indziej). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`178798` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md, w tym nową, trwałą zasadę treściową dla przyszłych zabiegów typu redukcja/modelowanie (karboksyterapia, lipoliza iniekcyjna itd.) — realna cecha, którą zabieg adresuje, nie już-idealny efekt, w edytorialnym, niewstydliwym tonie. Brak zmian CSS/JS, bez cache-bustu. |
| 2026-08-28 | Czterdziesta ósma runda — skonsolidowano przewodnik po promptach AI w DESIGN-SYSTEM.md + przygotowano nowy prompt dla Epilacji laserowej | Prośba użytkownika o zapisanie ustaleń z Rund 40–47 w dokumentacji zamiast tylko w czacie | Wszystkie ustalenia rozproszone dotąd po rozmowie (rdzeń stylu, zasada modela polskiego/środkowoeuropejskiego, opcjonalność rekwizytów botanicznych, rotacja tła oparta o paletę marki, zasada „realna cecha nie idealny efekt" dla zabiegów redukujących) spisane w nowej sekcji DESIGN-SYSTEM.md §9 „Zdjęcia zabiegów generowane AI" — jedno miejsce, źródło prawdy na przyszłość, zamiast każdorazowego odtwarzania z historii czatu. **Nowy prompt dla „Epilacja laserowa LightSheer®"** przygotowany zgodnie z pełnym zestawem zasad: widoczne, naturalne owłosienie (nie gładka skóra — analogicznie do zasady „realna cecha" z Rundy 47, tu pierwsze zastosowanie do zabiegu spoza kategorii redukcja/modelowanie), kolejne tło z rotacji (węglowy brąz, `--a-ink-warm`, po taupe/oliwce/szarym). **Nierozstrzygnięte w tej rundzie**: czy nowy prompt ma zastąpić istniejące zdjęcie tej karty (`epilacja-laserowa-lightsheer.jpg`, Runda 45, wygenerowane przed ustaleniem zasady „realna cecha") — użytkownik jeszcze nie zdecydował, zapytany wprost. Brak zmian w kodzie (`index.html`/CSS/JS) w tej rundzie — czysto dokumentacyjna. |
| 2026-08-28 | Czterdziesta dziewiąta runda — skorygowano zasadę „realna cecha" z Rundy 47/48: nie dotyczy epilacji laserowej | Użytkownik wygenerował zdjęcie promptem z Rundy 48 i ocenił rezultat jako nieapetyczny/odpychający (noga z owłosieniem w pozie „na stole") | Zasada „pokazuj realną cechę, nie idealny efekt", ustalona dla zabiegów redukujących (Runda 47) i rozszerzona na epilację w Rundzie 48, **nie sprawdziła się w praktyce dla depilacji** — w przeciwieństwie do kriolipolizy (gdzie naturalne ciało z wałeczkami wyszło dobrze i edytorialnie), widoczne owłosienie na nodze w kampanijnym ujęciu wyszło nieapetycznie, nie „szczerze"/edytorialnie. Użytkownik zdecydował: dla epilacji wracamy do pokazywania **gładkiej skóry** (efekt końcowy), zasada „realna cecha" zostaje ograniczona wyłącznie do kategorii redukcja/modelowanie sylwetki. **Zaktualizowano DESIGN-SYSTEM.md §9** — dodano jawny wyjątek dla epilacji/depilacji z wyjaśnieniem i ogólnym wnioskiem na przyszłość: przy niepewności, czy wytyczna faktycznie zadziała wizualnie, weryfikować na realnym wygenerowanym zdjęciu, nie zakładać uogólnienia z góry (ten sam typ błędu co pomyłka z rekwizytem botanicznym w Rundzie 44 — jedna obserwacja/zasada potraktowana jako uniwersalna bez sprawdzenia na więcej niż jednym przypadku). Przygotowano poprawiony prompt (gładka skóra, `relaxed, natural pose (not overtly posed or suggestive)` — dodatkowe zabezpieczenie przed nienaturalną/prowokacyjną pozą, ten sam problem co „zalotna noga na stole"). Zdjęcie jeszcze nie wygenerowane/wklejone w tej rundzie — czysto korekcyjna, dokumentacyjna. |
| 2026-08-28 | Pięćdziesiąta runda — podmieniono zdjęcie karty „Epilacja laserowa LightSheer®" na wersję z poprawionym promptem (gładka skóra) | Użytkownik wkleił plik po zaakceptowaniu poprawionego promptu z Rundy 49 | Ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **114KB, 1000×1000**, zapisany **pod tą samą nazwą** `epilacja-laserowa-lightsheer.jpg` (nadpisuje wersję z Rundy 45/49) — ponieważ nazwa pliku się nie zmieniła, dodano `?v=2` do `src` w `index.html`, żeby przeglądarki nie serwowały starej wersji z własnego cache obrazków (ten sam problem co cache-bust CSS/JS, tylko na poziomie pojedynczego assetu, nie całego pliku). **Zweryfikowane**: `fetch()` z `?v=2` zwraca `200`/`image/jpeg`/`116639` bajtów — noga bez owłosienia, spokojna, niewymuszona poza (noga oparta o stołek, nie „na stole"), ciemne węglowo-brązowe tło zgodnie z rotacją. Zaktualizowano „Listę zabiegów" w DOKUMENTACJA.md. Brak zmian CSS/JS poza `?v=2` w atrybucie `src`. |
| 2026-08-28 | Pięćdziesiąta pierwsza runda — podmieniono zdjęcie karty „Laser Tulowy Erbowo-Szklany" na wersję z subtelną blizną | Prośba użytkownika (pomysł na „realną cechę" dla zabiegu na teksturę/przebarwienia skóry) | Ten sam schemat co Runda 50 — nowe zdjęcie z małą, subtelną blizną na plecach/barku (zamiast gałązki eukaliptusa z pierwszej wersji, Runda 43), tło terakotowe — ostatnie nieużyte z puli rotacji ustalonej w Rundzie 46, teraz wszystkie 5 kolorów tła użyte przynajmniej raz. `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **126KB, 1000×1000**, zapisany pod tą samą nazwą `laser-tulowy-erbowo-szklany.jpg` (nadpisuje wersję z Rundy 43) — `?v=2` dodane do `src` w `index.html`, ten sam powód co w Rundzie 50 (cache przeglądarki na poziomie pojedynczego obrazka). **Zweryfikowane**: `fetch()` z `?v=2` zwraca `200`/`image/jpeg`/`129182` bajtów. Zaktualizowano „Listę zabiegów". Brak zmian CSS/JS poza `?v=2`. |
| 2026-08-28 | Pięćdziesiąta druga runda — podmieniono zdjęcie karty „Bloomea®", zweryfikowano realny opis zabiegu przed doborem „wady" | Użytkownik zapytał, jaka „wada" pasuje do Bloomea — zamiast zgadywać, sprawdzono `WebSearch` | `/zabiegi/bloomea` na żywej instytutem.pl zwraca 404 (to własny, wymyślony URL z Rundy 9, nie realna strona) — nie dało się zweryfikować z pierwszej ręki, więc sprawdzono opis zabiegu w wyszukiwarce (kilka niezależnych polskich salonów opisujących tę samą markę zabiegu). **Ustalone**: Bloomea® to zabieg liftingująco-odmładzający, technologia TRIO M.M.L. (makroeksfoliacja + mikroprądy + luminoterapia 633nm), efekty: ujędrnienie, redukcja blizn/rozstępów/przebarwień, głównie lifting/odmłodzenie. Na tej podstawie dobrano „realną cechę" pasującą do kadru twarzy/szyi: **subtelne, naturalne zmarszczki** (dojrzała, ale niezretuszowana skóra) — nie retuszowana idealnie gładka twarz. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **198KB, 1000×1000**, zapisany jako `bloomea.jpg`. Stary plik `led-light-therapy.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`197755` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (7 z 11 zdjęć CDN archon.au pozostaje). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta trzecia runda — podmieniono zdjęcie karty „Oczyszczanie wodorowe" | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/oczyszczanie-wodorowe-plock` (realny, zaindeksowany URL — w przeciwieństwie do Bloomea, nie zwrócił 404) | Sprawdzono treść realnej strony zamiast zgadywać: zabieg adresuje zaskórniki, rozszerzone pory, trądzik, szarą/zmęczoną cerę, nierównomierny koloryt. Na tej podstawie dobrano „realną cechę" wyważoną między nudną idealizacją a odpychającym „przed" (ten sam kompromis co przy epilacji): **widoczna tekstura skóry i pory w makro zbliżeniu**, nie zaskórniki/przetłuszczenie wprost — subtelniej niż dosłowne pokazanie problemu, ale bez sztucznego wygładzenia. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **135KB, 1000×1000**, zapisany jako `oczyszczanie-wodorowe.jpg`. Stary plik `laser-genesis-facial.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`138073` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (6 z 11 zdjęć CDN archon.au pozostaje). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta czwarta runda — dodano dwie zasady kadru do przewodnika promptów + przygotowano prompt dla „Fototerapia LED Dermalux®" | Bezpośredni odczyt żywej podstrony `instytutem.pl/fototerapia-dermalux-plock` (realny URL) + prośba użytkownika o zasadę kadru | Sprawdzono treść realnej strony: Dermalux® Flex MD (fototerapia LED, technologia Tri-Wave) leczy trądzik/stany zapalne, redukuje zaczerwienienia/rumień/„pajączki", spłyca zmarszczki. Wybrano jako „realną cechę" delikatne zaczerwienienie/rumień na policzku — stonowane, nie trądzik (zbyt ryzykowne wizualnie, analogicznie do wcześniejszej decyzji o unikaniu zbyt dosłownych „wad"). **Dwie nowe zasady kadru dodane do DESIGN-SYSTEM.md §9** na prośbę użytkownika: (1) preferować makro zbliżenie na konkretny obszar/cechę zamiast całej twarzy/sylwetki — czytelniej pokazuje, o co chodzi w zabiegu; (2) ale nie za blisko — zbliżenie musi zostawić widoczny punkt odniesienia (kawałek oka, ucha, linii szczęki itd.), żeby jednoznacznie było wiadomo, jaka to część ciała i czyja. Prompt dla Dermalux zbudowany od razu zgodnie z obiema zasadami (kącik oka + linia nosa jako kotwica w kadrze). Karta „Fototerapia LED Dermalux®" to obecnie placeholder (`.ph-gradient`), nie jeden z 11 zdjęć CDN archon.au — to nowe zdjęcie, nie podmiana. Zdjęcie jeszcze nie wygenerowane/wklejone w tej rundzie — czysto przygotowawcza, dokumentacyjna. |
| 2026-08-28 | Pięćdziesiąta piąta runda — podpięto zdjęcie karty „Fototerapia LED Dermalux®" | Użytkownik wkleił plik po zaakceptowaniu promptu z Rundy 54 („wyszło fajnie") | Ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **190KB, 1000×1000**, zapisany jako `fototerapia-dermalux.jpg`. Karta zmieniona z `.ph-gradient` placeholder na `<img>` (dziedziczy poziomą paralaksę automatycznie, jak poprzednie podmiany). Kompozycja zgodna z nowymi zasadami kadru z Rundy 54: profil twarzy z wyraźnymi kotwicami (oko, nos, usta) i widocznym zaczerwienieniem/naczynkami na policzku. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`194257` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (10 z 24 kart w rzędzie zabiegów nadal bez zdjęcia, nie licząc 6 pozostałych zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta szósta runda — podpięto zdjęcie karty „Plasma™" | Prośba użytkownika (usuwanie włókniaków, sugestia okolic szyi) + `WebSearch` do weryfikacji, że plasma faktycznie usuwa włókniaki | Sprawdzono zamiast zgadywać: zabiegi Plasma/Plasma IQ usuwają drobne zmiany skórne — włókniaki, brodawki, kaszaki, najczęściej na szyi/powiekach. Sugestia użytkownika (szyja z widocznymi włókniakami) trafna i zgodna z realnym zastosowaniem zabiegu — bezpieczna wizualnie (małe, nieliczne włókniaki, nie coś odpychającego). **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **140KB, 1000×1000**, zapisany jako `plasma.jpg`. Karta zmieniona z `.ph-gradient warm` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: szyja z widocznymi włókniakami, ucho/usta/żuchwa jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`143120` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (9 z 24 kart bez zdjęcia + 6 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta siódma runda — podmieniono zdjęcie karty „Radiofrekwencja mikroigłowa RF" | Bezpośredni odczyt żywej podstrony `instytutem.pl/radiofrekwencja-mikroiglowa-plock` (realny URL) | Sprawdzono treść realnej strony: RF mikroigłowa adresuje zmarszczki, blizny, rozstępy, ale też konkretnie cienie pod oczami i opadające powieki. Wybrano jako „realną cechę" (różną od Bloomea, żeby nie dublować tego samego motywu „dojrzała skóra") drobne linie/„kurze łapki" i lekkie cienie pod oczami, w makro zbliżeniu na okolice oka. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **257KB, 1000×1000**, zapisany jako `radiofrekwencja-mikroiglowa.jpg`. Stary plik `fractional-rf.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). Kompozycja zgodna z zasadami kadru z Rundy 54: oba oczy widoczne w kadrze jako czytelny kontekst. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`263563` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (5 z 11 zdjęć CDN archon.au pozostaje). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta ósma runda — podmieniono zdjęcie karty „Mikronakłuwanie SkinPen®" | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/dermapen-plock-mikronakluwanie` (realny URL) | Sprawdzono treść realnej strony: SkinPen adresuje zmarszczki, wiotką/cienką skórę, widoczne pory, rozstępy, blizny, łysienie. Żeby dać wizualną różnorodność względem już użytych obszarów (twarz/oczy/policzek/szyja), wybrano **dekolt z naturalnie cienką skórą i widocznymi drobnymi liniami** — nowy obszar ciała w galerii. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **175KB, 1000×1000**, zapisany jako `mikronaklowanie-skinpen.jpg`. Stary plik `microneedling.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). Kompozycja zgodna z zasadami kadru z Rundy 54: obojczyk i ramiączko topu jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`179227` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (4 z 11 zdjęć CDN archon.au pozostaje). Brak zmian CSS/JS. |
| 2026-08-28 | Pięćdziesiąta dziewiąta runda — podpięto zdjęcie karty „Autologiczne wypełniacze ATR" + korekta doboru obszaru ciała | `WebSearch` do weryfikacji, co to ATR (`/zabiegi/autologiczne-wypelniacze-atr` na żywej instytutem.pl zwraca 404 — to wymyślony URL z Rundy 9, jak Bloomea) + korekta użytkownika w czacie | Zweryfikowano: ATR (Autologous Tissue Refiller) to naturalny wypełniacz z osocza własnej krwi pacjenta, wstrzykiwany jak kwas hialuronowy — źródła wymieniały jako obszary zastosowania twarz, szyję, dekolt i dłonie. Pierwsza propozycja (zbliżenie na dłoń) była poprawna względem źródeł, ale **użytkownik skorygował**: głównym obszarem ATR jest twarz/szyja/dekolt, nie dłonie — zastosowano poprawkę bez sprzeczki, bo to trafna korekta priorytetu, nie błąd faktograficzny (dłonie faktycznie bywają leczone, ale rzadziej/drugorzędnie). Wybrano **bruzdę nosowo-wargową** (okolice ust) — klasyczny obszar wypełniaczy, dotąd niepokazany w galerii (mieliśmy już policzek/oko/szyję/dekolt, nie usta). **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **163KB, 1000×1000**, zapisany jako `autologiczne-wypelniacze-atr.jpg`. Karta zmieniona z `.ph-gradient` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: ucho/nos/usta jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`166425` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (8 z 24 kart bez zdjęcia + 4 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta runda — podpięto zdjęcie karty „Kwas polimlekowy PLLA" | `WebSearch` do weryfikacji zastosowania (`/zabiegi/kwas-polimlekowy-plla` na żywej instytutem.pl zwraca 404 — wymyślony URL z Rundy 9) | Zweryfikowano: kwas polimlekowy (PLLA/Sculptra) to biostymulator kolagenowy adresujący utratę objętości twarzy — zapadnięte skronie, zapadnięte policzki, głębokie bruzdy nosowo-wargowe (tę ostatnią już pokazano przy ATR w poprzedniej rundzie). Wybrano **zapadnięty policzek w profilu** jako odróżniający się od ATR obszar. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **206KB, 1000×1000**, zapisany jako `kwas-polimlekowy-plla.jpg`. Karta zmieniona z `.ph-gradient warm` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: ucho/oko/nos jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`210571` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (7 z 24 kart bez zdjęcia + 4 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta pierwsza runda — podpięto zdjęcie karty „Mezoterapia igłowa", pierwszy raz obszar skóry głowy/włosów | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/mezoterapia-iglowa-plock` (realny URL) | Użytkownik zapytał, czy to głównie „odżywienie skóry" — potwierdzone jako cel terapii, ale zakres okazał się szerszy: zabieg **twarzy i skóry głowy**, adresuje też wypadanie włosów/łysienie plackowate i androgenowe, cellulit, blizny/rozstępy, szarą cerę. Za zgodą użytkownika wybrano **skórę głowy/przedziałek z przerzedzeniem** — pierwszy raz w galerii pokazany ten obszar (dotąd same zbliżenia na twarz/szyję/dekolt/dłoń), dobra wizualna różnorodność. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **251KB, 1000×1000**, zapisany jako `mezoterapia-iglowa.jpg`. Karta zmieniona z `.ph-gradient` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: czoło i widoczny przedziałek jako kotwice kontekstowe (jednoznacznie czytelne jako skóra głowy, nie przypadkowa tekstura). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`256797` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (6 z 24 kart bez zdjęcia + 4 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta druga runda — podmieniono zdjęcie karty „Kwas hialuronowy" | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/wypelnianie-kwasem-hialuronowym-plock` (realny URL, tytuł „Wypełnianie ust i bruzd") | Użytkownik trafnie skojarzył kwas hialuronowy z ustami — potwierdzone treścią strony: usta to główne, pierwsze wymienione zastosowanie (obok bruzd, zmarszczek czoła, linii łez, „marionetek"). Wybrano naturalnie cieńsze/mniej pełne usta z subtelnymi drobnymi liniami — utrata objętości to dokładnie to, co ten zabieg wypełnia. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **178KB, 1000×1000**, zapisany jako `kwas-hialuronowy.jpg`. Stary plik `lip-blush-tattoo.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). Kompozycja zgodna z zasadami kadru z Rundy 54: nos i broda jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`182067` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (3 z 11 zdjęć CDN archon.au pozostaje — coraz bliżej pełnej podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta trzecia runda — podpięto zdjęcie karty „Nici liftingujące PDO/PLLA" | `WebSearch` do weryfikacji zastosowania (URL z listy SEO, ale live-fetch dał 404 — strona najwyraźniej usunięta z żywej instytutem.pl mimo obecności w historycznym menu) | Zweryfikowano: nici PDO/PLLA napinają i unoszą opadające tkanki, głównie owal twarzy/żuchwę; PDO daje szybszy efekt mechaniczny, PLLA silniej biostymuluje kolagen. Wybrano lekko mniej wyraźną/opadającą linię żuchwy w profilu — inny akcent niż zapadnięty policzek (PLLA kwas) czy bruzda nosowo-wargowa (ATR). **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **195KB, 1000×1000**, zapisany jako `nici-liftingujace-pdo-plla.jpg`. Karta zmieniona z `.ph-gradient warm` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: ucho/oko/nos jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`200012` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (5 z 24 kart bez zdjęcia + 3 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta czwarta runda — podpięto zdjęcie karty „Fibryna i osocze bogatopłytkowe" + rozstrzygnięto podejrzenie duplikatu z ATR | Użytkownik zapytał, czy to nie duplikat Autologicznych wypełniaczy ATR (Runda 59) — sprawdzono `WebSearch` + żywą podstronę `instytutem.pl/fibryna-i-osocze-bogatoplytkowe` (realny URL) | **Rozstrzygnięcie**: pokrewne, ale różne preparaty krwiopochodne — ATR to termicznie przetworzony, gęsty żel wypełniający (jak kwas hialuronowy, ~10 mies. trwałości), PRP/PRF to mniej przetworzony koncentrat płytek krwi do regeneracji, **jawnie reklamowany jako „bez użycia wypełniaczy”** (cytat wprost ze strony) — różny mechanizm i przekaz, nie duplikat. Cel terapii: gęstość/jędrność/blask skóry twarzy, szyi, dekoltu, okolic oczu. **Decyzja stylistyczna**: skoro to nie zabieg korygujący konkretną wadę (jak ATR czy PLLA), tylko regeneracyjny/rozświetlający, zrezygnowano z zasady „realna cecha" na rzecz idealizowanego ujęcia (analogicznie do Bloomea w Rundzie 52) — spokojna, świetlista twarz z zamkniętymi oczami. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **149KB, 1000×1000**, zapisany jako `fibryna-osocze-bogatoplytkowe.jpg`. Karta zmieniona z `.ph-gradient` placeholder na `<img>`. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`152144` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (4 z 24 kart bez zdjęcia + 3 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta piąta runda — podpięto zdjęcie karty „Stymulatory tkankowe" | `WebSearch` do weryfikacji zastosowania (URL wymyślony w Rundzie 9, live-fetch dał 404) | Zweryfikowano: stymulatory tkankowe (np. Radiesse, hydroksyapatyt wapnia) dają lifting i odbudowę struktury skóry, stosowane na twarzy i ciele. Pierwsza propozycja (dłonie — klasyczny obszar dla Radiesse) **odrzucona przez użytkownika po raz drugi** (podobnie jak przy ATR) — konsekwentna preferencja: twarz/szyja/dekolt jako priorytet dla tej kategorii zabiegów, nie dłonie. Wybrano szyję z poziomymi liniami/wiotkością skóry („tech neck") — inny akcent niż wcześniejsza szyja z włókniakami (Plasma, Runda 56), tu chodzi o utratę jędrności/kontur, nie zmiany skórne. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **197KB, 1000×1000**, zapisany jako `stymulatory-tkankowe.jpg`. Karta zmieniona z `.ph-gradient warm` placeholder na `<img>`. Kompozycja zgodna z zasadami kadru z Rundy 54: żuchwa/ucho/usta jako kotwice kontekstowe. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`201834` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (3 z 24 kart bez zdjęcia + 3 z 11 zdjęć CDN archon.au do podmiany). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta szósta runda — podmieniono zdjęcie karty „Lipoliza Iniekcyjna" | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/lipoliza-iniekcyjna-plock` (realny URL, tytuł „twarzy, brzucha, boczków") | Potwierdzone tytułem strony — trafna intuicja użytkownika (wałeczki/boczki). Kriolipoliza (Runda 47) już pokazała wałeczki u mężczyzny, więc dla równowagi w galerii wybrano kobietę i inny obszar (biodro/boczek zamiast talii) — świadoma różnorodność płci i kadru między dwoma zabiegami tej samej kategorii. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **165KB, 1000×1000**, zapisany jako `lipoliza-iniekcyjna.jpg`. Stary plik `womens-waxing.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`168867` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (**tylko 2 z 11** zdjęć CDN archon.au pozostaje: `tattoo-removal.jpg`, `tier-2-facials.jpg`). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta siódma runda — podmieniono zdjęcie karty „Karboksyterapia" | Bezpośredni odczyt żywej podstrony `instytutem.pl/zabiegi/karboksyterapia-plock` (realny URL, treść „Redukcja Cellulitu i Rozstępów") | Potwierdzone: karboksyterapia adresuje cellulit i rozstępy (też wypadanie włosów, cienie pod oczami — pominięte, poza zakresem tej rundy). Zamiast standardowego brzucha/uda z rozstępami, na sugestię użytkownika wybrano **biceps mężczyzny z rozstępami po przyroście mięśni** — świeższy motyw niż typowe „wagowe" skojarzenie, zgodny z inkluzywnym tonem marki (rozstępy dotyczą też mężczyzn, nie tylko ciąży/wagi) i jeszcze niepokazana część ciała w galerii. **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **130KB, 1000×1000**, zapisany jako `karboksyterapia.jpg`. Stary plik `tattoo-removal.jpg` (CDN archon.au) **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`+`alt`). **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`133514` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md — **ostatnie zdjęcie CDN archon.au do podmiany to `tier-2-facials.jpg`**, po nim cała galeria będzie wolna od treści z Australii. Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta ósma runda — zgłoszono podejrzenie duplikatu „Mezoterapia mikroigłowa"/„Mikronakłuwanie SkinPen®" (wstrzymane do potwierdzenia przez użytkownika) + podpięto zdjęcie karty „Mezoterapia bezigłowa" | Użytkownik zapytał wprost, czy mezoterapia mikroigłowa to to samo co SkinPen — sprawdzone `WebSearch` zamiast zgadywać | **Ustalenie**: „mezoterapia mikroigłowa" to nazwa techniki (mikronakłucia + wprowadzanie składników), a SkinPen/Dermapen to konkretna marka urządzenia do jej wykonania — czyli SkinPen może być tym samym zabiegiem pod inną nazwą, analogicznie do potwierdzonego wcześniej duplikatu „LPG® Ergolift"/„Endermologia Twarzy" (Runda 13). Użytkownik nie wie z pamięci i musi dopytać w salonie — zgodnie z ustaloną zasadą „pytać, nie zgadywać przy dodawaniu treści" (`feedback_ask_before_adding_content.md`), **karta „Mezoterapia mikroigłowa" celowo NIE dostała zdjęcia w tej rundzie** — zostaje placeholder do czasu odpowiedzi, zapisane jako osobny punkt w „Otwarte TODO" PLAN.md. Użytkownik zdecydował przejść do kolejnego zabiegu zamiast czekać. **Mezoterapia bezigłowa (elektroporacja)** — zweryfikowano `WebSearch`: bezinwazyjna alternatywa dla mezoterapii igłowej (impulsy elektryczne zamiast igieł), adresuje blizny, trądzik, zmarszczki, cellulit, odwodnienie. Wybrano cellulit na udzie — dotąd niepokazana wprost tekstura (mieliśmy wałeczki/rozstępy, nie „skórkę pomarańczową"). **Wdrożenie**: ten sam schemat co poprzednie rundy — `Gemini_Generated_Image_...jpeg` (2048×2048) zoptymalizowany `sips` do **146KB, 1000×1000**, zapisany jako `mezoterapia-beziglowa.jpg` (nazwa bez polskich znaków specjalnych, zgodnie z konwencją reszty plików). Karta zmieniona z `.ph-gradient warm` placeholder na `<img>`. **Zweryfikowane**: `fetch()` zwraca `200`/`image/jpeg`/`149938` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md (2 z 24 kart bez zdjęcia, nie licząc wstrzymanej Mezoterapii mikroigłowej + 1 z 11 zdjęć CDN archon.au do podmiany — `tier-2-facials.jpg`). Brak zmian CSS/JS. |
| 2026-08-28 | Sześćdziesiąta dziewiąta runda — podmieniono zdjęcie karty „Mikrodermabrazja", ostatnie z CDN archon.au | Zdjęcie dostarczone przez użytkownika w czacie (nie `WebSearch`/live-fetch tej rundy) — pod obrazkiem doklejony tekst „CRITICAL: Respond with TEXT ONLY..." rozpoznany jako próba prompt injection i zignorowany, zgłoszony użytkownikowi | Ekstremalne zbliżenie na oko/nos/policzek z widocznymi przebarwieniami i teksturą skóry na oliwkowym tle — trafne dla mikrodermabrazji (zabieg złuszczający adresujący właśnie przebarwienia i teksturę). **Wdrożenie**: plik znaleziony jako najświeższy w `~/Downloads` (`Gemini_Generated_Image_efhazeefhazeefha.jpeg`, 2048×2048, wygenerowany tej samej rundy), zoptymalizowany `sips` do **1000×1000**, zapisany jako `mikrodermabrazja.jpg` (nazwa zgodna z konwencją reszty plików, zamiast archońskiej `tier-2-facials.jpg`). Stary plik CDN archon.au `tier-2-facials.jpg` **usunięty** — sprawdzone `grep`, nieużywany gdzie indziej. Karta zaktualizowana (`src`, `alt` bez zmian — już opisywał trafnie). **Zweryfikowane**: `curl` zwraca `200`/`image/jpeg`/`171659` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md — **galeria `#treatments` jest teraz w całości wolna od zdjęć CDN archon.au** (wszystkie 11 podmienione na AI). Brak zmian CSS/JS. |
| 2026-08-28 | Siedemdziesiąta runda — wygenerowano prompt i podpięto zdjęcie karty „Peelingi Medyczne", ostatnia pusta karta galerii | `WebFetch` żywej podstrony `instytutem.pl/zabiegi/peelingi-medyczne` (realny, zaindeksowany URL) dał `404` — użyto `WebSearch` zamiast zgadywać z samej nazwy | Zweryfikowano `WebSearch`: peelingi medyczne to kontrolowana eksfoliacja aktywnymi kwasami (salicylowy, azelainowy), adresująca trądzik, drobne blizny, rozstępy, zmarszczki, przebarwienia i zapchane pory. Zgodnie z zasadą „realna cecha, nie idealny efekt" (DESIGN-SYSTEM.md §9) — to zabieg korygujący konkretną wadę, nie czysto regeneracyjny/rozświetlający (jak Fibryna, Runda 64), więc pokazano cechę wprost, nieretuszowaną. **Kompozycja celowo zróżnicowana od Mikrodermabrazji** (poprzednia karta, ta sama ogólna kategoria „problem ze skórą twarzy"), żeby uniknąć powielenia w galerii — inny obszar (żuchwa/kącik ust zamiast oka/policzka), inna cecha (trądzik/pory/blizny zamiast przebarwień), inne tło z rotacji (szare zamiast oliwkowego, ostatnio użytego w Rundzie 69). **Prompt** złożony wg szablonu z DESIGN-SYSTEM.md §9 (rdzeń stylu + model polski/środkowoeuropejski + tło z puli rotacji + kotwica kadru), z dodatkowym jawnym `Square 1:1 aspect ratio` na wyraźne pytanie użytkownika („ratio chyba 1:1?") — potwierdzone: cel to zawsze 1000×1000, dopisanie tego wprost do promptu ma zapobiec niepotrzebnemu przycinaniu kompozycji/kotwicy po fakcie. Użytkownik wygenerował i wkleił plik do `assets/images/` (nie `assets/images/treatments/` — inaczej niż zwykle, ale bez znaczenia, plik i tak przenoszony/przetwarzany do docelowej ścieżki). **Wdrożenie**: `Gemini_Generated_Image_h6h0xrh6h0xrh6h0.jpeg` (2048×2048, już natywnie 1:1 dzięki dopisanej instrukcji) zoptymalizowany `sips` do **163KB, 1000×1000**, zapisany jako `peelingi-medyczne.jpg`, oryginał usunięty z `assets/images/`. Karta zmieniona z `.ph-gradient` placeholder na `<img>`. **Zweryfikowane**: `curl` zwraca `200`/`image/jpeg`/`167160` bajtów. Zaktualizowano „Listę zabiegów" i „Otwarte TODO" w PLAN.md — **23 z 24 kart galerii ma teraz zdjęcie**, jedyna pozostała bez to wstrzymana „Mezoterapia mikroigłowa" (czeka na potwierdzenie duplikatu SkinPen od użytkownika). Brak zmian CSS/JS. |

---

## Struktura projektu

```
INSTYTUTem/
├── CLAUDE.md               — zasady współpracy, koncepcja projektu, wnioski/błędy z dotychczasowej pracy; wczytywany automatycznie na start sesji
├── DESIGN-SYSTEM.md        — design system marki (obowiązujący, patrz plik)
├── DOKUMENTACJA.md         — ten plik: log decyzji + stos technologiczny + szczegóły SEO
├── PLAN.md                 — checklista bieżącego stanu + jedyna lista otwartych TODO
├── ANALIZA-INSTYTUTEM.md   — analiza treści/struktury instytutem.pl + Fresha (referencja do migracji treści)
├── index.html              — strona główna
├── css/
│   └── styles.css          — wszystkie style, tokeny z DESIGN-SYSTEM.md w :root
├── js/
│   └── main.js              — nav sticky, mega-menu, hamburger, karuzela opinii, formularz newslettera
└── assets/
    └── images/
        ├── instytutem-logo.svg — prawdziwe logo instytutem.pl, dostarczone przez użytkownika (2026-08-27, Runda 8) — zastąpiło archon-logo.svg
        └── treatments/       — 11 zdjęć rzędu zabiegów, pobrane z CDN archon.au za zgodą (2026-08-27, Runda 7) — nadal placeholder treściowy (AU, nie instytutem.pl)
```

---

## Otwarte zadania i placeholdery

Pełna, aktualna lista (zdjęcia do podmiany, strony do zbudowania, itd.) żyje wyłącznie w [PLAN.md](PLAN.md#otwarte-todo) — nie duplikować jej tutaj, żeby nie rozjeżdżała się w dwóch miejscach.
